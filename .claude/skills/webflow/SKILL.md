---
name: webflow
description: webflow integration via Composio MCP. Use when user needs to interact with webflow.
---

# webflow Integration

> Auto-generated by Composio Skills Extension on 2026-03-27T09:32:07.108Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Integration with webflow services.

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### WEBFLOW_CREATE_ASSET_FOLDER

**MCP Tool**: `mcp__composio__WEBFLOW_CREATE_ASSET_FOLDER`

Tool to create a new Asset Folder within a Webflow site. Use when you need to organize assets by creating folders. Requires assets:write scope.

**Parameters:**

- `site_id` (string, required): Unique identifier for the Webflow site
- `display_name` (string, required): A human readable name for the Asset Folder
- `parent_folder` (string, optional): An optional pointer to a parent Asset Folder ID. If not provided or set to null, the folder will be created at the root level.

### WEBFLOW_CREATE_BULK_COLLECTION_ITEMS

**MCP Tool**: `mcp__composio__WEBFLOW_CREATE_BULK_COLLECTION_ITEMS`

Tool to create one or multiple items in a Webflow CMS Collection across multiple locales. Use when you need to create up to 100 collection items in a single request. If cmsLocaleIds is not included for an item, it will only be created in the primary locale. Requires CMS:write scope.

**Parameters:**

- `items` (array, required): List of items to create (maximum 100 items per request). Each item must include fieldData with at least 'name' and 'slug' fields.
- `collection_id` (string, required): Unique identifier for the Webflow collection
- `skipInvalidFiles` (boolean, optional): When true, invalid files are skipped and processing continues. When false, the entire request fails if any file is invalid.

### WEBFLOW_CREATE_COLLECTION

**MCP Tool**: `mcp__composio__WEBFLOW_CREATE_COLLECTION`

Tool to create a new collection in a Webflow site. Each collection includes required 'name' and 'slug' fields which are generated automatically. Use this when you need to create a new CMS collection with custom fields. Required scope: cms:write

**Parameters:**

- `slug` (string, optional): URL-friendly identifier for the collection. If not provided, it will be auto-generated from displayName. Field slugs are automatically converted to lowercase with spaces replaced by hyphens
- `fields` (array, optional): An array of custom fields to add to the collection. Each collection automatically includes required 'name' and 'slug' fields which cannot be removed
- `site_id` (string, required): Unique identifier for the Webflow site where the collection will be created
- `displayName` (string, required): Name of the collection. Each collection name must be distinct within the site
- `singularName` (string, required): Singular name of each item in the collection (e.g., 'Blog Post' if the collection is 'Blog Posts')

### WEBFLOW_CREATE_COLLECTION_FIELD

**MCP Tool**: `mcp__composio__WEBFLOW_CREATE_COLLECTION_FIELD`

Tool to create a custom field in a Webflow collection. Use when you need to add a new field to an existing collection. Field validation is currently not available through the API. Bulk creation of fields is not supported - fields must be created one at a time.

**Parameters:**

- `id` (string, optional): Optional unique identifier for the field. If not provided, Webflow will auto-generate one
- `type` (string, required): The type of field to create. Choose the appropriate field type for your collection data
- `helpText` (string, optional): Additional text to help anyone filling out this field
- `metadata` (string, optional): Metadata for the field. Required for Option, Reference, and MultiReference field types. For Option fields: provide OptionFieldMetadata with 'options' array. For Reference/MultiReference fields: provide ReferenceFieldMetadata with 'collectionId'.
- `isEditable` (boolean, optional): Define whether the field is editable. Defaults to true if not specified
- `isRequired` (boolean, optional): Define whether the field is required in the collection. Defaults to false if not specified
- `displayName` (string, required): The display name of the field
- `collection_id` (string, required): Unique identifier for the Collection

### WEBFLOW_CREATE_COLLECTION_ITEM

**MCP Tool**: `mcp__composio__WEBFLOW_CREATE_COLLECTION_ITEM`

This tool creates a new item in a specified Webflow collection. It requires the collection_id and field_data parameters (including required name and slug) and optionally accepts an is_draft flag. Authentication is assumed to be provided, and the collection_id can be obtained by using the WEBFLOW_LIST_COLLECTIONS tool. IMPORTANT: The field_data keys must use the exact field 'slug' values from the collection schema, not display names. Use WEBFLOW_GET_COLLECTION first to retrieve the collection schema and identify the correct field slugs to use.

**Parameters:**

- `is_draft` (boolean, optional): Whether to create the item as a draft
- `field_data` (object, required): The data for the item's fields. Must include 'name' and 'slug' fields. IMPORTANT: Field keys must be the exact field 'slug' values from the collection schema, NOT display names or arbitrary names. Use WEBFLOW_GET_COLLECTION with the collection_id to retrieve the collection schema and find valid field slugs. For example, if a field has displayName='Author Name' but slug='author-name', you must use 'author-name' as the key.
- `collection_id` (string, required): The unique identifier of the Webflow collection

### WEBFLOW_CREATE_LIVE_COLLECTION_ITEM

**MCP Tool**: `mcp__composio__WEBFLOW_CREATE_LIVE_COLLECTION_ITEM`

Tool to create a collection item that will be immediately published to the live site. Use when you need items to appear on the live site instantly without staging. This bypasses the staged item workflow and creates items directly in the live database.

**Parameters:**

- `field_data` (object, required): The data for the item's fields. Must include 'name' and 'slug' fields. IMPORTANT: Field keys must be the exact field 'slug' values from the collection schema, NOT display names or arbitrary names. Use WEBFLOW_GET_COLLECTION with the collection_id to retrieve the collection schema and find valid field slugs. For example, if a field has displayName='Author Name' but slug='author-name', you must use 'author-name' as the key.
- `collection_id` (string, required): The unique identifier of the Webflow collection
- `skip_invalid_files` (boolean, optional): When true, invalid files are skipped and processing continues. When false, the entire request fails if any file is invalid.

### WEBFLOW_DELETE_ASSET

**MCP Tool**: `mcp__composio__WEBFLOW_DELETE_ASSET`

Tool to delete an Asset from Webflow. Use when you need to permanently remove an asset from a site. Requires assets:write scope.

**Parameters:**

- `asset_id` (string, required): Unique identifier for an Asset on a site

### WEBFLOW_DELETE_COLLECTION

**MCP Tool**: `mcp__composio__WEBFLOW_DELETE_COLLECTION`

Tool to delete a collection from Webflow using its unique identifier. Use when you need to permanently remove a collection. This operation requires the 'cms:write' scope and cannot be undone.

**Parameters:**

- `collection_id` (string, required): Unique identifier for the collection to delete

### WEBFLOW_DELETE_COLLECTION_FIELD

**MCP Tool**: `mcp__composio__WEBFLOW_DELETE_COLLECTION_FIELD`

Tool to delete a custom field from a Webflow collection. Use when you need to permanently remove a field from a collection schema. This endpoint does not currently support bulk deletion and requires the 'cms:write' authentication scope.

**Parameters:**

- `field_id` (string, required): Unique identifier for a Field in a collection
- `collection_id` (string, required): Unique identifier for a Collection

### WEBFLOW_DELETE_COLLECTION_ITEM

**MCP Tool**: `mcp__composio__WEBFLOW_DELETE_COLLECTION_ITEM`

This tool allows you to delete a specific item from a collection in Webflow. It permanently removes the item from the specified collection and complements existing collection management tools. The tool requires a collection_id to identify the collection and an item_id to identify the item, with an optional cms_locale_id parameter for handling multi-language content.

**Parameters:**

- `item_id` (string, required): The unique identifier of the item to delete
- `cms_locale_id` (string, optional): The locale ID for multi-language content
- `collection_id` (string, required): The unique identifier of the Webflow collection

### WEBFLOW_DELETE_COLLECTION_ITEMS

**MCP Tool**: `mcp__composio__WEBFLOW_DELETE_COLLECTION_ITEMS`

Tool to delete multiple items from a Webflow collection in a single request. Use when you need to remove multiple collection items efficiently. Items are deleted only in the primary locale unless cmsLocaleIds are specified. Requires CMS:write scope.

**Parameters:**

- `items` (array, required): List of items to delete from the collection. Each item must have an 'id' field and optionally 'cmsLocaleIds' for locale-specific deletion.
- `collection_id` (string, required): Unique identifier of the Webflow collection

### WEBFLOW_DELETE_WEBHOOK

**MCP Tool**: `mcp__composio__WEBFLOW_DELETE_WEBHOOK`

Tool to remove a Webhook from Webflow. Use when you need to delete an existing webhook by its ID. Requires 'sites:read' scope.

**Parameters:**

- `webhook_id` (string, required): Unique identifier for the Webhook to delete

### WEBFLOW_FULFILL_ORDER

**MCP Tool**: `mcp__composio__WEBFLOW_FULFILL_ORDER`

This tool allows you to mark an order as fulfilled in Webflow's e-commerce system. It's essential for managing order fulfillment in Webflow stores.

**Parameters:**

- `site_id` (string, required): Unique identifier for the site where the order exists
- `order_id` (string, required): Unique identifier for the order to be fulfilled
- `send_order_fulfilled_email` (boolean, optional): Whether to send the order fulfillment email to the customer

### WEBFLOW_GET_ASSET

**MCP Tool**: `mcp__composio__WEBFLOW_GET_ASSET`

Retrieves detailed information about a specific asset by its ID from a Webflow site. Returns asset metadata including content type, size, URLs, variants, and other properties. Use this when you need to access details about an uploaded asset. This tool requires the 'assets:read' authentication scope.

**Parameters:**

- `locale` (string, optional): Unique identifier for a specific locale. Used when working with localized content.
- `asset_id` (string, required): Unique identifier for the asset to retrieve

### WEBFLOW_GET_ASSET_FOLDER

**MCP Tool**: `mcp__composio__WEBFLOW_GET_ASSET_FOLDER`

Tool to retrieve details about a specific Asset Folder in Webflow. Use when you need information about a folder's name, parent folder, assets it contains, and metadata like creation/update dates. Requires assets:read scope.

**Parameters:**

- `asset_folder_id` (string, required): Unique identifier for the Asset Folder

### WEBFLOW_GET_COLLECTION

**MCP Tool**: `mcp__composio__WEBFLOW_GET_COLLECTION`

Retrieves a specific collection by its ID from a Webflow site. This endpoint returns detailed information about the collection, including its name, slug, and schema. It requires a valid collection_id as a parameter and complements the WEBFLOW_LIST_COLLECTIONS tool for accessing specific collection details. This tool requires the 'cms:read' authentication scope.

**Parameters:**

- `collection_id` (string, required): Unique identifier for the collection to retrieve

### WEBFLOW_GET_COLLECTION_ITEM

**MCP Tool**: `mcp__composio__WEBFLOW_GET_COLLECTION_ITEM`

This tool retrieves a specific item from a Webflow collection. It allows users to fetch detailed information about a single collection item using its unique identifier. The tool is used to get an item's details such as its id, timestamps (lastPublished, lastUpdated, createdOn), archive/draft status, fieldData, and cmsLocaleId. This tool requires appropriate 'cms:read' authentication scope.

**Parameters:**

- `item_id` (string, required): Unique identifier for the item within the collection
- `cms_locale_id` (string, optional): Unique identifier for a CMS Locale. To query multiple locales, input a comma-separated string.
- `collection_id` (string, required): Unique identifier for the collection

### WEBFLOW_GET_COMPONENT_PROPERTIES

**MCP Tool**: `mcp__composio__WEBFLOW_GET_COMPONENT_PROPERTIES`

Tool to get the default property values of a component definition. Use when you need to retrieve text content properties (plain text or rich text) for a specific component. If you do not include a localeId in your request, the response will return any properties that can be localized from the Primary locale. Required scope: components:read

**Parameters:**

- `limit` (integer, optional): Maximum number of records to return (max: 100)
- `offset` (integer, optional): Offset for pagination
- `site_id` (string, required): Unique identifier for the site
- `branchId` (string, optional): Scope the operation to work on a specific branch
- `localeId` (string, optional): Unique identifier for a specific locale. If not included, returns properties that can be localized from the Primary locale
- `component_id` (string, required): Unique identifier for the component

### WEBFLOW_GET_CUSTOM_DOMAINS

**MCP Tool**: `mcp__composio__WEBFLOW_GET_CUSTOM_DOMAINS`

Tool to retrieve all custom domains associated with a specific Webflow site. Use when you need to list the custom domain URLs configured for a site.

**Parameters:**

- `site_id` (string, required): Unique identifier for a Site

### WEBFLOW_GET_ITEM_INVENTORY

**MCP Tool**: `mcp__composio__WEBFLOW_GET_ITEM_INVENTORY`

This tool retrieves the current inventory levels for a specific SKU item in a Webflow e-commerce site. It provides information about the item's inventory quantity and type (finite or infinite) and complements the WEBFLOW_UPDATE_ITEM_INVENTORY action by allowing users to check inventory levels before making any updates. Requirements: - E-commerce must be enabled on the Webflow site - The collection_id must be a SKU collection ID (not a regular CMS collection) - The item_id must be a valid SKU item ID - Requires the 'ecommerce:read' authentication scope

**Parameters:**

- `item_id` (string, required): Unique identifier for the SKU Item whose inventory you want to retrieve. Use the List Products & SKUs API to find SKU item IDs.
- `collection_id` (string, required): Unique identifier for the SKU Collection (not a regular CMS collection). This is automatically created when e-commerce is enabled on the site. Use the List Collections API to find the SKU collection ID.

## Usage Examples

#### WEBFLOW_CREATE_ASSET_FOLDER

```
Use MCP tool: mcp__composio__WEBFLOW_CREATE_ASSET_FOLDER
Parameters: {
  "site_id": "your_site_id_here",
  "display_name": "your_display_name_here"
}
```

#### WEBFLOW_CREATE_BULK_COLLECTION_ITEMS

```
Use MCP tool: mcp__composio__WEBFLOW_CREATE_BULK_COLLECTION_ITEMS
Parameters: {
  "collection_id": "your_collection_id_here",
  "items": "value"
}
```

#### WEBFLOW_CREATE_COLLECTION

```
Use MCP tool: mcp__composio__WEBFLOW_CREATE_COLLECTION
Parameters: {
  "site_id": "your_site_id_here",
  "displayName": "your_displayName_here"
}
```

## Quick Reference

| MCP Tool                                              | Description                                           |
| ----------------------------------------------------- | ----------------------------------------------------- |
| `mcp__composio__WEBFLOW_CREATE_ASSET_FOLDER`          | Tool to create a new Asset Folder within a Webflow... |
| `mcp__composio__WEBFLOW_CREATE_BULK_COLLECTION_ITEMS` | Tool to create one or multiple items in a Webflow ... |
| `mcp__composio__WEBFLOW_CREATE_COLLECTION`            | Tool to create a new collection in a Webflow site.... |
| `mcp__composio__WEBFLOW_CREATE_COLLECTION_FIELD`      | Tool to create a custom field in a Webflow collect... |
| `mcp__composio__WEBFLOW_CREATE_COLLECTION_ITEM`       | This tool creates a new item in a specified Webflo... |
| `mcp__composio__WEBFLOW_CREATE_LIVE_COLLECTION_ITEM`  | Tool to create a collection item that will be imme... |
| `mcp__composio__WEBFLOW_DELETE_ASSET`                 | Tool to delete an Asset from Webflow. Use when you... |
| `mcp__composio__WEBFLOW_DELETE_COLLECTION`            | Tool to delete a collection from Webflow using its... |
| `mcp__composio__WEBFLOW_DELETE_COLLECTION_FIELD`      | Tool to delete a custom field from a Webflow colle... |
| `mcp__composio__WEBFLOW_DELETE_COLLECTION_ITEM`       | This tool allows you to delete a specific item fro... |
| `mcp__composio__WEBFLOW_DELETE_COLLECTION_ITEMS`      | Tool to delete multiple items from a Webflow colle... |
| `mcp__composio__WEBFLOW_DELETE_WEBHOOK`               | Tool to remove a Webhook from Webflow. Use when yo... |
| `mcp__composio__WEBFLOW_FULFILL_ORDER`                | This tool allows you to mark an order as fulfilled... |
| `mcp__composio__WEBFLOW_GET_ASSET`                    | Retrieves detailed information about a specific as... |
| `mcp__composio__WEBFLOW_GET_ASSET_FOLDER`             | Tool to retrieve details about a specific Asset Fo... |
| `mcp__composio__WEBFLOW_GET_COLLECTION`               | Retrieves a specific collection by its ID from a W... |
| `mcp__composio__WEBFLOW_GET_COLLECTION_ITEM`          | This tool retrieves a specific item from a Webflow... |
| `mcp__composio__WEBFLOW_GET_COMPONENT_PROPERTIES`     | Tool to get the default property values of a compo... |
| `mcp__composio__WEBFLOW_GET_CUSTOM_DOMAINS`           | Tool to retrieve all custom domains associated wit... |
| `mcp__composio__WEBFLOW_GET_ITEM_INVENTORY`           | This tool retrieves the current inventory levels f... |

## When to Use This Integration

**Use webflow when:**

- Interacting with webflow services
- Automating webflow workflows
- Reading and writing webflow data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:

1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
