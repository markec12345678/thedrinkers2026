---
name: linkedin
description: LinkedIn is a professional networking platform enabling job seekers, companies, and thought leaders to connect, share content, and discover business opportunities integration via Composio MCP. Use when user needs to interact with LinkedIn.
---

# LinkedIn Integration

> Auto-generated by Composio Skills Extension on 2026-03-27T09:37:42.720Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

LinkedIn is a professional networking platform enabling job seekers, companies, and thought leaders to connect, share content, and discover business opportunities

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### LINKEDIN_CREATE_ARTICLE_OR_URL_SHARE

**MCP Tool**: `mcp__composio__LINKEDIN_CREATE_ARTICLE_OR_URL_SHARE`

Tool to create an article or URL share on LinkedIn using the UGC Posts API. Use when you need to share a link with optional commentary on LinkedIn. Supports sharing URLs as articles with customizable visibility settings.

**Parameters:**

- `author` (string, required): URN of the author (person or organization). For a person: 'urn:li:person:{id}'. For an organization: 'urn:li:organization:{id}'. Use GET_MY_INFO action to get your person ID.
- `visibility` (object, required): Visibility settings for the post. Controls who can see the shared content.
- `lifecycleState` (string, optional): State of the post. Use 'PUBLISHED' to post immediately or 'DRAFT' to save as draft.
- `specificContent` (object, required): Container for the share content including URL, commentary, and media category.

### LINKEDIN_CREATE_COMMENT_ON_POST

**MCP Tool**: `mcp__composio__LINKEDIN_CREATE_COMMENT_ON_POST`

Tool to create a first-level or nested comment on a LinkedIn share, UGC post, or parent comment via the Social Actions Comments API. Use when you need to engage with posts by adding comments or replying to existing comments. Supports text comments with optional @-mentions and image attachments.

**Parameters:**

- `actor` (string, required): Entity which authored the comment. For personal comments: 'urn:li:person:{id}'. For organization comments: 'urn:li:organization:{id}'. Use the GET_USER_INFO action to retrieve the 'id' field.
- `object` (string, required): The URN of the share or ugcPost that contains the comment. Typically the same as target_urn for first-level comments, or the root post URN for nested comments. Format: 'urn:li:share:{id}' or 'urn:li:ugcPost:{id}'. NOTE: Activity URNs ('urn:li:activity:{id}') are NOT reliably supported - use the share or ugcPost URN instead.
- `content` (array, optional): Array of content entities such as images to include in the comment. Each entity should contain an image URN or other content type.
- `message` (object, required): The comment message content including text and optional @-mentions.
- `target_urn` (string, required): The URN of the share, UGC post, or parent comment where the comment will be created. Format: 'urn:li:share:{id}', 'urn:li:ugcPost:{id}', or 'urn:li:comment:({parentUrn},{commentId})'. NOTE: Activity URNs ('urn:li:activity:{id}') are NOT reliably supported - use the share or ugcPost URN instead. To find the correct URN, use the post creation response or retrieve it via the Posts API.
- `parentComment` (string, optional): For nested comments (replies), the URN of the parent comment being replied to. Format: 'urn:li:comment:({parentUrn},{commentId})'. Leave empty for first-level comments on the post.

### LINKEDIN_CREATE_LINKED_IN_POST

**MCP Tool**: `mcp__composio__LINKEDIN_CREATE_LINKED_IN_POST`

Creates a new post on LinkedIn for the authenticated user or an organization they manage. Requires w_member_social scope for posting as a person, and w_organization_social scope for posting as an organization (with ADMINISTRATOR, DIRECT_SPONSORED_CONTENT_POSTER, or CONTENT_ADMIN role).

**Parameters:**

- `author` (string, required): The URN of the LinkedIn member or organization creating the post. IMPORTANT: Replace placeholder text with actual ID values. For a person: use 'urn:li:person:<actual_person_id>' (e.g., 'urn:li:person:F6kGF1Uduq' or 'urn:li:person:123456789'). Requires w_member_social scope. For an organization: use 'urn:li:organization:<actual_org_id>' (e.g., 'urn:li:organization:987654321'). Requires w_organization_social scope and you must have ADMINISTRATOR, DIRECT_SPONSORED_CONTENT_POSTER, or CONTENT_ADMIN role for the organization. Get your person ID using the GET_MY_INFO action, or get organization IDs from GET_COMPANY_INFO. Do NOT pass literal placeholder text like '{id}' or '{organization_id}'.
- `images` (array, optional): Array of images to upload and attach to the post. Each element is a Supports 1-20 images. For a single image, use a list with one element. LinkedIn will automatically use the appropriate content type (media for single image, multiImage for 2+ images).
- `container` (string, optional): Container entity URN for posting within a specific container (e.g., 'urn:li:group:123456' for group posts). Use this field when posting to a group or other container entity.
- `commentary` (string, required): The main text content of the post. This field supports plain text and @-mentions (e.g., @[LinkedIn Member](urn:li:person:xxxx)). Maximum length: 3000 characters.
- `visibility` (string, optional): Controls who can see the post. 'PUBLIC' makes it visible to everyone, 'CONNECTIONS' to connections only, 'LOGGED_IN' to signed-in LinkedIn members, and 'CONTAINER' for specific group/event posts.
- `distribution` (object, optional): Specifies distribution rules for the post, including feed distribution and targeting.
- `lifecycleState` (string, optional): The state of the post. Use 'PUBLISHED' to post directly, 'DRAFT' to save as a draft, or 'PUBLISH_REQUESTED' if the post requires review before publishing.
- `reshareContext` (object, optional): Context for resharing another post.
- `contentLandingPage` (string, optional): URL of the landing page that opens when the call-to-action button is clicked. Required when contentCallToActionLabel is specified.
- `contentCallToActionLabel` (string, optional): Call-to-action button labels for posts.
- `isReshareDisabledByAuthor` (boolean, optional): Set to true to prevent others from resharing this post.

### LINKEDIN_DELETE_LINKED_IN_POST

**MCP Tool**: `mcp__composio__LINKEDIN_DELETE_LINKED_IN_POST`

Deletes a specific LinkedIn post (share) by its unique `share_id`, which must correspond to an existing share.

**Parameters:**

- `share_id` (string, required): Unique identifier of the LinkedIn share (post) to be deleted. Accepts either numeric ID (e.g., '7245341016004718592') or URN format (e.g., 'urn:li:share:7245341016004718592').

### LINKEDIN_DELETE_POST

**MCP Tool**: `mcp__composio__LINKEDIN_DELETE_POST`

Delete a LinkedIn post using the Posts API REST endpoint. Supports both ugcPost and share URN formats. The endpoint is idempotent - previously deleted posts return success (204).

**Parameters:**

- `post_urn` (string, required): The URN of the post to delete. Accepts either ugcPost or share URN formats (e.g., 'urn:li:ugcPost:7890123456' or 'urn:li:share:7245341016004718592'). The URN will be URL-encoded automatically.

### LINKEDIN_DELETE_UGC_POST

**MCP Tool**: `mcp__composio__LINKEDIN_DELETE_UGC_POST`

Delete a UGC post using the legacy UGC Post API endpoint. Use when you need to delete a post using the v2/ugcPosts endpoint. Deletion is idempotent - previously deleted posts also return success.

**Parameters:**

- `ugc_post_urn` (string, required): The URN of the UGC post or share to delete. Format: 'urn:li:ugcPost:{id}' or 'urn:li:share:{id}'. Will be URL-encoded automatically by the action.

### LINKEDIN_GET_AD_TARGETING_FACETS

**MCP Tool**: `mcp__composio__LINKEDIN_GET_AD_TARGETING_FACETS`

Tool to retrieve available ad targeting facets from LinkedIn Marketing API. Use when you need to discover what targeting options are available for ad campaigns (e.g., locations, industries, job functions).

**Parameters:**

- No parameters required

### LINKEDIN_GET_AUDIENCE_COUNTS

**MCP Tool**: `mcp__composio__LINKEDIN_GET_AUDIENCE_COUNTS`

Retrieves audience size counts for specified targeting criteria. Use when estimating reach for LinkedIn ad campaigns or targeted content.

**Parameters:**

- `q` (string, optional): Query type parameter. Must be 'targetingCriteriaV2' for targeting criteria queries.
- `targetingCriteria` (string, required): Targeting criteria with URL-encoded URNs. Format: (include:(and:List((or:(urn%3Ali%3AadTargetingFacet%3AFACET:List(urn%3Ali%3ATYPE%3AID)))))). IMPORTANT: Only the URN colons should be encoded as %3A, keep the structure syntax (parentheses, colons in 'and:' and 'or:') unencoded. Example for locations: (include:(and:List((or:(urn%3Ali%3AadTargetingFacet%3Alocations:List(urn%3Ali%3Ageo%3A102221843))))))

### LINKEDIN_GET_COMPANY_INFO

**MCP Tool**: `mcp__composio__LINKEDIN_GET_COMPANY_INFO`

Retrieves organizations where the authenticated user has specific roles (ACLs), to determine their management or content posting capabilities for LinkedIn company pages.

**Parameters:**

- `role` (string, optional): The specific role to filter organization ACLs by.
- `count` (integer, optional): The number of organization ACLs to return per page.
- `start` (integer, optional): The starting index for pagination, representing the number of initial ACLs to skip.
- `state` (string, optional): The approval state of the role to filter by.

### LINKEDIN_GET_IMAGE

**MCP Tool**: `mcp__composio__LINKEDIN_GET_IMAGE`

Tool to retrieve details of a LinkedIn image using its URN. Use when you need to check image status, get download URLs, or access image metadata for a single image.

**Parameters:**

- `image_urn` (string, required): The image URN identifier. Can be provided as 'urn:li:image:XXX' or just the ID 'XXX'. The URN will be URL-encoded automatically when making the API request.

### LINKEDIN_GET_IMAGES

**MCP Tool**: `mcp__composio__LINKEDIN_GET_IMAGES`

Tool to retrieve image metadata including download URLs, status, and dimensions from LinkedIn's Images API. Use when you need to access image details for posts, profiles, or media library assets.

**Parameters:**

- `ids` (array, required): List of image URNs to retrieve. Each URN should be in the format 'urn:li:image:{ID}' (e.g., 'urn:li:image:C4E10AQFn10iWtKexVA'). Maximum recommended batch size depends on LinkedIn's API limits.
- `linkedin_version` (string, optional): LinkedIn API version in YYYYMM format. Defaults to 202511.

### LINKEDIN_GET_MY_INFO

**MCP Tool**: `mcp__composio__LINKEDIN_GET_MY_INFO`

Fetches the authenticated LinkedIn user's profile information including name, headline, profile picture, and other profile details.

**Parameters:**

- No parameters required

### LINKEDIN_GET_NETWORK_SIZE

**MCP Tool**: `mcp__composio__LINKEDIN_GET_NETWORK_SIZE`

Tool to retrieve the follower count for a LinkedIn organization. Use when you need to get the number of members following a specific company or organization on LinkedIn.

**Parameters:**

- `edgeType` (string, optional): The type of network relationship to count. Use COMPANY_FOLLOWED_BY_MEMBER to get follower count.
- `linkedinVersion` (string, optional): LinkedIn API version to use for the request. Format: YYYYMM (e.g., 202510 for October 2025).
- `organization_id` (string, required): The unique identifier for the LinkedIn organization. This is the numeric ID from the organization URN (e.g., for 'urn:li:organization:3803', use '3803').

### LINKEDIN_GET_ORG_PAGE_STATS

**MCP Tool**: `mcp__composio__LINKEDIN_GET_ORG_PAGE_STATS`

Tool to retrieve page statistics for a LinkedIn organization page. Use when you need engagement metrics like page views and custom button clicks. Supports both lifetime statistics (all-time data segmented by demographics) and time-bound statistics (aggregate data for specific time ranges). Requires rw_organization_admin permission with ADMINISTRATOR role for the organization.

**Parameters:**

- `organization` (string, required): Organization URN in format urn:li:organization:{id}. Example: urn:li:organization:2414183
- `timeRangeEnd` (integer, optional): Inclusive ending timestamp in milliseconds since epoch. Only required for time-bound statistics. Omit for lifetime statistics.
- `timeRangeStart` (integer, optional): Exclusive starting timestamp in milliseconds since epoch. Only required for time-bound statistics. Omit for lifetime statistics.
- `linkedinVersion` (string, optional): LinkedIn API version in YYYYMM format. Defaults to 202602.
- `timeGranularityType` (string, optional): Granularity of the statistics. Use DAY for daily statistics or MONTH for monthly statistics. Only required for time-bound statistics. Omit for lifetime statistics.

### LINKEDIN_GET_PERSON

**MCP Tool**: `mcp__composio__LINKEDIN_GET_PERSON`

Retrieves a LinkedIn member's profile information by their person ID. Returns lite profile fields (name, profile picture) by default, or basic profile fields (including headline and vanity name) with appropriate permissions.

**Parameters:**

- `person_id` (string, required): The unique identifier of the LinkedIn member. Note: Each member id is unique to the context of your application only.

### LINKEDIN_GET_POST_CONTENT

**MCP Tool**: `mcp__composio__LINKEDIN_GET_POST_CONTENT`

Tool to retrieve detailed post content including text, images, videos, and metadata from LinkedIn by post URN. Use when you need to fetch the full content and details of a specific LinkedIn post.

**Parameters:**

- `post_id` (string, required): Post URN identifier in the format 'urn:li:ugcPost:{id}' or 'urn:li:share:{id}'. Can also be URL-encoded. Example: 'urn:li:ugcPost:7428263313739988992'
- `linkedin_version` (string, optional): LinkedIn API version in YYYYMM format. Defaults to 202601.

### LINKEDIN_GET_SHARE_STATS

**MCP Tool**: `mcp__composio__LINKEDIN_GET_SHARE_STATS`

Retrieves share statistics for a LinkedIn organization, including impressions, clicks, likes, comments, and shares. Use to analyze content performance for an organization page. Optionally filter by time intervals to get time-bound statistics.

**Parameters:**

- `time_intervals` (string, optional): Time-bound statistics filter in RestLi 2.0 complex object format. Specify time range and granularity as: '(timeRange:(start:<start_timestamp>,end:<end_timestamp>),timeGranularityType:<GRANULARITY>)' where timestamps are in milliseconds since epoch and GRANULARITY is one of: DAY, WEEK, MONTH. Example: '(timeRange:(start:1634018799000,end:1634623599000),timeGranularityType:DAY)'. If omitted, returns lifetime statistics.
- `organizational_entity` (string, required): The URN of the organization to retrieve share statistics for. Must be in the format 'urn:li:organization:<id>' where <id> is the organization ID (e.g., 'urn:li:organization:2414183'). Get organization IDs using the GET_COMPANY_INFO action.

### LINKEDIN_GET_VIDEOS

**MCP Tool**: `mcp__composio__LINKEDIN_GET_VIDEOS`

Retrieves video metadata from LinkedIn Marketing API. Supports single video retrieval, batch retrieval (multiple videos), and finding videos by associated account with pagination. Use when you need to get video details including duration, dimensions, status, download URLs, and media library information.

**Parameters:**

- `count` (integer, optional): Number of elements to return for pagination, up to 1,000. Only used with 'associated_account'. Defaults to 10.
- `start` (integer, optional): Index where returned elements begin for pagination. Only used with 'associated_account'. Defaults to 0.
- `video_ids` (array, optional): List of video URNs for batch retrieval (e.g., ['urn:li:video:C4D10AQE9WE5n4uJKUg', 'urn:li:video:C5F10AQElP1fVH1Ke6g']). Use this for batch retrieval. Mutually exclusive with 'video_urn' and 'associated_account'.
- `video_urn` (string, optional): Single video URN to retrieve (e.g., 'urn:li:video:C4E10AQGUkQY7trgh-Q'). Use this for single video retrieval. Mutually exclusive with 'video_ids' and 'associated_account'.
- `associated_account` (string, optional): Sponsored account URN to find videos by associated account (e.g., 'urn:li:sponsoredAccount:123456789'). Use this to find all videos for an account. Mutually exclusive with 'video_urn' and 'video_ids'.
- `media_library_status` (string, optional): Filter by media library status. Only used with 'associated_account'. When omitted, returns all videos regardless of status.

### LINKEDIN_INITIALIZE_IMAGE_UPLOAD

**MCP Tool**: `mcp__composio__LINKEDIN_INITIALIZE_IMAGE_UPLOAD`

Tool to initialize an image upload to LinkedIn and return a presigned upload URL plus the resulting image URN. Use when you need to prepare an image upload for LinkedIn posts. After calling this tool, upload the image bytes to the returned upload_url via PUT request, then use the image URN in CREATE_LINKED_IN_POST action.

**Parameters:**

- `owner` (string, required): Owner URN (person or organization) that will own the uploaded image. Must be in the format 'urn:li:person:{id}' for personal posts or 'urn:li:organization:{id}' for organization posts. Use the GET_MY_INFO action to retrieve the person ID.

### LINKEDIN_LIST_REACTIONS

**MCP Tool**: `mcp__composio__LINKEDIN_LIST_REACTIONS`

Retrieves reactions (likes, celebrations, etc.) on a LinkedIn entity such as a share, post, or comment. Use when you need to see who reacted to content and what type of reactions were used.

**Parameters:**

- `sort` (string, optional): Sort order for reactions. CHRONOLOGICAL sorts by created date ascending, REVERSE_CHRONOLOGICAL sorts by created date descending (default), RELEVANCE sorts by relevance to the viewer.
- `count` (integer, optional): Number of reactions to return per page.
- `start` (integer, optional): Starting index for pagination (number of reactions to skip).
- `entity` (string, required): The URN of the entity (share, post, or comment) to get reactions for. Format: 'urn:li:share:{id}', 'urn:li:ugcPost:{id}', 'urn:li:activity:{id}', or 'urn:li:comment:(activity:{activityId},{commentId})'. Do not URL-encode this value.

## Usage Examples

#### LINKEDIN_CREATE_ARTICLE_OR_URL_SHARE

```
Use MCP tool: mcp__composio__LINKEDIN_CREATE_ARTICLE_OR_URL_SHARE
Parameters: {
  "author": "your_author_here",
  "specificContent": "value"
}
```

#### LINKEDIN_CREATE_COMMENT_ON_POST

```
Use MCP tool: mcp__composio__LINKEDIN_CREATE_COMMENT_ON_POST
Parameters: {
  "target_urn": "your_target_urn_here",
  "actor": "your_actor_here"
}
```

#### LINKEDIN_CREATE_LINKED_IN_POST

```
Use MCP tool: mcp__composio__LINKEDIN_CREATE_LINKED_IN_POST
Parameters: {
  "author": "your_author_here",
  "commentary": "your_commentary_here"
}
```

## Quick Reference

| MCP Tool                                              | Description                                           |
| ----------------------------------------------------- | ----------------------------------------------------- |
| `mcp__composio__LINKEDIN_CREATE_ARTICLE_OR_URL_SHARE` | Tool to create an article or URL share on LinkedIn... |
| `mcp__composio__LINKEDIN_CREATE_COMMENT_ON_POST`      | Tool to create a first-level or nested comment on ... |
| `mcp__composio__LINKEDIN_CREATE_LINKED_IN_POST`       | Creates a new post on LinkedIn for the authenticat... |
| `mcp__composio__LINKEDIN_DELETE_LINKED_IN_POST`       | Deletes a specific LinkedIn post (share) by its un... |
| `mcp__composio__LINKEDIN_DELETE_POST`                 | Delete a LinkedIn post using the Posts API REST en... |
| `mcp__composio__LINKEDIN_DELETE_UGC_POST`             | Delete a UGC post using the legacy UGC Post API en... |
| `mcp__composio__LINKEDIN_GET_AD_TARGETING_FACETS`     | Tool to retrieve available ad targeting facets fro... |
| `mcp__composio__LINKEDIN_GET_AUDIENCE_COUNTS`         | Retrieves audience size counts for specified targe... |
| `mcp__composio__LINKEDIN_GET_COMPANY_INFO`            | Retrieves organizations where the authenticated us... |
| `mcp__composio__LINKEDIN_GET_IMAGE`                   | Tool to retrieve details of a LinkedIn image using... |
| `mcp__composio__LINKEDIN_GET_IMAGES`                  | Tool to retrieve image metadata including download... |
| `mcp__composio__LINKEDIN_GET_MY_INFO`                 | Fetches the authenticated LinkedIn user's profile ... |
| `mcp__composio__LINKEDIN_GET_NETWORK_SIZE`            | Tool to retrieve the follower count for a LinkedIn... |
| `mcp__composio__LINKEDIN_GET_ORG_PAGE_STATS`          | Tool to retrieve page statistics for a LinkedIn or... |
| `mcp__composio__LINKEDIN_GET_PERSON`                  | Retrieves a LinkedIn member's profile information ... |
| `mcp__composio__LINKEDIN_GET_POST_CONTENT`            | Tool to retrieve detailed post content including t... |
| `mcp__composio__LINKEDIN_GET_SHARE_STATS`             | Retrieves share statistics for a LinkedIn organiza... |
| `mcp__composio__LINKEDIN_GET_VIDEOS`                  | Retrieves video metadata from LinkedIn Marketing A... |
| `mcp__composio__LINKEDIN_INITIALIZE_IMAGE_UPLOAD`     | Tool to initialize an image upload to LinkedIn and... |
| `mcp__composio__LINKEDIN_LIST_REACTIONS`              | Retrieves reactions (likes, celebrations, etc.) on... |

## When to Use This Integration

**Use LinkedIn when:**

- Interacting with LinkedIn services
- Automating LinkedIn workflows
- Reading and writing LinkedIn data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:

1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
