---
name: hubspot
description: hubspot integration via Composio MCP. Use when user needs to interact with hubspot.
---

# hubspot Integration

> Auto-generated by Composio Skills Extension on 2026-03-27T09:32:08.273Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Integration with hubspot services.

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### HUBSPOT_ADD_ASSET_ASSOCIATION

**MCP Tool**: `mcp__composio__HUBSPOT_ADD_ASSET_ASSOCIATION`

Associates an existing asset ('FORM', 'OBJECT_LIST', or 'EXTERNAL_WEB_URL') with a specified HubSpot marketing campaign.

**Parameters:**

- `assetId` (string, required): The unique identifier of the asset to be associated with the campaign. This ID is specific to the assetType. For FORMs, use the form's numeric ID. For OBJECT_LIST, use the list ID (ILS ID). For EMAIL, use the email's content ID. For LANDING_PAGE or BLOG_POST, use the page/post ID. These IDs can be obtained from HubSpot's UI (in the asset's URL or details panel) or by using the corresponding list/search actions for that asset type.
- `assetType` (string, required): Type of asset to associate with the campaign. Commonly supported types include: 'FORM', 'OBJECT_LIST' (Static/Contact List), 'EXTERNAL_WEB_URL', 'EMAIL', 'LANDING_PAGE', 'BLOG_POST', 'CTA', 'WORKFLOW', 'SOCIAL_POST', 'WEBSITE_PAGE', 'SEQUENCE', 'MEETING_EVENT', 'PLAYBOOK', 'FEEDBACK_SURVEY', 'SALES_DOCUMENT'. HubSpot continues to expand asset type support over time.
- `campaignGuid` (string, required): The unique identifier (UUID) of the HubSpot campaign to which the asset will be associated.

### HUBSPOT_ADD_TOKEN_TO_EVENT_TEMPLATE

**MCP Tool**: `mcp__composio__HUBSPOT_ADD_TOKEN_TO_EVENT_TEMPLATE`

Adds a new custom data token to an existing event template for a specified HubSpot application, optionally populating a CRM object property if objectPropertyName is provided.

**Parameters:**

- `name` (string, required): The internal name of the token, used for referencing it within templates. Must be unique for this event template. Allowed characters: alphanumeric, periods (.), dashes (-), or underscores (\_).
- `type` (string, required): The data type of the token. Determines how the token's value is stored and validated.
- `appId` (integer, required): Numeric identifier of the target application associated with the event template. Provided in the URL path.
- `label` (string, required): The user-facing label for the token. This label is used for list segmentation and in reporting.
- `options` (array, optional): A list of options for the token, required and applicable only if the token `type` is 'enumeration'. Each option must have a 'label' and a 'value'.
- `eventTemplateId` (string, required): Unique identifier of the event template. Provided in the URL path.
- `objectPropertyName` (string, optional): The name of an existing CRM object property (e.g., 'dealstage', 'lifecyclestage'). If provided, this token will populate the specified CRM object property associated with the event. This allows for building or updating CRM objects through the Timeline API.

### HUBSPOT_ARCHIVE_BATCH_OF_FEEDBACK_SUBMISSIONS

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_BATCH_OF_FEEDBACK_SUBMISSIONS`

Asynchronously archives a batch of HubSpot feedback submissions using their unique IDs, which must correspond to valid and existing submissions; the operation is queued, and submissions are moved from active views without being deleted.

**Parameters:**

- `inputs` (array, required): A list of objects, where each object contains the 'id' of a feedback submission to be archived.

### HUBSPOT_ARCHIVE_BATCH_OF_LINE_ITEMS

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_BATCH_OF_LINE_ITEMS`

Archives a batch of existing line items by their unique IDs in HubSpot CRM; this operation is irreversible via the API.

**Parameters:**

- `inputs` (array, required): A list of objects, where each object contains the 'id' of a line item to be archived.

### HUBSPOT_ARCHIVE_BATCH_OF_OBJECTS

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_BATCH_OF_OBJECTS`

Archives a batch of existing, non-archived CRM objects of a specified `objectType` by their IDs, effectively hiding them from active use.

**Parameters:**

- `inputs` (array, required): A list of input objects, where each object contains the 'id' of a CRM record to be archived.
- `objectType` (string, required): The type of CRM object to archive (e.g., 'contacts', 'companies', 'deals', 'tickets').

### HUBSPOT_ARCHIVE_BATCH_OF_PROPERTIES

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_BATCH_OF_PROPERTIES`

Archives a batch of properties by their internal names for a specified HubSpot CRM object type; this operation is idempotent and safe to retry.

**Parameters:**

- `inputs` (array, required): A list of properties to archive, each specified by its internal `name`.
- `objectType` (string, required): The HubSpot CRM object type for which properties are being archived.

### HUBSPOT_ARCHIVE_BATCH_OF_QUOTES

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_BATCH_OF_QUOTES`

Archives a batch of existing quotes by their IDs, removing them from active views while keeping them accessible in your HubSpot account for viewing, downloading, cloning, or deletion; note that archived quotes cannot be restored to active status.

**Parameters:**

- `inputs` (array, required): A list of objects, where each object contains the ID of a quote to be archived.

### HUBSPOT_ARCHIVE_COMPANIES

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_COMPANIES`

Archives multiple HubSpot companies by their IDs.

**Parameters:**

- `inputs` (array, required): A list of company objects, each specifying the ID of the company to be archived.

### HUBSPOT_ARCHIVE_COMPANY

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_COMPANY`

Archives an existing company in HubSpot CRM by its `companyId`, moving it to a recycling bin from which it can be restored, rather than permanently deleting it.

**Parameters:**

- `companyId` (string, required): The numeric identifier for the company to archive. Must contain only digits (e.g., '1234567890'). Do not use email addresses or other non-numeric identifiers.

### HUBSPOT_ARCHIVE_CONTACT

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_CONTACT`

Archives a HubSpot contact by its ID.

**Parameters:**

- `contactId` (string, required): Numeric HubSpot contact ID (e.g., '386009987808'). Note: The archive endpoint does NOT support email addresses or idProperty - use HUBSPOT_GET_CONTACTS or HUBSPOT_SEARCH_CONTACTS to find the numeric ID first.

### HUBSPOT_ARCHIVE_CONTACTS

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_CONTACTS`

Archives multiple HubSpot contacts by their IDs.

**Parameters:**

- `inputs` (array, required): A list of contact objects, each specifying the ID of the contact to be archived.

### HUBSPOT_ARCHIVE_CRM_OBJECT_BY_ID

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_CRM_OBJECT_BY_ID`

Archives a specific HubSpot CRM object by its type and ID, moving it to the recycling bin; this action is irreversible via the API but objects can often be restored via the HubSpot UI.

**Parameters:**

- `objectId` (string, required): The unique identifier for the CRM object to be archived. This ID must correspond to an existing object of the specified `objectType`. The format is typically a string of numbers or a UUID. Ensure the correct ID is provided as this operation is irreversible via the API.
- `objectType` (string, required): The type of CRM object to be archived. This value is case-sensitive, must be a valid HubSpot CRM object type (e.g., contacts, companies, deals, quotes), and should be provided in lowercase plural form (e.g., 'contacts', not 'Contact').

### HUBSPOT_ARCHIVE_DEALS

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_DEALS`

Archives multiple HubSpot deals by their IDs.

**Parameters:**

- `inputs` (array, required): List of deal objects to be archived.

### HUBSPOT_ARCHIVE_EMAIL

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_EMAIL`

Archives the HubSpot email specified by `emailId` by moving it to the recycling bin, making it inaccessible unless restored.

**Parameters:**

- `emailId` (string, required): Numeric object ID of the email engagement record in HubSpot. This is NOT an email address - it must be a numeric ID (e.g., '1234567890'). The ID can be retrieved from the HubSpot CRM email activity list or from previous API responses.

### HUBSPOT_ARCHIVE_EMAILS

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_EMAILS`

Archives multiple HubSpot emails by their IDs.

**Parameters:**

- `inputs` (array, required): List of email objects to be archived.

### HUBSPOT_ARCHIVE_FEEDBACK_SUBMISSION

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_FEEDBACK_SUBMISSION`

Archives an existing, non-archived Feedback Submission in HubSpot CRM by its ID, moving it to the recycling bin (not permanently deleting it).

**Parameters:**

- `feedbackSubmissionId` (string, required): The unique identifier of an existing Feedback Submission in HubSpot CRM to be archived.

### HUBSPOT_ARCHIVE_LINE_ITEM

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_LINE_ITEM`

Archives a specific HubSpot line item by its ID, moving it to a recoverable state.

**Parameters:**

- `lineItemId` (string, required): The unique identifier of the existing line item in HubSpot CRM to be archived.

### HUBSPOT_ARCHIVE_PRODUCT

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_PRODUCT`

Archives a HubSpot product by its ID.

**Parameters:**

- `productId` (string, required): Unique HubSpot identifier for the product to be archived.

### HUBSPOT_ARCHIVE_PRODUCTS

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_PRODUCTS`

Archives multiple HubSpot products by their IDs.

**Parameters:**

- `inputs` (array, required): List of product objects to be archived.

### HUBSPOT_ARCHIVE_PROPERTY_BY_OBJECT_TYPE_AND_NAME

**MCP Tool**: `mcp__composio__HUBSPOT_ARCHIVE_PROPERTY_BY_OBJECT_TYPE_AND_NAME`

Archives a specified CRM property by its object type and name, moving it to the recycling bin; note that some default HubSpot properties cannot be archived.

**Parameters:**

- `objectType` (string, required): The case-sensitive type of CRM object (e.g., 'contacts', 'companies') for which the property is being archived; must match an existing object type.
- `propertyName` (string, required): The case-sensitive internal name of the property to archive (e.g., 'custom_field_1', 'annual_revenue').

## Usage Examples

#### HUBSPOT_ADD_ASSET_ASSOCIATION

```
Use MCP tool: mcp__composio__HUBSPOT_ADD_ASSET_ASSOCIATION
Parameters: {
  "campaignGuid": "your_campaignGuid_here",
  "assetType": "your_assetType_here"
}
```

#### HUBSPOT_ADD_TOKEN_TO_EVENT_TEMPLATE

```
Use MCP tool: mcp__composio__HUBSPOT_ADD_TOKEN_TO_EVENT_TEMPLATE
Parameters: {
  "eventTemplateId": "your_eventTemplateId_here",
  "appId": "123"
}
```

#### HUBSPOT_ARCHIVE_BATCH_OF_FEEDBACK_SUBMISSIONS

```
Use MCP tool: mcp__composio__HUBSPOT_ARCHIVE_BATCH_OF_FEEDBACK_SUBMISSIONS
Parameters: {
  "inputs": "value"
}
```

## Quick Reference

| MCP Tool                                                          | Description                                           |
| ----------------------------------------------------------------- | ----------------------------------------------------- |
| `mcp__composio__HUBSPOT_ADD_ASSET_ASSOCIATION`                    | Associates an existing asset ('FORM', 'OBJECT_LIST... |
| `mcp__composio__HUBSPOT_ADD_TOKEN_TO_EVENT_TEMPLATE`              | Adds a new custom data token to an existing event ... |
| `mcp__composio__HUBSPOT_ARCHIVE_BATCH_OF_FEEDBACK_SUBMISSIONS`    | Asynchronously archives a batch of HubSpot feedbac... |
| `mcp__composio__HUBSPOT_ARCHIVE_BATCH_OF_LINE_ITEMS`              | Archives a batch of existing line items by their u... |
| `mcp__composio__HUBSPOT_ARCHIVE_BATCH_OF_OBJECTS`                 | Archives a batch of existing, non-archived CRM obj... |
| `mcp__composio__HUBSPOT_ARCHIVE_BATCH_OF_PROPERTIES`              | Archives a batch of properties by their internal n... |
| `mcp__composio__HUBSPOT_ARCHIVE_BATCH_OF_QUOTES`                  | Archives a batch of existing quotes by their IDs, ... |
| `mcp__composio__HUBSPOT_ARCHIVE_COMPANIES`                        | Archives multiple HubSpot companies by their IDs.     |
| `mcp__composio__HUBSPOT_ARCHIVE_COMPANY`                          | Archives an existing company in HubSpot CRM by its... |
| `mcp__composio__HUBSPOT_ARCHIVE_CONTACT`                          | Archives a HubSpot contact by its ID.                 |
| `mcp__composio__HUBSPOT_ARCHIVE_CONTACTS`                         | Archives multiple HubSpot contacts by their IDs.      |
| `mcp__composio__HUBSPOT_ARCHIVE_CRM_OBJECT_BY_ID`                 | Archives a specific HubSpot CRM object by its type... |
| `mcp__composio__HUBSPOT_ARCHIVE_DEALS`                            | Archives multiple HubSpot deals by their IDs.         |
| `mcp__composio__HUBSPOT_ARCHIVE_EMAIL`                            | Archives the HubSpot email specified by `emailId` ... |
| `mcp__composio__HUBSPOT_ARCHIVE_EMAILS`                           | Archives multiple HubSpot emails by their IDs.        |
| `mcp__composio__HUBSPOT_ARCHIVE_FEEDBACK_SUBMISSION`              | Archives an existing, non-archived Feedback Submis... |
| `mcp__composio__HUBSPOT_ARCHIVE_LINE_ITEM`                        | Archives a specific HubSpot line item by its ID, m... |
| `mcp__composio__HUBSPOT_ARCHIVE_PRODUCT`                          | Archives a HubSpot product by its ID.                 |
| `mcp__composio__HUBSPOT_ARCHIVE_PRODUCTS`                         | Archives multiple HubSpot products by their IDs.      |
| `mcp__composio__HUBSPOT_ARCHIVE_PROPERTY_BY_OBJECT_TYPE_AND_NAME` | Archives a specified CRM property by its object ty... |

## When to Use This Integration

**Use hubspot when:**

- Interacting with hubspot services
- Automating hubspot workflows
- Reading and writing hubspot data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:

1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
