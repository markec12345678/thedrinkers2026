---
name: productboard
description: productboard integration via Composio MCP. Use when user needs to interact with productboard.
---

# productboard Integration

> Auto-generated by Composio Skills Extension on 2026-03-27T09:32:07.566Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Integration with productboard services.

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### PRODUCTBOARD_ADD_NOTE_FOLLOWERS

**MCP Tool**: `mcp__composio__PRODUCTBOARD_ADD_NOTE_FOLLOWERS`

Tool to add multiple followers to a Productboard note. Use when you need to bulk-add existing users as followers to a specific note by their email addresses.

**Parameters:**

- `emails` (array, required): List of email addresses of existing workspace members to add as followers. Only users who are members of the workspace can be added as followers.
- `noteId` (string, required): UUID of the note to which followers will be added

### PRODUCTBOARD_ADD_NOTE_TAG

**MCP Tool**: `mcp__composio__PRODUCTBOARD_ADD_NOTE_TAG`

Adds a tag to a Productboard note for categorization and organization. Use this action to apply tags to notes based on their content, priority, source, or any other classification criteria. Tags help organize and filter notes in Productboard. If the specified tag doesn't exist in the workspace, it will be automatically created. Tags are case-sensitive. Common use cases: - Categorize notes by priority (e.g., "Urgent", "High Priority") - Label feature requests vs. bugs (e.g., "feature-request", "bug") - Mark notes for review stages (e.g., "needs review", "approved") - Associate notes with time periods (e.g., "Q1-2026", "Sprint-5")

**Parameters:**

- `noteId` (string, required): UUID of the note to tag. Obtain from list/create note actions.
- `tagName` (string, required): Name of the tag to add to the note. If the tag doesn't exist in the workspace, it will be automatically created. Supports alphanumeric characters, spaces, hyphens, and special characters.

### PRODUCTBOARD_CREATE_COMPANY

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_COMPANY`

Tool to create a new company in Productboard. Use when you need to add a company record after gathering its details and optional metadata.

**Parameters:**

- `name` (string, required): Primary name of the company to create.
- `domain` (string, optional): Internet domain of the company (e.g., 'example.com').
- `source` (object, optional): Metadata about the external origin of this company record.
- `description` (string, optional): Free-text description of the company.

### PRODUCTBOARD_CREATE_COMPANY_FIELD

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_COMPANY_FIELD`

Tool to create a new custom field for companies. Use when you need to define custom company attributes in Productboard after confirming the field schema.

**Parameters:**

- `body` (object, required): JSON body containing the custom field definition as per Productboard API schema

### PRODUCTBOARD_CREATE_COMPONENT

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_COMPONENT`

Tool to create a new (sub)component under a product or component. Use after determining the parent component ID.

**Parameters:**

- `data` (object, required): Payload wrapper containing component details

### PRODUCTBOARD_CREATE_ENTITIES_RELATIONSHIPS_V2

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_ENTITIES_RELATIONSHIPS_V2`

Tool to create a relationship between two entities in Productboard. Use when you need to establish a parent, child, link, or dependency relationship between entities.

**Parameters:**

- `id` (string, required): The UUID of the entity to create the relationship from.
- `data` (object, required): The relationship data payload.

### PRODUCTBOARD_CREATE_ENTITIES_V2

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_ENTITIES_V2`

Tool to create a new entity in Productboard using the v2 API. Use when you need to create products, components, features, subfeatures, initiatives, objectives, key results, releases, or release groups.

**Parameters:**

- `data` (object, required): Entity data including type, fields, and relationships

### PRODUCTBOARD_CREATE_FEATURE

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_FEATURE`

Tool to create a new feature or subfeature in Productboard. Use when you need to add a feature to a product or component, or create a subfeature under an existing feature.

**Parameters:**

- `data` (object, required): Feature data payload

### PRODUCTBOARD_CREATE_FEATURE_OBJECTIVE_LINK

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_FEATURE_OBJECTIVE_LINK`

Tool to create a link between a feature and an objective (OKR). Use when associating a feature with a specific objective to track alignment.

**Parameters:**

- `id` (string, required): UUID of the feature to link. Must be a top-level feature ID, not a subfeature ID.
- `objectiveId` (string, required): UUID of the objective to link to the feature.

### PRODUCTBOARD_CREATE_NOTE_LINK

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_NOTE_LINK`

Tool to create a link between a note and an entity. Use when associating a note with a productboard entity.

**Parameters:**

- `noteId` (string, required): UUID of the note to link
- `entityId` (string, required): UUID of the entity (product, component, feature, or subfeature) to link

### PRODUCTBOARD_CREATE_NOTES_V2

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_NOTES_V2`

Tool to create a new note in Productboard using the v2 API. Use when you need to create simple text notes or conversation-style notes with optional relationships to customers, companies, or external links.

**Parameters:**

- `data` (object, required): Note data including type, fields, and optional relationships.

### PRODUCTBOARD_CREATE_OBJECTIVES

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_OBJECTIVES`

Tool to create a new objective in Productboard. Use when you need to create a strategic objective with optional details like owner, parent, status, and timeframe.

**Parameters:**

- `data` (object, required): Data payload containing the objective details.

### PRODUCTBOARD_CREATE_OBJECTIVES_LINKS_FEATURES

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_OBJECTIVES_LINKS_FEATURES`

Tool to create a new link between an objective and a feature. Use when associating a feature with an objective (OKR).

**Parameters:**

- `id` (string, required): UUID of the objective to link to a feature.
- `featureId` (string, required): UUID of the feature to link to the objective.

### PRODUCTBOARD_CREATE_RELEASE

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_RELEASE`

Tool to create a new release in Productboard. Use when you need to create a release with a name, description, and assigned release group.

**Parameters:**

- `data` (object, required): Payload containing the release details to create.

### PRODUCTBOARD_CREATE_USERS

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_USERS`

Tool to create a new user in Productboard. Use when you need to add a user with email, name, and/or external identifier.

**Parameters:**

- `data` (object, required): User data containing email, name, and externalId fields.

### PRODUCTBOARD_CREATE_WEBHOOK

**MCP Tool**: `mcp__composio__PRODUCTBOARD_CREATE_WEBHOOK`

Tool to create a new webhook subscription. Use when you need to register a public endpoint to receive webhooks from Productboard.

**Parameters:**

- `url` (string, required): Public URL where Productboard will send the subscriptionProbe (GET) and webhookNotification (POST) callbacks.
- `name` (string, required): Human-readable name for the webhook subscription.
- `events` (array, required): List of events to subscribe to, each with eventType. Examples: insight.created, release.updated, key-result.updated, key-result.deleted.
- `notification_headers` (object, optional): Optional headers Productboard will include when sending callbacks.
- `notification_version` (integer, required): Notification payload version for callbacks (default: 1).

### PRODUCTBOARD_DELETE_COMPANY

**MCP Tool**: `mcp__composio__PRODUCTBOARD_DELETE_COMPANY`

Tool to delete a specific company. Use when you have confirmed the company ID and need to remove it from Productboard.

**Parameters:**

- `id` (string, required): ID of the company to delete

### PRODUCTBOARD_DELETE_COMPANY_FIELD

**MCP Tool**: `mcp__composio__PRODUCTBOARD_DELETE_COMPANY_FIELD`

Tool to delete a specific company custom field. Use after confirming the field ID and ensuring no dependencies remain.

**Parameters:**

- `id` (string, required): ID of the company custom field to delete (UUID format)

### PRODUCTBOARD_DELETE_CUSTOM_FIELD_VALUE

**MCP Tool**: `mcp__composio__PRODUCTBOARD_DELETE_CUSTOM_FIELD_VALUE`

Tool to delete a custom field value from a hierarchy entity in Productboard. Use when you need to remove a custom field value from a product, component, or feature after confirming the field and entity IDs.

**Parameters:**

- `customField.id` (string, required): UUID of the custom field whose value should be deleted.
- `hierarchyEntity.id` (string, required): UUID of the hierarchy entity (product, component, or feature) from which to delete the custom field value.

### PRODUCTBOARD_DELETE_ENTITIES_RELATIONSHIPS_V2

**MCP Tool**: `mcp__composio__PRODUCTBOARD_DELETE_ENTITIES_RELATIONSHIPS_V2`

Tool to delete a relationship between two entities. Use when you need to remove a specific relationship type between entities in Productboard.

**Parameters:**

- `id` (string, required): The UUID of the entity to delete the relationship from.
- `type` (string, required): The type of the relationship to delete (e.g., 'link', 'parent', 'child').
- `targetId` (string, required): The UUID of the target entity to remove the relationship with.

## Usage Examples

#### PRODUCTBOARD_ADD_NOTE_FOLLOWERS

```
Use MCP tool: mcp__composio__PRODUCTBOARD_ADD_NOTE_FOLLOWERS
Parameters: {
  "noteId": "your_noteId_here",
  "emails": "value"
}
```

#### PRODUCTBOARD_ADD_NOTE_TAG

```
Use MCP tool: mcp__composio__PRODUCTBOARD_ADD_NOTE_TAG
Parameters: {
  "noteId": "your_noteId_here",
  "tagName": "your_tagName_here"
}
```

#### PRODUCTBOARD_CREATE_COMPANY

```
Use MCP tool: mcp__composio__PRODUCTBOARD_CREATE_COMPANY
Parameters: {
  "name": "your_name_here"
}
```

## Quick Reference

| MCP Tool                                                       | Description                                           |
| -------------------------------------------------------------- | ----------------------------------------------------- |
| `mcp__composio__PRODUCTBOARD_ADD_NOTE_FOLLOWERS`               | Tool to add multiple followers to a Productboard n... |
| `mcp__composio__PRODUCTBOARD_ADD_NOTE_TAG`                     | Adds a tag to a Productboard note for categorizati... |
| `mcp__composio__PRODUCTBOARD_CREATE_COMPANY`                   | Tool to create a new company in Productboard. Use ... |
| `mcp__composio__PRODUCTBOARD_CREATE_COMPANY_FIELD`             | Tool to create a new custom field for companies. U... |
| `mcp__composio__PRODUCTBOARD_CREATE_COMPONENT`                 | Tool to create a new (sub)component under a produc... |
| `mcp__composio__PRODUCTBOARD_CREATE_ENTITIES_RELATIONSHIPS_V2` | Tool to create a relationship between two entities... |
| `mcp__composio__PRODUCTBOARD_CREATE_ENTITIES_V2`               | Tool to create a new entity in Productboard using ... |
| `mcp__composio__PRODUCTBOARD_CREATE_FEATURE`                   | Tool to create a new feature or subfeature in Prod... |
| `mcp__composio__PRODUCTBOARD_CREATE_FEATURE_OBJECTIVE_LINK`    | Tool to create a link between a feature and an obj... |
| `mcp__composio__PRODUCTBOARD_CREATE_NOTE_LINK`                 | Tool to create a link between a note and an entity... |
| `mcp__composio__PRODUCTBOARD_CREATE_NOTES_V2`                  | Tool to create a new note in Productboard using th... |
| `mcp__composio__PRODUCTBOARD_CREATE_OBJECTIVES`                | Tool to create a new objective in Productboard. Us... |
| `mcp__composio__PRODUCTBOARD_CREATE_OBJECTIVES_LINKS_FEATURES` | Tool to create a new link between an objective and... |
| `mcp__composio__PRODUCTBOARD_CREATE_RELEASE`                   | Tool to create a new release in Productboard. Use ... |
| `mcp__composio__PRODUCTBOARD_CREATE_USERS`                     | Tool to create a new user in Productboard. Use whe... |
| `mcp__composio__PRODUCTBOARD_CREATE_WEBHOOK`                   | Tool to create a new webhook subscription. Use whe... |
| `mcp__composio__PRODUCTBOARD_DELETE_COMPANY`                   | Tool to delete a specific company. Use when you ha... |
| `mcp__composio__PRODUCTBOARD_DELETE_COMPANY_FIELD`             | Tool to delete a specific company custom field. Us... |
| `mcp__composio__PRODUCTBOARD_DELETE_CUSTOM_FIELD_VALUE`        | Tool to delete a custom field value from a hierarc... |
| `mcp__composio__PRODUCTBOARD_DELETE_ENTITIES_RELATIONSHIPS_V2` | Tool to delete a relationship between two entities... |

## When to Use This Integration

**Use productboard when:**

- Interacting with productboard services
- Automating productboard workflows
- Reading and writing productboard data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:

1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
