---
name: asana
description: Tool to help teams organize, track, and manage their work. integration via Composio MCP. Use when user needs to interact with Asana.
---

# Asana Integration

> Auto-generated by Composio Skills Extension on 2026-03-27T09:37:37.799Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Tool to help teams organize, track, and manage their work.

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### ASANA_ADD_FOLLOWERS_TO_PROJECT

**MCP Tool**: `mcp__composio__ASANA_ADD_FOLLOWERS_TO_PROJECT`

Tool to add followers to a project in Asana. Use this tool when you need to add one or more users as followers to a specific project. Followers will receive notifications when tasks are added to the project.

**Parameters:**

- `followers` (string, required): A string identifying users. These can either be the string "me", an email, or the gid of a user. For multiple followers, use a comma-separated string.
- `opt_fields` (array, optional): This endpoint returns a resource which excludes some properties by default. To include those optional properties, set this query parameter to a comma-separated list of the properties you wish to include.
- `opt_pretty` (boolean, optional): Provides "pretty" output. Provides the response in a "pretty" format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
- `project_gid` (string, required): Globally unique identifier for the project.

### ASANA_ADD_FOLLOWERS_TO_TASK

**MCP Tool**: `mcp__composio__ASANA_ADD_FOLLOWERS_TO_TASK`

Tool to add followers to a task in Asana. Use this tool when you need to add one or more users as followers to a specific task. This will notify them of updates to the task.

**Parameters:**

- `task_gid` (string, required): The globally unique identifier for the task.
- `followers` (array, required): An array of user GIDs to add as followers to the task. For example: ["12345", "67890"].
- `opt_fields` (array, optional): This endpoint returns a resource which excludes some properties by default. To include those optional properties, set this query parameter to a comma-separated list of the properties you wish to include. For example ["followers", "assignee"].
- `opt_pretty` (boolean, optional): Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.

### ASANA_ADD_ITEM_TO_PORTFOLIO

**MCP Tool**: `mcp__composio__ASANA_ADD_ITEM_TO_PORTFOLIO`

Add a project (or other supported item) to an Asana portfolio using the native addItem endpoint. Use when a workflow needs to attach a newly created project to a portfolio without using ASANA_SUBMIT_PARALLEL_REQUESTS.

**Parameters:**

- `item` (string, required): The globally unique identifier (GID) of the item to add to the portfolio. Typically a project GID, but can be any supported portfolio item type. Must be a numeric string.
- `insert_after` (string, optional): GID of a portfolio item after which to insert the new item. Mutually exclusive with `insert_before`.
- `insert_before` (string, optional): GID of a portfolio item before which to insert the new item. Mutually exclusive with `insert_after`.
- `portfolio_gid` (string, required): The globally unique identifier (GID) of the portfolio to add the item to. Must be a numeric string.

### ASANA_ADD_MEMBERS_TO_PROJECT

**MCP Tool**: `mcp__composio__ASANA_ADD_MEMBERS_TO_PROJECT`

Tool to add users to a project in Asana. Use this tool when you need to add one or more users as members to a specific project. Members can view and contribute to the project.

**Parameters:**

- `members` (string, required): An array of strings identifying users. These can either be the string "me", an email, or the gid of a user. For example: "521621,621373" or "me,user@example.com".
- `opt_fields` (array, optional): This endpoint returns a resource which excludes some properties by default. To include those optional properties, set this query parameter to a comma-separated list of the properties you wish to include. For example: ["members", "name", "archived"].
- `opt_pretty` (boolean, optional): Provides "pretty" output. Provides the response in a "pretty" format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
- `project_gid` (string, required): Globally unique identifier for the project.

### ASANA_ADD_PROJECT_FOR_TASK

**MCP Tool**: `mcp__composio__ASANA_ADD_PROJECT_FOR_TASK`

Tool to add a project to a task in Asana. Use when you need to associate a task with a project. Optionally position the task within the project using insert_before, insert_after, or section parameters.

**Parameters:**

- `project` (string, required): The globally unique identifier for the project to add to the task.
- `section` (string, optional): A section in the project to insert the task into. The task will be inserted at the bottom of the section unless combined with `insert_before: null` (end of section) or `insert_after: null` (beginning of section). Can also be combined with non-null `insert_before` or `insert_after` to position relative to a task within the section.
- `task_gid` (string, required): The globally unique identifier for the task to add a project to.
- `opt_pretty` (boolean, optional): Provides 'pretty' output. Provides the response in a 'pretty' format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
- `insert_after` (string, optional): A task in the project to insert the task after, or `null` to insert at the beginning of the list. When used with `section`, `null` will insert at the beginning of the specified section, otherwise the task must be in the specified section.
- `insert_before` (string, optional): A task in the project to insert the task before, or `null` to insert at the end of the list. When used with `section`, `null` will insert at the end of the specified section, otherwise the task must be in the specified section.

### ASANA_ADD_SUPPORTING_RELATIONSHIP

**MCP Tool**: `mcp__composio__ASANA_ADD_SUPPORTING_RELATIONSHIP`

Tool to add a supporting goal relationship to a goal. Use when you want to link a project, task, portfolio, or another goal as a supporting resource to a specific goal in Asana.

**Parameters:**

- `data` (object, required): The request data for adding a supporting relationship.
- `goal_gid` (string, required): Globally unique identifier for the goal.
- `opt_fields` (array, optional): This endpoint returns a compact resource, which excludes some properties by default. To include those optional properties, set this query parameter to a comma-separated list of the properties you wish to include.
- `opt_pretty` (boolean, optional): Provides “pretty” output. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.

### ASANA_ADD_TAG_TO_TASK

**MCP Tool**: `mcp__composio__ASANA_ADD_TAG_TO_TASK`

Tool to add an existing tag to a task in Asana. Use when you need to add a tag for prioritization, routing, or automation workflows. Tags cannot be added via Update Task, so this dedicated endpoint is required.

**Parameters:**

- `tag_gid` (string, required): The globally unique identifier for the tag to add. The tag must already exist in the workspace.
- `task_gid` (string, required): The globally unique identifier for the task.
- `opt_fields` (string, optional): Comma-separated list of fields to include in the response. Limited utility for this endpoint as it returns an empty data block.
- `opt_pretty` (boolean, optional): Provides 'pretty' output. Provides the response in a 'pretty' format with proper line breaking and indentation for readability. This will take extra time and increase the response size so it is advisable only to use this during debugging.

### ASANA_ADD_TASK_DEPENDENCIES

**MCP Tool**: `mcp__composio__ASANA_ADD_TASK_DEPENDENCIES`

Tool to add dependency relationships to an Asana task. Use when you need to mark one or more tasks as prerequisites (dependencies) for another task, ensuring the dependency tasks must be completed first.

**Parameters:**

- `task_gid` (string, required): The globally unique identifier for the task that will depend on other tasks.
- `opt_fields` (string, optional): Comma-separated list of fields to include in the response. By default, the response returns a compact representation of the task. Use this to retrieve additional fields such as 'name', 'assignee', 'due_on', 'dependencies', etc.
- `opt_pretty` (boolean, optional): Provides 'pretty' output. Provides the response in a 'pretty' format with proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
- `dependencies` (array, required): An array of task GIDs that the target task depends on (i.e., these tasks must be completed before the target task can be started). Each GID should be a valid task identifier.

### ASANA_ADD_TASK_TO_SECTION

**MCP Tool**: `mcp__composio__ASANA_ADD_TASK_TO_SECTION`

Adds an existing task to a section, optionally positioning it before or after another task in that section; if no position is specified, the task is added to the end.

**Parameters:**

- `task_gid` (string, required): The GID of the existing task to add to the section. Must be a numeric string.
- `section_gid` (string, required): The GID of the target section. Must be a section GID (not a project or task GID). Use GET_SECTIONS_IN_PROJECT to retrieve valid section GIDs from a project.
- `insert_after` (string, optional): GID of a task in the section after which to insert the current task. Mutually exclusive with insert_before.
- `insert_before` (string, optional): GID of a task in the section before which to insert the current task. Mutually exclusive with insert_after.

### ASANA_ADD_USER_TO_TEAM

**MCP Tool**: `mcp__composio__ASANA_ADD_USER_TO_TEAM`

Tool to add a user to a team in Asana. Use this when you need to add a user to a specific team by providing their user GID, email, or "me" for the current user.

**Parameters:**

- `user` (string, required): A string identifying a user. This can either be the string "me", an email, or the gid of a user.
- `team_gid` (string, required): Globally unique identifier for the team.
- `opt_fields` (array, optional): This endpoint returns a resource which excludes some properties by default. To include those optional properties, set this query parameter to a comma-separated list of the properties you wish to include.
- `opt_pretty` (boolean, optional): Provides "pretty" output. Provides the response in a "pretty" format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.

### ASANA_ADD_USER_TO_WORKSPACE

**MCP Tool**: `mcp__composio__ASANA_ADD_USER_TO_WORKSPACE`

Tool to add a user to a workspace or organization in Asana. Use this when you need to add a user to a specific workspace by providing their user GID, email, or "me" for the current user.

**Parameters:**

- `user` (string, required): A string identifying a user. This can either be the string "me", an email, or the gid of a user.
- `opt_fields` (array, optional): This endpoint returns a resource which excludes some properties by default. To include those optional properties, set this query parameter to a comma-separated list of the properties you wish to include.
- `opt_pretty` (boolean, optional): Provides "pretty" output. Provides the response in a "pretty" format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
- `workspace_gid` (string, required): Globally unique identifier for the workspace or organization.

### ASANA_APPROVE_ACCESS_REQUEST

**MCP Tool**: `mcp__composio__ASANA_APPROVE_ACCESS_REQUEST`

Tool to approve an access request in Asana. Use when you need to grant access to a resource that requires approval workflow.

**Parameters:**

- `opt_fields` (string, optional): Comma-separated list of fields to include in the response.
- `opt_pretty` (boolean, optional): Provides "pretty" output. Provides the response in a "pretty" format with proper line breaking and indentation for readability. This will take extra time and increase the response size so it is advisable only to use this during debugging.
- `access_request_gid` (string, required): Globally unique identifier for the access request to approve.

### ASANA_CREATE_ACCESS_REQUEST

**MCP Tool**: `mcp__composio__ASANA_CREATE_ACCESS_REQUEST`

Tool to create an access request in Asana. Use when you need to request access to a project or portfolio that you don't currently have access to.

**Parameters:**

- `target` (string, required): The globally unique identifier (gid) of the access requestable object that the user is requesting access to. Supports projects and portfolios.
- `message` (string, optional): The optional message to include with the access request. This can be used to provide context or additional information about the request.

### ASANA_CREATE_ALLOCATION

**MCP Tool**: `mcp__composio__ASANA_CREATE_ALLOCATION`

Creates a new allocation. Use when you need to schedule or assign a specific amount of a user's time per week to a task or project within a defined period.

**Parameters:**

- `notes` (string, optional): Any additional notes related to the allocation.
- `end_on` (string, required): The end date of the allocation. The format is YYYY-MM-DD.
- `start_on` (string, required): The start date of the allocation. The format is YYYY-MM-DD.
- `project_gid` (string, optional): The project GID for which this allocation is being made. If provided, the allocation will be linked to this project.
- `assignee_gid` (string, required): The ID of the user to whom the allocation is assigned. This can be the user's GID or the string 'me' to refer to the authenticated user.
- `workspace_gid` (string, required): The ID of the workspace where the allocation will be created.
- `minutes_per_week` (integer, required): The number of minutes per week the user is allocated for. Must be greater than 0.

### ASANA_CREATE_A_PROJECT

**MCP Tool**: `mcp__composio__ASANA_CREATE_A_PROJECT`

Creates a new Asana project in the specified workspace. Requires a `workspace` GID, and additionally a `team` GID if the workspace is an organization.

**Parameters:**

- `data` (object, required): Data payload for the new project.
- `opt_fields` (array, optional): Array of optional properties to include in the response.
- `opt_pretty` (boolean, optional): True for human-readable JSON response (debugging only due to size/processing).

### ASANA_CREATE_A_TAG_IN_A_WORKSPACE

**MCP Tool**: `mcp__composio__ASANA_CREATE_A_TAG_IN_A_WORKSPACE`

Creates a new tag, with properties like name and color defined in the request body, within a specific Asana workspace (using `workspace_gid`); this tag helps categorize tasks, is confined to the workspace, and is not automatically applied to tasks.

**Parameters:**

- `data` (object, required): The properties for the tag creation, including name (required), color (optional), and notes (optional).
- `opt_fields` (array, optional): Comma-separated string of fields from `OptFieldsEnm0` (e.g., 'name', 'color') to include in the response; returns a compact representation by default.
- `opt_pretty` (boolean, optional): If true, pretty-prints the JSON response; useful for debugging but may increase response size.
- `workspace_gid` (string, required): The globally unique identifier (GID) for the workspace or organization in which to create the tag.

### ASANA_CREATE_A_TASK

**MCP Tool**: `mcp__composio__ASANA_CREATE_A_TASK`

Creates a new Asana task; requires 'workspace', 'parent', or 'projects' for association, and 'followers', 'projects', 'tags' are set only at creation.

**Parameters:**

- `data` (object, required): Task details for creation.
- `opt_fields` (array, optional): List of optional field names to include in the response. Common fields include: name, assignee, due_on, completed, projects, memberships, memberships.section, memberships.section.name, etc. For section information, use memberships.section or memberships.section.name. Default is compact resource.
- `opt_pretty` (boolean, optional): Provides 'pretty' JSON output with line breaks/indentation. Useful for debugging; may increase response time/size.

### ASANA_CREATE_ATTACHMENT_FOR_OBJECT

**MCP Tool**: `mcp__composio__ASANA_CREATE_ATTACHMENT_FOR_OBJECT`

Tool to upload an attachment or link an external resource to a task, project, or project_brief in Asana. Use when you need to attach a file or external URL to any Asana object.

**Parameters:**

- `url` (string, optional): The URL of the external resource being attached. Required for attachments of type 'external'. Mutually exclusive with 'file'.
- `file` (string, optional): Required for 'asana' attachments. This field should be provided when uploading a file. Mutually exclusive with 'url'.
- `name` (string, optional): The name of the external resource being attached. Required for attachments of type 'external'. For file uploads, this is optional and will be derived from the file if not provided.
- `parent` (string, required): Required identifier of the parent task, project, or project_brief, as a string. This is the GID of the object to attach the file or URL to.
- `connect_to_app` (boolean, optional): Optional. Only relevant for external attachments with a parent task. A boolean indicating whether the current app should be connected with the attachment for the purposes of showing an app components widget. Requires the app to have been added to a project the parent task is in. This property can only be set if an OAuth token is used to authenticate the request.
- `resource_subtype` (string, optional): The type of the attachment. Must be 'asana' for file uploads or 'external' for URL attachments. If not specified, a file attachment of type 'asana' will be assumed. Note that if the value is 'external', 'name' and 'url' must also be provided.

### ASANA_CREATE_ATTACHMENT_FOR_TASK

**MCP Tool**: `mcp__composio__ASANA_CREATE_ATTACHMENT_FOR_TASK`

Tool to upload an attachment to a task. Use when you need to attach a file to a specific task in Asana.

**Parameters:**

- `file` (string, required): The file to upload.
- `parent_gid` (string, required): Globally unique identifier for the parent task or comment.
- `connect_to_app` (boolean, optional): Whether to connect the attachment to the app. If true, the attachment will be displayed in the Asana UI as an app attachment. If false, the attachment will be displayed as a regular file attachment. Defaults to true.
- `resource_subtype` (string, optional): The type of the attachment. Must be one of the given values. If not specified, a regular file attachment will be created. If 'asana' is specified, the attachment will be a link to another Asana task. If 'external' is specified, the attachment will be a link to an external resource. If 'gdrive' is specified, the attachment will be a link to a Google Drive file. If 'box' is specified, the attachment will be a link to a Box file. If 'dropbox' is specified, the attachment will be a link to a Dropbox file. If 'vimeo' is specified, the attachment will be a link to a Vimeo video.

### ASANA_CREATE_CUSTOM_FIELD

**MCP Tool**: `mcp__composio__ASANA_CREATE_CUSTOM_FIELD`

Tool to create a new custom field in a workspace. Use when you need to define a new field for tracking specific information within Asana tasks.

**Parameters:**

- `data` (object, required): The custom field object to create.
- `opt_fields` (array, optional): Comma-separated list of properties to include in the response.
- `opt_pretty` (boolean, optional): Provides “pretty” output.

## Usage Examples

#### ASANA_ADD_FOLLOWERS_TO_PROJECT

```
Use MCP tool: mcp__composio__ASANA_ADD_FOLLOWERS_TO_PROJECT
Parameters: {
  "project_gid": "your_project_gid_here",
  "followers": "your_followers_here"
}
```

#### ASANA_ADD_FOLLOWERS_TO_TASK

```
Use MCP tool: mcp__composio__ASANA_ADD_FOLLOWERS_TO_TASK
Parameters: {
  "task_gid": "your_task_gid_here",
  "followers": "value"
}
```

#### ASANA_ADD_ITEM_TO_PORTFOLIO

```
Use MCP tool: mcp__composio__ASANA_ADD_ITEM_TO_PORTFOLIO
Parameters: {
  "portfolio_gid": "your_portfolio_gid_here",
  "item": "your_item_here"
}
```

## Quick Reference

| MCP Tool                                            | Description                                           |
| --------------------------------------------------- | ----------------------------------------------------- |
| `mcp__composio__ASANA_ADD_FOLLOWERS_TO_PROJECT`     | Tool to add followers to a project in Asana. Use t... |
| `mcp__composio__ASANA_ADD_FOLLOWERS_TO_TASK`        | Tool to add followers to a task in Asana. Use this... |
| `mcp__composio__ASANA_ADD_ITEM_TO_PORTFOLIO`        | Add a project (or other supported item) to an Asan... |
| `mcp__composio__ASANA_ADD_MEMBERS_TO_PROJECT`       | Tool to add users to a project in Asana. Use this ... |
| `mcp__composio__ASANA_ADD_PROJECT_FOR_TASK`         | Tool to add a project to a task in Asana. Use when... |
| `mcp__composio__ASANA_ADD_SUPPORTING_RELATIONSHIP`  | Tool to add a supporting goal relationship to a go... |
| `mcp__composio__ASANA_ADD_TAG_TO_TASK`              | Tool to add an existing tag to a task in Asana. Us... |
| `mcp__composio__ASANA_ADD_TASK_DEPENDENCIES`        | Tool to add dependency relationships to an Asana t... |
| `mcp__composio__ASANA_ADD_TASK_TO_SECTION`          | Adds an existing task to a section, optionally pos... |
| `mcp__composio__ASANA_ADD_USER_TO_TEAM`             | Tool to add a user to a team in Asana. Use this wh... |
| `mcp__composio__ASANA_ADD_USER_TO_WORKSPACE`        | Tool to add a user to a workspace or organization ... |
| `mcp__composio__ASANA_APPROVE_ACCESS_REQUEST`       | Tool to approve an access request in Asana. Use wh... |
| `mcp__composio__ASANA_CREATE_ACCESS_REQUEST`        | Tool to create an access request in Asana. Use whe... |
| `mcp__composio__ASANA_CREATE_ALLOCATION`            | Creates a new allocation. Use when you need to sch... |
| `mcp__composio__ASANA_CREATE_A_PROJECT`             | Creates a new Asana project in the specified works... |
| `mcp__composio__ASANA_CREATE_A_TAG_IN_A_WORKSPACE`  | Creates a new tag, with properties like name and c... |
| `mcp__composio__ASANA_CREATE_A_TASK`                | Creates a new Asana task; requires 'workspace', 'p... |
| `mcp__composio__ASANA_CREATE_ATTACHMENT_FOR_OBJECT` | Tool to upload an attachment or link an external r... |
| `mcp__composio__ASANA_CREATE_ATTACHMENT_FOR_TASK`   | Tool to upload an attachment to a task. Use when y... |
| `mcp__composio__ASANA_CREATE_CUSTOM_FIELD`          | Tool to create a new custom field in a workspace. ... |

## When to Use This Integration

**Use Asana when:**

- Interacting with Asana services
- Automating Asana workflows
- Reading and writing Asana data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:

1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
