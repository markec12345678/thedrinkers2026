---
name: googlesuper
description: googlesuper integration via Composio MCP. Use when user needs to interact with googlesuper.
---

# googlesuper Integration

> Auto-generated by Composio Skills Extension on 2026-03-09T19:28:58.834Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Integration with googlesuper services.

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### GOOGLESUPER_ACL_DELETE

**MCP Tool**: `mcp__composio__GOOGLESUPER_ACL_DELETE`

Deletes an access control rule from a Google Calendar. Use when you need to remove sharing permissions for a user, group, or domain.

**Parameters:**
  - `rule_id` (string, required): ACL rule identifier.
  - `calendar_id` (string, required): Calendar identifier. To retrieve calendar IDs call the calendarList.list method. If you want to access the primary calendar of the currently logged in user, use the "primary" keyword.

### GOOGLESUPER_ACL_GET

**MCP Tool**: `mcp__composio__GOOGLESUPER_ACL_GET`

Retrieves a specific access control rule for a calendar. Use when you need to check permissions for a specific user, group, or domain.

**Parameters:**
  - `rule_id` (string, required): ACL rule identifier. Format: 'scope_type:scope_value' or 'default'. Valid scope types: 'user' (email), 'group' (group email), 'domain' (domain name), 'default' (public access). Examples: 'user:john@example.com', 'group:team@example.com', 'domain:example.com', 'default'. Note: 'me' is NOT valid; use actual email/domain. The rule must exist - use GOOGLECALENDAR_LIST_ACL_RULES to find valid IDs.
  - `calendar_id` (string, required): Calendar identifier. To retrieve calendar IDs call the calendarList.list method. If you want to access the primary calendar of the currently logged in user, use the "primary" keyword.

### GOOGLESUPER_ACL_INSERT

**MCP Tool**: `mcp__composio__GOOGLESUPER_ACL_INSERT`

Creates an access control rule for a calendar. Use when you need to grant sharing permissions to a user, group, or domain.

**Parameters:**
  - `role` (string, required): The role assigned to the scope. Possible values: "none" - Provides no access; "freeBusyReader" - Provides read access to free/busy information; "reader" - Provides read access to the calendar; "writer" - Provides read and write access to the calendar; "owner" - Provides ownership of the calendar.
  - `scope` (object, required): The extent to which calendar access is granted by this ACL rule. Specifies who gets the access (user, group, domain, or default).
  - `calendar_id` (string, required): Calendar identifier. To retrieve calendar IDs call the calendarList.list method. If you want to access the primary calendar of the currently logged in user, use the "primary" keyword.
  - `send_notifications` (boolean, optional): Whether to send notifications about the calendar sharing change. Optional. The default is true.

### GOOGLESUPER_ACL_PATCH

**MCP Tool**: `mcp__composio__GOOGLESUPER_ACL_PATCH`

Updates an existing access control rule for a calendar using patch semantics (partial update). This allows modifying specific fields without affecting other properties. IMPORTANT: The ACL rule must already exist on the calendar. This action cannot create new rules. If you receive a 404 Not Found error, the rule does not exist - use ACL insert to create it first, or use ACL list to verify available rules. Each patch request consumes three quota units. For domain-type ACL rules, if PATCH fails with 500 error, this action will automatically fallback to UPDATE method.

**Parameters:**
  - `role` (string, optional): The role assigned to the scope. Possible values are: "none" - Provides no access; "freeBusyReader" - Provides read access to free/busy information; "reader" - Provides read access to the calendar (private events appear but details are hidden); "writer" - Provides read and write access to the calendar (private events and details are visible); "owner" - Provides ownership of the calendar (all permissions of writer plus ability to see and manipulate ACLs).
  - `scope` (object, optional): The extent to which calendar access is granted by this ACL rule. Optional for patch operations.
  - `rule_id` (string, required): ACL rule identifier of an existing rule. IMPORTANT: The rule must already exist on the calendar - this action cannot create new rules, only modify existing ones. Use the ACL list action to find existing rule IDs, or use the ACL insert action to create a new rule first. Format: 'type:value', such as 'user:email@example.com' or 'group:group@example.com'.
  - `calendar_id` (string, required): Calendar identifier. To retrieve calendar IDs call the calendarList.list method. If you want to access the primary calendar of the currently logged in user, use the "primary" keyword.
  - `send_notifications` (boolean, optional): Whether to send notifications about the calendar sharing change. Note that there are no notifications on access removal. Optional. The default is True.

### GOOGLESUPER_ACL_WATCH

**MCP Tool**: `mcp__composio__GOOGLESUPER_ACL_WATCH`

Tool to watch for changes to ACL resources. Use when you need to set up real-time notifications for access control list modifications on a calendar.

**Parameters:**
  - `id` (string, required): A UUID or similar unique string that identifies this channel.
  - `type` (string, optional): The type of delivery mechanism used for this channel. Valid values are "web_hook" or "webhook".
  - `token` (string, optional): An arbitrary string delivered to the target address with each notification delivered over this channel. Optional.
  - `params` (object, optional): Additional parameters controlling delivery channel behavior. Optional.
  - `address` (string, required): The address where notifications are delivered for this channel.
  - `pageToken` (string, optional): Token specifying which result page to return. Optional.
  - `syncToken` (string, optional): Token obtained from the nextSyncToken field returned on the last page of results from the previous list request. It makes the result of this list request contain only entries that have changed since then. All entries deleted since the previous list request will always be in the result set and it is not allowed to set showDeleted to False. Optional.
  - `calendarId` (string, required): Calendar identifier. To retrieve calendar IDs call the calendarList.list method. If you want to access the primary calendar of the currently logged in user, use the "primary" keyword.
  - `maxResults` (integer, optional): Maximum number of entries returned on one result page. By default the value is 100 entries. The page size can never be larger than 250 entries. Optional.
  - `showDeleted` (boolean, optional): Whether to include deleted ACLs in the result. Deleted ACLs are represented by role equal to 'none'. Deleted ACLs will always be included if syncToken is provided. Optional.

### GOOGLESUPER_ADD_ENRICHMENT

**MCP Tool**: `mcp__composio__GOOGLESUPER_ADD_ENRICHMENT`

Adds an enrichment at a specified position in a defined album.

**Parameters:**
  - `albumId` (string, required): Identifier of the album where the enrichment is to be added
  - `albumPosition` (object, required): Position in the album where the enrichment is to be inserted. Required field with valid position types: FIRST_IN_ALBUM, LAST_IN_ALBUM, AFTER_MEDIA_ITEM (requires relativeMediaItemId), AFTER_ENRICHMENT_ITEM (requires relativeEnrichmentItemId)
  - `newEnrichmentItem` (object, required): The enrichment to be added, can be a text, location, or map enrichment

### GOOGLESUPER_ADD_FILE_SHARING_PREFERENCE

**MCP Tool**: `mcp__composio__GOOGLESUPER_ADD_FILE_SHARING_PREFERENCE`

Modifies sharing permissions for an existing Google Drive file, granting a specified role to a user, group, domain, or 'anyone'.

**Parameters:**
  - `role` (string, required): Permission role to grant.
  - `type` (string, required): Type of grantee for the permission.
  - `domain` (string, optional): Domain to grant permission to (e.g., 'example.com'). Required if 'type' is 'domain'.
  - `file_id` (string, required): Unique identifier of the file to update sharing settings for. Must be an alphanumeric string containing only letters, numbers, hyphens, and underscores (no slashes, spaces, or other special characters). Use GOOGLEDRIVE_FIND_FILE or GOOGLEDRIVE_LIST_FILES to get valid file IDs from your Google Drive.
  - `email_address` (string, optional): Email address of the user or group. Required if 'type' is 'user' or 'group'.
  - `transfer_ownership` (boolean, optional): Whether to transfer ownership to the specified user. Required when role is 'owner'. Only a single user can be specified in the request when transferring ownership.

### GOOGLESUPER_ADD_LABEL_TO_EMAIL

**MCP Tool**: `mcp__composio__GOOGLESUPER_ADD_LABEL_TO_EMAIL`

Adds and/or removes specified Gmail labels for a message; ensure `message_id` and all `label_ids` are valid (use 'listLabels' for custom label IDs).

**Parameters:**
  - `user_id` (string, optional): User's email address or 'me' for the authenticated user.
  - `message_id` (string, required): Immutable ID of the message to modify. Gmail message IDs are 15-16 character hexadecimal strings (e.g., '1a2b3c4d5e6f7890'). IMPORTANT: Do NOT use UUIDs (32-character strings like '093ca4662b214d5eba8f4ceeaad63433'), thread IDs, or internal system IDs - these will cause 'Invalid id value' errors. Obtain valid message IDs from: (1) 'GMAIL_FETCH_EMAILS' response 'messageId' field, (2) 'GMAIL_FETCH_MESSAGE_BY_THREAD_ID' response, or (3) 'GMAIL_LIST_THREADS' and then fetching thread messages.
  - `add_label_ids` (array, optional): IMPORTANT: Label IDs are NOT the same as label names shown in Gmail UI. MODIFIABLE SYSTEM LABELS (use these exact IDs): INBOX, SPAM, TRASH, UNREAD, STARRED, IMPORTANT, CATEGORY_PERSONAL, CATEGORY_SOCIAL, CATEGORY_PROMOTIONS, CATEGORY_UPDATES, CATEGORY_FORUMS. Note: 'UPDATES', 'SOCIAL', 'PROMOTIONS', 'FORUMS', 'PERSONAL' are INVALID - you must use the full CATEGORY_ prefix (e.g., 'CATEGORY_UPDATES' not 'UPDATES'). CUSTOM LABELS: You MUST call 'listLabels' action first to get the label ID (format: 'Label_<number>', e.g., 'Label_1', 'Label_123'). Do NOT use the label name displayed in Gmail UI - the API requires the ID, not the name. Example: if listLabels returns {"id": "Label_5", "name": "Work Projects"}, use 'Label_5' (NOT 'Work Projects'). IMMUTABLE LABELS (cannot be added or removed): SENT, DRAFT, and CHAT are system labels managed by Gmail and cannot be modified via the API. Attempting to use these will return 'Invalid label' errors. A label cannot appear in both add_label_ids and remove_label_ids. At least one of 'add_label_ids' or 'remove_label_ids' must be non-empty.
  - `remove_label_ids` (array, optional): IMPORTANT: Label IDs are NOT the same as label names shown in Gmail UI. MODIFIABLE SYSTEM LABELS (use these exact IDs): INBOX, SPAM, TRASH, UNREAD, STARRED, IMPORTANT, CATEGORY_PERSONAL, CATEGORY_SOCIAL, CATEGORY_PROMOTIONS, CATEGORY_UPDATES, CATEGORY_FORUMS. Note: 'UPDATES', 'SOCIAL', 'PROMOTIONS', 'FORUMS', 'PERSONAL' are INVALID - you must use the full CATEGORY_ prefix (e.g., 'CATEGORY_UPDATES' not 'UPDATES'). CUSTOM LABELS: You MUST call 'listLabels' action first to get the label ID (format: 'Label_<number>', e.g., 'Label_1', 'Label_123'). Do NOT use the label name displayed in Gmail UI - the API requires the ID, not the name. IMMUTABLE LABELS (cannot be added or removed): SENT, DRAFT, and CHAT are system labels managed by Gmail and cannot be modified via the API. Attempting to use these will return 'Invalid label' errors. Common operations: to mark as read, REMOVE 'UNREAD'; to archive, REMOVE 'INBOX'. A label cannot appear in both add_label_ids and remove_label_ids. At least one of 'add_label_ids' or 'remove_label_ids' must be non-empty.

### GOOGLESUPER_ADD_OR_REMOVE_TO_CUSTOMER_LIST

**MCP Tool**: `mcp__composio__GOOGLESUPER_ADD_OR_REMOVE_TO_CUSTOMER_LIST`

AddOrRemoveToCustomerList Tool will add a contact to a customer list in Google Ads. Note: It takes 6 to 12 hours for changes to be reflected in the customer list.

**Parameters:**
  - `emails` (array, required): Array of emails of the contacts to be added to the customer list.
  - `operation` (string, optional): Operation to be performed on the customer list. Either create or remove.
  - `resource_name` (string, required): Resource name of the customer list. For example: customers/1234567890/userLists/1234567890

### GOOGLESUPER_ADD_SHARED_DRIVE_MEMBER

**MCP Tool**: `mcp__composio__GOOGLESUPER_ADD_SHARED_DRIVE_MEMBER`

Tool to add a member (user/group/domain) to a Shared Drive with a specified role. Use when you need to grant access to a Shared Drive, ensuring supportsAllDrives is set correctly.

**Parameters:**
  - `role` (string, required): Permission role to grant. Must be one of: organizer, fileOrganizer, writer, commenter, or reader.
  - `type` (string, required): Type of grantee for the permission. Must be 'user', 'group', or 'domain'.
  - `domain` (string, optional): Domain to grant permission to (e.g., 'example.com'). Required if 'type' is 'domain'.
  - `drive_id` (string, required): The ID of the Shared Drive to which the member will be added. Must be a non-empty string.
  - `email_address` (string, optional): Email address of the user or group. Required if 'type' is 'user' or 'group'.
  - `email_message` (string, optional): A plain text custom message to include in the notification email.
  - `send_notification_email` (boolean, optional): Whether to send a notification email when sharing to users or groups. This defaults to true for users and groups, and is not allowed for other requests.
  - `use_domain_admin_access` (boolean, optional): Issue the request as a domain administrator; if set to true, then the requester will be granted access if the Shared Drive belongs to the requester's domain and the requester is a domain administrator.

### GOOGLESUPER_ADD_SHEET

**MCP Tool**: `mcp__composio__GOOGLESUPER_ADD_SHEET`

Adds a new sheet to a spreadsheet. Supports three sheet types: GRID, OBJECT, and DATA_SOURCE. SHEET TYPES: - GRID (default): Standard spreadsheet with rows/columns. Use properties to set dimensions, tab color, etc. - OBJECT: Sheet containing a chart. Requires objectSheetConfig with chartSpec (basicChart or pieChart). - DATA_SOURCE: Sheet connected to BigQuery. Requires dataSourceConfig with bigQuery spec and bigquery.readonly OAuth scope. OTHER NOTES: - Sheet names must be unique; use forceUnique=true to auto-append suffix (_2, _3) if name exists - For tab colors, use EITHER rgbColor OR themeColor, not both - Avoid 'index' when creating sheets in parallel (causes errors) - OBJECT sheets are created via addChart with position.newSheet=true - DATA_SOURCE sheets require bigquery.readonly OAuth scope Use cases: Add standard grid sheet, create chart on dedicated sheet, connect to BigQuery data source.

**Parameters:**
  - `title` (string, optional): The name for the new sheet tab. Must be unique within the spreadsheet. Example: "Q3 Report", "Sales Data 2025". This is a convenience parameter - alternatively, you can set this via properties.title. Note: sheet_name is also accepted as an alias for title.
  - `properties` (object, optional): Advanced sheet properties (grid dimensions, tab color, position, etc.). For simple cases, just use the 'title' parameter directly. Use this for additional customization.
  - `forceUnique` (boolean, optional): When True (default), automatically ensures the sheet name is unique by appending a numeric suffix (e.g., '_2', '_3') if the requested name already exists. This makes the action resilient to retries and parallel workflows. When False, the action fails with an error if a sheet with the same name already exists.
  - `spreadsheetId` (string, required): REQUIRED. Cannot be empty. The ID of the target spreadsheet where the new sheet will be added. This is the long alphanumeric string in the Google Sheet URL (e.g., '1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms'). Use 'Search Spreadsheets' action to find the spreadsheet ID by name if you don't have it.
  - `dataSourceConfig` (object, optional): Configuration for creating a DATA_SOURCE sheet.

DATA_SOURCE sheets connect to external data sources like BigQuery.
The API uses addDataSource request which automatically creates the associated sheet.

IMPORTANT: Requires additional OAuth scope: bigquery.readonly
  - `objectSheetConfig` (object, optional): Configuration for creating an OBJECT sheet (a sheet containing a chart).

To create an OBJECT sheet, you must provide chart configuration.
The API uses addChart with position.newSheet=true to create the chart on its own sheet.

### GOOGLESUPER_ADMIN_PROPS_DV360_AD_LINKS_LIST

**MCP Tool**: `mcp__composio__GOOGLESUPER_ADMIN_PROPS_DV360_AD_LINKS_LIST`

Tool to list Display & Video 360 advertiser links on a property. Use when you need to retrieve DisplayVideo360AdvertiserLink resources associated with a Google Analytics property. Results support pagination for large datasets.

**Parameters:**
  - `parent` (string, required): Required. The name of the property to list DisplayVideo360AdvertiserLinks under. Format: properties/{property_id} (e.g., properties/1234).
  - `pageSize` (integer, optional): Optional. Maximum number of resources to return. The service may return fewer than this value. If unspecified, at most 50 resources will be returned. The maximum value is 200; higher values will be coerced to the maximum.
  - `pageToken` (string, optional): Optional. A page token received from a previous ListDisplayVideo360AdvertiserLinks call. Provide this to retrieve the subsequent page. When paginating, all other parameters provided must match the call that provided the page token.

### GOOGLESUPER_ADMIN_PROPS_DV360_LINK_PROPOSALS_LIST

**MCP Tool**: `mcp__composio__GOOGLESUPER_ADMIN_PROPS_DV360_LINK_PROPOSALS_LIST`

Tool to list DisplayVideo360AdvertiserLinkProposals on a property. Use when you need to retrieve Display & Video 360 advertiser link proposals associated with a Google Analytics property. Results support pagination for large datasets.

**Parameters:**
  - `parent` (string, required): Required. The name of the property to list DisplayVideo360AdvertiserLinkProposals under. Format: properties/{property_id} (e.g., properties/489591273).
  - `pageSize` (integer, optional): Optional. Maximum number of resources to return. If unspecified, at most 50 resources will be returned. The maximum value is 200; higher values will be coerced to the maximum.
  - `pageToken` (string, optional): Optional. A page token received from a previous ListDisplayVideo360AdvertiserLinkProposals call. Provide this to retrieve the subsequent page. When paginating, all other parameters must match the call that provided the page token.

### GOOGLESUPER_AGGREGATE_COLUMN_DATA

**MCP Tool**: `mcp__composio__GOOGLESUPER_AGGREGATE_COLUMN_DATA`

Searches for rows where a specific column matches a value and performs mathematical operations on data from another column.

**Parameters:**
  - `operation` (string, required): The mathematical operation to perform on the target column values.
  - `sheet_name` (string, required): The name of the specific sheet within the spreadsheet. Matching is case-insensitive. If no exact match is found, partial matches will be attempted (e.g., 'overview' will match 'Overview 2025').
  - `search_value` (string, optional): The exact value to search for in the search column. Case-sensitive by default. If not provided (or if search_column is not provided), all rows in the target column will be aggregated without filtering.
  - `search_column` (string, optional): The column to search in for filtering rows. Can be a letter (e.g., 'A', 'B') or column name from header row (e.g., 'Region', 'Department'). If not provided, all rows in the target column will be aggregated without filtering.
  - `target_column` (string, required): The column to aggregate data from. Can be a letter (e.g., 'C', 'D') or column name from header row (e.g., 'Sales', 'Revenue').
  - `case_sensitive` (boolean, optional): Whether the search should be case-sensitive.
  - `has_header_row` (boolean, optional): Whether the first row contains column headers. If True, column names can be used for search_column and target_column.
  - `spreadsheet_id` (string, required): The unique identifier of the Google Sheets spreadsheet.
  - `percentage_total` (number, optional): For percentage operation, the total value to calculate percentage against. If not provided, uses sum of all values in target column.

### GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_AUDIENCES_LIST

**MCP Tool**: `mcp__composio__GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_AUDIENCES_LIST`

Tool to list Audiences on a property. Use when you need to retrieve audience configurations for a Google Analytics property. Audiences created before 2020 may not be supported.

**Parameters:**
  - `parent` (string, required): Required. The property for which to list Audiences. Format: properties/{propertyId}
  - `pageSize` (integer, optional): Optional. The maximum number of resources to return. If unspecified, at most 50 resources will be returned. The maximum value is 200
  - `pageToken` (string, optional): Optional. A page token received from a previous ListAudiences call. Provide this to retrieve the subsequent page

### GOOGLESUPER_ANALYTICS_ADMIN_PROPERTIES_BIG_QUERY_LINKS

**MCP Tool**: `mcp__composio__GOOGLESUPER_ANALYTICS_ADMIN_PROPERTIES_BIG_QUERY_LINKS`

Tool to list BigQuery Links on a property. Use when you need to retrieve BigQuery link resources associated with a Google Analytics property. Results support pagination for large datasets.

**Parameters:**
  - `parent` (string, required): Required. The name of the property to list BigQuery links under. Format: properties/{property_id} (e.g., properties/1234).
  - `pageSize` (integer, optional): Optional. Maximum number of resources to return. The service may return fewer than this value. If unspecified, at most 50 resources will be returned. The maximum value is 200; higher values will be coerced to the maximum.
  - `pageToken` (string, optional): Optional. A page token received from a previous ListBigQueryLinks call. Provide this to retrieve the subsequent page. When paginating, all other parameters provided to ListBigQueryLinks must match the call that provided the page token.

### GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_CHANNELGROUPS

**MCP Tool**: `mcp__composio__GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_CHANNELGROUPS`

Tool to list ChannelGroups on a property. Use when you need to retrieve channel groups that categorize traffic sources in Analytics reports.

**Parameters:**
  - `parent` (string, required): Required. The property for which to list ChannelGroups. Format: properties/{property_id}
  - `pageSize` (integer, optional): Optional. The maximum number of resources to return. If unspecified, at most 50 resources will be returned. The maximum value is 200
  - `pageToken` (string, optional): Optional. A page token received from a previous ListChannelGroups call. Provide this to retrieve the subsequent page

### GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_CONVERSIONEVENTS

**MCP Tool**: `mcp__composio__GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_CONVERSIONEVENTS`

Tool to list conversion events on a property. Use when you need to retrieve conversion events configured for a given property.

**Parameters:**
  - `parent` (string, required): Required. The resource name of the parent property. Format: 'properties/{propertyId}'
  - `pageSize` (integer, optional): Optional. The maximum number of resources to return. If unspecified, at most 50 resources will be returned. The maximum value is 200
  - `pageToken` (string, optional): Optional. A page token received from a previous ListConversionEvents call. Provide this to retrieve the subsequent page

### GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_CUSTOMDIMENSIONS

**MCP Tool**: `mcp__composio__GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_CUSTOMDIMENSIONS`

Tool to list CustomDimensions on a property. Use when you need to retrieve custom dimensions configured for a given property.

**Parameters:**
  - `parent` (string, required): Required. The property for which to list CustomDimensions. Format: properties/{property_id}
  - `pageSize` (integer, optional): Optional. The maximum number of resources to return. If unspecified, at most 50 resources will be returned. The maximum value is 200
  - `pageToken` (string, optional): Optional. A page token received from a previous ListCustomDimensions call. Provide this to retrieve the subsequent page

### GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_DATASTREAMS

**MCP Tool**: `mcp__composio__GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_DATASTREAMS`

Tool to list MeasurementProtocolSecrets under a data stream. Use when you need to retrieve measurement protocol secrets for server-side event tracking.

**Parameters:**
  - `parent` (string, required): Required. The resource name of the parent data stream. Format: properties/{property}/dataStreams/{dataStream}
  - `pageSize` (integer, optional): Optional. Maximum number of resources to return. If unspecified, at most 10 resources will be returned. The maximum value is 10
  - `pageToken` (string, optional): Optional. A page token received from a previous ListMeasurementProtocolSecrets call. Provide this to retrieve the subsequent page


## Usage Examples

#### GOOGLESUPER_ACL_DELETE
```
Use MCP tool: mcp__composio__GOOGLESUPER_ACL_DELETE
Parameters: {
  "calendar_id": "your_calendar_id_here",
  "rule_id": "your_rule_id_here"
}
```

#### GOOGLESUPER_ACL_GET
```
Use MCP tool: mcp__composio__GOOGLESUPER_ACL_GET
Parameters: {
  "calendar_id": "your_calendar_id_here",
  "rule_id": "your_rule_id_here"
}
```

#### GOOGLESUPER_ACL_INSERT
```
Use MCP tool: mcp__composio__GOOGLESUPER_ACL_INSERT
Parameters: {
  "calendar_id": "your_calendar_id_here",
  "role": "your_role_here"
}
```

## Quick Reference

| MCP Tool | Description |
|----------|-------------|
| `mcp__composio__GOOGLESUPER_ACL_DELETE` | Deletes an access control rule from a Google Calen... |
| `mcp__composio__GOOGLESUPER_ACL_GET` | Retrieves a specific access control rule for a cal... |
| `mcp__composio__GOOGLESUPER_ACL_INSERT` | Creates an access control rule for a calendar. Use... |
| `mcp__composio__GOOGLESUPER_ACL_PATCH` | Updates an existing access control rule for a cale... |
| `mcp__composio__GOOGLESUPER_ACL_WATCH` | Tool to watch for changes to ACL resources. Use wh... |
| `mcp__composio__GOOGLESUPER_ADD_ENRICHMENT` | Adds an enrichment at a specified position in a de... |
| `mcp__composio__GOOGLESUPER_ADD_FILE_SHARING_PREFERENCE` | Modifies sharing permissions for an existing Googl... |
| `mcp__composio__GOOGLESUPER_ADD_LABEL_TO_EMAIL` | Adds and/or removes specified Gmail labels for a m... |
| `mcp__composio__GOOGLESUPER_ADD_OR_REMOVE_TO_CUSTOMER_LIST` | AddOrRemoveToCustomerList Tool will add a contact ... |
| `mcp__composio__GOOGLESUPER_ADD_SHARED_DRIVE_MEMBER` | Tool to add a member (user/group/domain) to a Shar... |
| `mcp__composio__GOOGLESUPER_ADD_SHEET` | Adds a new sheet to a spreadsheet. Supports three ... |
| `mcp__composio__GOOGLESUPER_ADMIN_PROPS_DV360_AD_LINKS_LIST` | Tool to list Display & Video 360 advertiser links ... |
| `mcp__composio__GOOGLESUPER_ADMIN_PROPS_DV360_LINK_PROPOSALS_LIST` | Tool to list DisplayVideo360AdvertiserLinkProposal... |
| `mcp__composio__GOOGLESUPER_AGGREGATE_COLUMN_DATA` | Searches for rows where a specific column matches ... |
| `mcp__composio__GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_AUDIENCES_LIST` | Tool to list Audiences on a property. Use when you... |
| `mcp__composio__GOOGLESUPER_ANALYTICS_ADMIN_PROPERTIES_BIG_QUERY_LINKS` | Tool to list BigQuery Links on a property. Use whe... |
| `mcp__composio__GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_CHANNELGROUPS` | Tool to list ChannelGroups on a property. Use when... |
| `mcp__composio__GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_CONVERSIONEVENTS` | Tool to list conversion events on a property. Use ... |
| `mcp__composio__GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_CUSTOMDIMENSIONS` | Tool to list CustomDimensions on a property. Use w... |
| `mcp__composio__GOOGLESUPER_ANALYTICSADMIN_PROPERTIES_DATASTREAMS` | Tool to list MeasurementProtocolSecrets under a da... |

## When to Use This Integration

**Use googlesuper when:**
- Interacting with googlesuper services
- Automating googlesuper workflows
- Reading and writing googlesuper data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:
1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
