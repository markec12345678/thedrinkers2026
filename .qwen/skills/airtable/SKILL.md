---
name: airtable
description: Airtable merges spreadsheet functionality with database power, enabling teams to organize projects, track tasks, and collaborate through customizable views, automation, and integrations for data management integration via Composio MCP. Use when user needs to interact with Airtable.
---

# Airtable Integration

> Auto-generated by Composio Skills Extension on 2026-03-09T16:10:25.936Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Airtable merges spreadsheet functionality with database power, enabling teams to organize projects, track tasks, and collaborate through customizable views, automation, and integrations for data management

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### AIRTABLE_CREATE_BASE

**MCP Tool**: `mcp__composio__AIRTABLE_CREATE_BASE`

Creates a new Airtable base with specified tables and fields within a workspace.

**Parameters:**
  - `name` (string, required): Name for the new Airtable base, visible in the UI.
  - `tables` (array, required): Configurations for tables to be created in the new base; each table needs 'name' and 'fields'. Must be an array of table objects, not a stringified JSON. The first field in each table cannot be 'singleSelect' as it becomes the primary field. Select field choices must include both 'id' and 'name' for each choice. Field types 'formula', 'rollup', 'count', 'lookup', 'multipleLookupValues', 'autoNumber', 'createdTime', and 'lastModifiedTime' are not supported at base creation. For 'dateTime' fields, options require 'dateFormat' (with name: 'local'|'friendly'|'us'|'european'|'iso') and 'timeFormat' (with name: '12hour'|'24hour'); 'timeZone' defaults to 'utc' if not specified.
  - `workspaceId` (string, required): Workspace ID where the base will be created. MUST start with 'wsp' prefix (e.g., 'wspXXXXXXXXXXXXXX'). Common mistake: Do not use user IDs (usr prefix), base IDs (app prefix), or other Airtable IDs - only workspace IDs work here. Get your workspace ID from Airtable workspace settings or via the List Bases API.

### AIRTABLE_CREATE_COMMENT

**MCP Tool**: `mcp__composio__AIRTABLE_CREATE_COMMENT`

Tool to create a comment on a specific Airtable record. Use when adding comments to records, mentioning collaborators using @[userId] syntax, or creating threaded comment replies. Supports optional parentCommentId for threaded conversations.

**Parameters:**
  - `text` (string, required): The content of the comment. To mention a user, use the format @[userId] (e.g., @[usrGISFlfA7l5F7kY6]).
  - `baseId` (string, required): The unique identifier of the Airtable base. This typically starts with 'app'.
  - `recordId` (string, required): The unique identifier of the record on which the comment will be created. This typically starts with 'rec'.
  - `tableIdOrName` (string, required): The unique identifier (typically starting with 'tbl') or the name of the table within the base.
  - `parentCommentId` (string, optional): The ID of the parent comment for creating threaded replies. If provided, this comment will be a reply to the specified parent comment.

### AIRTABLE_CREATE_FIELD

**MCP Tool**: `mcp__composio__AIRTABLE_CREATE_FIELD`

Creates a new field within a specified table in an Airtable base.

**Parameters:**
  - `name` (string, required): REQUIRED. The name for the new field. Field names must be unique within a table.
  - `type` (string, optional): The type for the new field. If not provided, it defaults to 'singleLineText'. Types that can be created via API: 'singleLineText', 'multilineText', 'richText', 'singleSelect', 'multipleSelects', 'checkbox', 'number', 'percent', 'currency', 'date', 'dateTime', 'duration', 'phoneNumber', 'email', 'url', 'multipleAttachments', 'barcode', 'rating', 'multipleRecordLinks', 'createdBy', 'lastModifiedBy', 'externalSyncSource'. For attachment fields, use 'multipleAttachments' (legacy 'attachment' is auto-converted). Cannot be created via API: 'formula', 'rollup', 'count', 'lookup', 'multipleLookupValues', 'autoNumber', 'createdTime', 'lastModifiedTime', 'button'.
  - `baseId` (string, required): REQUIRED. The unique identifier of the base where the field will be created. Format: 'appXXXXXXXXXXXXXXX' (17 characters starting with 'app').
  - `options` (string, optional): Type-specific configuration for the new field. Required for types: 'number', 'percent' (requires 'precision'), 'currency' (requires 'precision' and 'symbol'), 'checkbox' (requires 'color' and 'icon'), 'date' (requires 'dateFormat'), 'dateTime' (requires 'dateFormat', 'timeFormat', and 'timeZone'), 'rating' (requires 'color' and 'icon'), 'multipleRecordLinks' (requires 'linkedTableId' - the ID of the table to link to, format: tblXXXXXXXXXXXXXX). For 'singleSelect' and 'multipleSelects', 'choices' array must include objects with 'name' (required) and optionally 'color'. Do not include 'id' when creating new choices - the API will auto-generate them.
  - `description` (string, optional): An optional description for the new field. This can provide context or instructions for the field's usage.
  - `tableIdOrName` (string, required): The ID or name of the table within the base where the field will be created. Table IDs start with 'tbl' followed by 14 characters. Table names are case-sensitive and must match exactly as they appear in Airtable.

### AIRTABLE_CREATE_MULTIPLE_RECORDS

**MCP Tool**: `mcp__composio__AIRTABLE_CREATE_MULTIPLE_RECORDS`

Creates multiple new records in a specified Airtable table.

**Parameters:**
  - `baseId` (string, required): Required. The unique identifier of the Airtable base (e.g., 'appXXXXXXXXXXXXXX').
  - `records` (array, required): Required. List of record objects to create. Each record must have a 'fields' key containing a dictionary mapping field names to values. Field names are case-sensitive and can contain any Unicode characters (e.g., 'Тема Reels', '日期'). Field values must match their configured types in Airtable (e.g., number fields require numeric values). Set typecast=true to enable automatic type conversion from strings. Note: Airtable's API limits creation to 10 records per request; if you provide more than 10 records, they will be automatically split into batches and processed in multiple requests.
  - `typecast` (boolean, optional): If true, Airtable will perform automatic type conversion. Helps with: converting strings to numbers, creating new options for single/multi-select fields, automatically creating/linking records in linked record fields from text values, and type coercion for compatible types. Does NOT help with: text field length/format constraints or fundamental type incompatibilities. If you get INVALID_VALUE_FOR_COLUMN errors on text fields, check: (1) field type in Airtable (single-line vs long text), (2) any custom character limits, (3) whether the field expects a specific format. Defaults to false.
  - `tableIdOrName` (string, required): Required. The table ID (e.g., 'tblXXXXXXXXXXXXXX') or the table name (e.g., 'Tasks').
  - `returnFieldsByFieldId` (boolean, optional): If true, the returned field objects will use field IDs as keys instead of field names. Using field IDs is recommended for integrations that need to be resilient to field name changes. Default is false, which returns field names as keys.

### AIRTABLE_CREATE_RECORD

**MCP Tool**: `mcp__composio__AIRTABLE_CREATE_RECORD`

Creates a new record in a specified Airtable table; field values must conform to the table's column types.

**Parameters:**
  - `baseId` (string, required): Required. The unique identifier of the Airtable base (e.g., 'appXXXXXXXXXXXXXX'). Use AIRTABLE_LIST_BASES to find available base IDs.
  - `fields` (object, optional): A dictionary of field names (or IDs) and their values for the new record. Field names must exactly match those in the table schema (case-sensitive). Field IDs (e.g., 'fldXXXXXXXXXXXXXX') are preferred for stability, but are UNIQUE to each base and table - a field ID from one table cannot be used in a different table. IMPORTANT: Use AIRTABLE_GET_BASE_SCHEMA with the specific baseId to discover valid field names, IDs, types, and options for that table before creating records. Note: Fields with null, empty string, or boolean false values are automatically omitted from the request - null/empty strings are rejected by many field types, and boolean false for checkbox fields means 'unchecked' which is the default state. Field type value requirements: - singleSelect: Must be a plain string option name from the field's 'choices' array in the schema (e.g., 'High', 'Todo', 'In Progress'). DO NOT pass dict objects like {'id': 'selXXX'} or {'name': 'High'} - pass only the plain string 'High'. Invalid options will be rejected. - multipleSelects: Must be an array of exact option names from the field's 'choices' array (e.g., ['Bug', 'Feature']). - checkbox: Use true to check the box. To leave unchecked, either omit the field or pass false (false values are filtered out as unchecked is the default). - number/currency/percent/rating: Must be a numeric value (int or float). For currency fields, you can pass formatted currency strings with symbols (e.g., '$100', '£689', '€50') which will be automatically converted to numeric values. Plain numeric strings without currency symbols (e.g., '100', '50.25') are NOT automatically converted - pass actual numbers or use typecast=true.- date: Must be ISO 8601 format 'YYYY-MM-DD'. - dateTime: Must be ISO 8601 format with or without timezone (e.g., '2024-01-15T14:30:00.000Z', '2024-01-15T14:30:00.208000', '2024-01-15T14:30:00Z', '2024-01-15T14:30:00.648697+00:00'). For fields with date/datetime-related names (containing 'date', 'datetime', 'timestamp', 'created', 'modified', 'scheduled', 'deadline', 'due', etc.): ISO 8601 formats (with T separator) are passed through completely unchanged to preserve exact precision and timezone information. Other common formats like '2024-01-15 14:30:00', 'Jan 15, 2024', '01/15/2024' are automatically converted to ISO 8601 with UTC timezone. - singleLineText/multilineText/richText: Must be a string. - email/url/phoneNumber: Must be a string in appropriate format. - multipleAttachments: Must be an array of attachment objects with 'url' field. - singleCollaborator: Must be an object with 'id' or 'email' of a valid collaborator. - multipleRecordLinks: Must be an array of record IDs (e.g., ['recXXX', 'recYYY']). - duration: Must be a number representing total seconds (e.g., 278 for 4 minutes 38 seconds). Common conversions: 1 minute = 60, 1 hour = 3600, 1h30m = 5400. For fields with duration-related names (containing 'duration', 'elapsed', 'length', 'runtime', 'playtime'), time strings like 'H:MM:SS' (e.g., '0:04:38') or ISO 8601 duration (e.g., 'PT4M38S') are automatically converted to seconds. For fields with currency-related names (containing 'amount', 'price', 'cost', 'fee', 'salary', 'budget', 'payment', 'total', etc.), only formatted currency strings WITH currency symbols (e.g., '£689', '$1,000.50', '€50') are automatically converted to numeric values. Plain numeric strings like '100' or '50.25' are NOT converted to avoid corrupting text fields - if you need to pass plain numeric strings to currency fields, use typecast=true.
  - `typecast` (boolean, optional): When set to true, enables automatic data conversion from strings to appropriate field types. For example, if a singleSelect field receives a value that doesn't match an existing option, Airtable will create a new option with that value. Also converts string numbers to numeric types. Default is false, which requires exact type matching.
  - `tableIdOrName` (string, required): Required. The table ID (e.g., 'tblXXXXXXXXXXXXXX') or table name (e.g., 'Tasks', 'Meal Plan') where the record will be created. Table names with spaces and special characters are supported. This is separate from baseId and must be provided. Use AIRTABLE_GET_BASE_SCHEMA to find table IDs/names within a base.
  - `returnFieldsByFieldId` (boolean, optional): If true, the returned field objects will use field IDs as keys instead of field names. Using field IDs is recommended for integrations that need to be resilient to field name changes. Default is false, which returns field names as keys.

### AIRTABLE_CREATE_RECORD_FROM_NATURAL_LANGUAGE

**MCP Tool**: `mcp__composio__AIRTABLE_CREATE_RECORD_FROM_NATURAL_LANGUAGE`

Creates a new record in an Airtable table from a natural language description. Fetches the table schema, uses an LLM to generate the correct field payload, and creates the record with typecast enabled for automatic type conversion.

**Parameters:**
  - `baseId` (string, required): Unique identifier of the Airtable base, typically starting with 'app'.
  - `nl_query` (string, required): Natural language description of the record to create. Example: 'Add a task called Fix Login Bug, status In Progress, priority High, due date 2025-03-15'.
  - `typecast` (boolean, optional): If True, Airtable will automatically convert string values to their appropriate types (e.g., string to number, date parsing). Enabled by default.
  - `tableIdOrName` (string, required): Unique identifier (typically starting with 'tbl') or name of the table within the specified base.

### AIRTABLE_CREATE_RECORDS

**MCP Tool**: `mcp__composio__AIRTABLE_CREATE_RECORDS`

Tool to create multiple records (up to 10) in a specified Airtable table. Use when you need to add new rows to a table with field values. Rate limit: 5 requests per second per base.

**Parameters:**
  - `baseId` (string, required): The unique identifier of the Airtable base. Format: 'app' followed by alphanumeric characters (e.g., 'appHPvgqYXl3LNHXq').
  - `records` (array, required): Array of record objects to create. Each record object must contain a 'fields' property with the cell values. Maximum of 10 records can be created per request.
  - `typecast` (boolean, optional): If true, Airtable will perform best-effort automatic data conversion from string values to the appropriate cell type. If false (default), field values must exactly match the expected type. Use with caution as it may produce unexpected results.
  - `tableIdOrName` (string, required): The unique identifier or name of the table where records will be created. Table IDs are recommended over names for stability. Format: 'tbl' followed by alphanumeric characters (e.g., 'tblUkxCONS303zkN7').
  - `returnFieldsByFieldId` (boolean, optional): If true, the returned field objects will use field IDs as keys instead of field names. Defaults to false.

### AIRTABLE_CREATE_TABLE

**MCP Tool**: `mcp__composio__AIRTABLE_CREATE_TABLE`

Creates a new table within a specified existing Airtable base, allowing definition of its name, description, and field structure.

**Parameters:**
  - `name` (string, required): The desired name for the new table.
  - `baseId` (string, required): The unique identifier of the base where the new table will be created.
  - `fields` (array, required): A list of field configurations for the new table. Each configuration defines a column and must include a 'name' (string) and 'type' (string). IMPORTANT: The first field becomes the primary field and has strict type restrictions. Valid primary field types for API creation: singleLineText, number, email, url, phoneNumber, date, dateTime, currency, percent, barcode, richText, multilineText, duration. Note: While formula and autoNumber CAN be primary fields in the Airtable UI, they CANNOT be created via the API and are therefore not valid options when creating tables. INVALID primary field types (cannot be first): multipleRecordLinks, singleSelect, multipleSelects, checkbox, singleCollaborator, multipleCollaborators, multipleAttachments, lookup, rollup, count, createdTime, lastModifiedTime, createdBy, lastModifiedBy, button, aiText, externalSyncSource, rating. If you need a multipleRecordLinks field, place a singleLineText field first to serve as the primary field. Field types 'formula', 'rollup', 'count', 'lookup', 'multipleLookupValues', 'autoNumber', 'createdTime', and 'lastModifiedTime' are not supported for table creation via the API (computed/formula-based fields can only be created in the UI). Some field types require an 'options' object: 'number'/'percent' (precision), 'currency' (precision, symbol), 'checkbox' (color, icon - colors must end in 'Bright', e.g., 'yellowBright'), 'date' (dateFormat), 'dateTime' (dateFormat, timeFormat, timeZone), 'rating' (color, icon, and optionally max 1-10 which defaults to 5 - colors must end in 'Bright', e.g., 'yellowBright'). For 'singleSelect' and 'multipleSelects', provide 'choices' with objects containing 'name' only (do not include 'id' - Airtable auto-generates valid IDs). Optionally include 'color' for each choice. For 'multipleRecordLinks', provide options with 'linkedTableId' (must be a valid Airtable table ID in format tblXXXXXXXXXXXXXX). Do NOT include 'isReversed' or 'prefersSingleRecordLink' - these are not accepted during creation and will be auto-set by the API.
  - `if_exists` (string, optional): Options for handling duplicate table name scenarios.
  - `description` (string, optional): An optional textual description for the new table.
  - `reorder_message` (string, optional): Internal field used to track field reordering messages. This is populated automatically when fields are reordered.

### AIRTABLE_DELETE_COMMENT

**MCP Tool**: `mcp__composio__AIRTABLE_DELETE_COMMENT`

Tool to delete a comment from a record in an Airtable table. Use when you need to remove an existing comment. Non-admin users can only delete their own comments; Enterprise Admins can delete any comment.

**Parameters:**
  - `baseId` (string, required): The unique identifier of the Airtable base from which the comment will be deleted.
  - `recordId` (string, required): The unique identifier of the record within the specified table from which the comment will be deleted.
  - `rowCommentId` (string, required): The unique identifier of the comment to be deleted from the specified record.
  - `tableIdOrName` (string, required): The unique identifier (ID) or name of the table within the specified base that contains the record and comment.

### AIRTABLE_DELETE_MULTIPLE_RECORDS

**MCP Tool**: `mcp__composio__AIRTABLE_DELETE_MULTIPLE_RECORDS`

Tool to delete up to 10 specified records from a table within an Airtable base. Use when you need to remove multiple records in a single operation.

**Parameters:**
  - `baseId` (string, required): The unique identifier of the Airtable base containing the records to be deleted.
  - `recordIds` (array, required): A list of unique identifiers for the records to be deleted.
  - `tableIdOrName` (string, required): The unique identifier (e.g., 'tbluIzIsxPIM2V3Y4') or the name (e.g., 'Marketing Leads') of the table within the base from which records will be deleted.

### AIRTABLE_DELETE_RECORD

**MCP Tool**: `mcp__composio__AIRTABLE_DELETE_RECORD`

Permanently deletes a specific record from an existing table within an existing Airtable base.

**Parameters:**
  - `baseId` (string, required): Unique identifier of the Airtable base, typically starting with 'app'.
  - `recordId` (string, required): Unique identifier of the record to be deleted, typically starting with 'rec'.
  - `tableIdOrName` (string, required): Unique identifier (typically starting with 'tbl') or name of the table within the specified base.

### AIRTABLE_GET_BASE_SCHEMA

**MCP Tool**: `mcp__composio__AIRTABLE_GET_BASE_SCHEMA`

Retrieves the detailed schema for a specified Airtable base, including its tables, fields, field types, and configurations, using the `baseId`.

**Parameters:**
  - `baseId` (string, required): The unique identifier for the Airtable base. This ID typically starts with 'app' and is followed by a string of alphanumeric characters. It can be found in the URL when viewing the base.
  - `include` (array, optional): Optional list of additional fields to include in the views object response. Currently only supports the value 'visibleFieldIds' (for views of type 'grid' only). When specified, grid views will include a visibleFieldIds array showing which fields are visible in that view.

### AIRTABLE_GET_RECORD

**MCP Tool**: `mcp__composio__AIRTABLE_GET_RECORD`

Retrieves a specific record from an Airtable table by its record ID. Requires a known, valid record ID obtained from listing records or another API call - this tool cannot search or list records. Use the list records tool to find record IDs. Empty field values are not returned in the response.

**Parameters:**
  - `baseId` (string, required): ID of the Airtable base.
  - `recordId` (string, required): ID of the record to retrieve. Must be a real record ID obtained from listing records or another API call. Format: starts with 'rec' followed by exactly 14 alphanumeric characters (e.g., 'rec4YsVV8cU6V4EiT'). This tool retrieves a single specific record - to list or search multiple records, use the list records tool instead.
  - `timeZone` (string, optional): Time zone for formatting dates when cellFormat='string'. Required when cellFormat='string'. Uses IANA time zone format (e.g., 'America/New_York', 'Europe/London', 'UTC').
  - `cellFormat` (string, optional): Controls cell value formatting: 'json' for native types, 'string' for all values as strings. When 'string', timeZone and userLocale are required.
  - `userLocale` (string, optional): User locale for formatting dates when cellFormat='string'. Required when cellFormat='string'. Uses BCP 47 language tag format (e.g., 'en-us', 'en-gb', 'de-de').
  - `tableIdOrName` (string, required): ID or name of the table.
  - `returnFieldsByFieldId` (boolean, optional): If true, field keys in the response are field IDs; otherwise, field names.

### AIRTABLE_GET_USER_INFO

**MCP Tool**: `mcp__composio__AIRTABLE_GET_USER_INFO`

Retrieves information, such as ID and permission scopes, for the currently authenticated Airtable user from the `/meta/whoami` endpoint.

**Parameters:**
  - No parameters required

### AIRTABLE_LIST_BASES

**MCP Tool**: `mcp__composio__AIRTABLE_LIST_BASES`

Retrieves all Airtable bases accessible to the authenticated user, which may include an 'offset' for pagination.

**Parameters:**
  - `offset` (string, optional): Pagination token for retrieving the next page of results. This is an opaque cursor returned in the 'offset' field of a previous list bases response. Pass this exact value to retrieve the next page. Do not construct or modify this value. Only present when there are more than 1,000 bases.

### AIRTABLE_LIST_COMMENTS

**MCP Tool**: `mcp__composio__AIRTABLE_LIST_COMMENTS`

Tool to list comments on a specific Airtable record. Use when retrieving comments for a record, with optional pagination support for large comment threads.

**Parameters:**
  - `baseId` (string, required): The unique identifier of the Airtable base from which to list comments.
  - `offset` (string, optional): Pagination token to retrieve the next page of comments. If specified, the returned comments will begin at the specified offset. Obtained from the previous response.
  - `pageSize` (integer, optional): The number of comments to return per page. Must be less than or equal to 100. Defaults to 100 if not specified.
  - `recordId` (string, required): The unique identifier of the record for which comments are to be listed.
  - `tableIdOrName` (string, required): The unique identifier or name of the table within the specified base containing the record.

### AIRTABLE_LIST_RECORDS

**MCP Tool**: `mcp__composio__AIRTABLE_LIST_RECORDS`

Tool to list records from an Airtable table with filtering, sorting, and pagination. Use when you need to retrieve multiple records from a table with optional query parameters.

**Parameters:**
  - `sort` (array, optional): Array of sort objects to apply to records. Each object specifies a field and direction. Records are sorted by the first sort object, then by the second, and so on. IMPORTANT: Cannot sort by system metadata fields like 'createdTime' or 'lastModifiedTime' - these are metadata properties returned with records but not sortable fields. To sort by creation/modification time, you must add a 'Created time' or 'Last modified time' field to your table (using those field types in Airtable).
  - `view` (string, optional): The name or ID of a view to use for filtering and sorting records. Note: this parameter is overridden if sort parameter is provided.
  - `baseId` (string, required): The ID of the Airtable base. Must start with 'app' followed by 14 characters.
  - `fields` (array, optional): Array of field names or IDs to include in the response. If not specified or if ['*'] is passed, all fields are returned. Field names are case-sensitive and must match exactly as they appear in Airtable. IMPORTANT: System fields 'id' and 'createdTime' are always returned automatically as part of the record structure - do NOT include them in this parameter. Only specify user-defined table fields here. UNKNOWN_FIELD_NAME errors indicate: (1) a typo or case mismatch, (2) the field was renamed/deleted, or (3) you included a system field name. RECOMMENDED: Use field IDs (format: 'fldXXXXXXXXXXXXXX') for reliable operation. Use AIRTABLE_GET_BASE_SCHEMA to retrieve valid field names and IDs.
  - `offset` (string, optional): Opaque pagination cursor token returned in the 'offset' field of a previous list records response. This value must be used exactly as returned by Airtable - do not construct, modify, or decode it. Only pass an offset if you received it from a previous response's 'offset' field. Omit this parameter for the first page of results.
  - `pageSize` (integer, optional): Number of records to return per page. Must be between 1 and 100. Defaults to 100.
  - `timeZone` (string, optional): The time zone to use for formatting date and time fields. Required when cellFormat is 'string'. If not provided when cellFormat='string', defaults to 'utc'. Examples: 'utc', 'America/New_York', 'Europe/London'.
  - `cellFormat` (string, optional): Format for cell values. 'json' returns structured JSON values (default), 'string' returns simple string representations. When set to 'string', timeZone and userLocale are required - if not provided, defaults will be auto-populated (timeZone='utc', userLocale='en-US').
  - `maxRecords` (integer, optional): Maximum total number of records to return across all pages. If not specified, all records matching the query will be returned.
  - `userLocale` (string, optional): The user locale to use for formatting cell values (e.g., 'en-US', 'fr-FR'). Required when cellFormat is 'string'. If not provided when cellFormat='string', defaults to 'en-US'.
  - `tableIdOrName` (string, required): The ID or name of the table within the base. Table IDs start with 'tbl' followed by 14 characters. Table names are case-sensitive and must match exactly as they appear in Airtable. Can also be passed as 'tableId' or 'tableName'.
  - `recordMetadata` (array, optional): Array of strings specifying additional metadata to include. Currently supports 'commentCount' to include comment counts on records.
  - `filterByFormula` (string, optional): Airtable formula to filter records. SYNTAX REQUIREMENTS: (1) BALANCED PARENTHESES: Every '(' must have a matching ')'. (2) Field names in curly braces: {FieldName} - case-sensitive, must match exactly as they appear in the table. (3) String values in quotes: 'value' or "value". (4) Function calls: AND(), OR(), NOT(), FIND(), BLANK(), TODAY(), etc. Zero-argument functions like BLANK() and TODAY() are valid. (5) Escape single quotes in strings by doubling: 'John''s'. IMPORTANT - FIELD TYPES VS FORMULA FUNCTIONS: ROLLUP, LOOKUP, FORMULA, and similar are Airtable FIELD TYPES, NOT formula functions. You cannot call them in filterByFormula (e.g., ROLLUP({field}) is INVALID). Instead, reference the field directly by name: {RollupFieldName}, {LookupFieldName}. The values from Rollup/Lookup/Formula fields are pre-computed and accessed like any other field. LINKED RECORD FILTERING: Linked record fields referenced directly (e.g., {LinkedFieldName}) return PRIMARY FIELD VALUES as text, NOT record IDs. To filter by specific record IDs: (1) Create a lookup field in your table that looks up RECORD_ID() from the linked table, (2) Then use: FIND('recXXXXXXXXXXXXXX', ARRAYJOIN({RecordIDLookupField})). Alternatively, filter by the primary field values directly if that meets your needs. COMMON ERRORS: - Unknown field names: Field names are CASE-SENSITIVE. '{status}' is NOT the same as '{Status}'. Use AIRTABLE_GET_BASE_SCHEMA to verify exact field names. - Unknown function: Using field type names (ROLLUP, LOOKUP) as functions. These are NOT functions - reference the field directly. - Missing closing parenthesis: AND({A}='x', OR({B}='y', {C}='z') - missing final ')'. - Unquoted strings: {Status}=Active - must be {Status}='Active'. RECOMMENDED: Use AIRTABLE_GET_BASE_SCHEMA to verify exact field names before using them in formulas.
  - `returnFieldsByFieldId` (boolean, optional): If true, field keys in the response will be field IDs instead of field names. Recommended when working with field names that contain special characters or when building integrations that need stability across field renames.

### AIRTABLE_UPDATE_COMMENT

**MCP Tool**: `mcp__composio__AIRTABLE_UPDATE_COMMENT`

Tool to update an existing comment on a specific Airtable record. Use when modifying comment text or updating user mentions using @[userId] syntax. API users can only update comments they have created.

**Parameters:**
  - `text` (string, required): The updated comment text. To mention a user, use the format @[userId] (e.g., @[usrGISFlfA7l5F7kY6]).
  - `baseId` (string, required): The unique identifier of the Airtable base. This typically starts with 'app'.
  - `recordId` (string, required): The unique identifier of the record containing the comment. This typically starts with 'rec'.
  - `rowCommentId` (string, required): The unique identifier of the comment to update. This typically starts with 'com'.
  - `tableIdOrName` (string, required): The unique identifier (typically starting with 'tbl') or the name of the table within the base.

### AIRTABLE_UPDATE_FIELD

**MCP Tool**: `mcp__composio__AIRTABLE_UPDATE_FIELD`

Updates a field's name or description in an Airtable table. Use this action to modify field metadata without changing the field's type or options. At least one of 'name' or 'description' must be provided.

**Parameters:**
  - `name` (string, optional): The new name for the field. At least one of 'name' or 'description' must be provided.
  - `baseId` (string, required): The unique identifier of the Airtable base containing the field.
  - `fieldId` (string, required): The ID of the field to update. IMPORTANT: Unlike data API endpoints, the Metadata API requires field IDs - field names are not supported. Field IDs start with 'fld' followed by 14 alphanumeric characters. Use AIRTABLE_GET_BASE_SCHEMA to find field IDs.
  - `tableId` (string, required): The ID of the table containing the field to update. IMPORTANT: Unlike data API endpoints, the Metadata API requires table IDs - table names are not supported. Table IDs start with 'tbl' followed by 14 alphanumeric characters. Use AIRTABLE_GET_BASE_SCHEMA to find table IDs.
  - `description` (string, optional): The new description for the field. Must not exceed 20,000 characters. At least one of 'name' or 'description' must be provided.

### AIRTABLE_UPDATE_MULTIPLE_RECORDS

**MCP Tool**: `mcp__composio__AIRTABLE_UPDATE_MULTIPLE_RECORDS`

Tool to update up to 10 records in an Airtable table with selective field modifications. Use when you need to modify multiple existing records or perform upsert operations. Updates are not performed atomically.

**Parameters:**
  - `baseId` (string, required): The ID of the Airtable base containing the table.
  - `records` (array, required): List of records to be updated. Airtable's API limits updates to a maximum of 10 records per request. If you need to update more records, split them into multiple requests of 10 or fewer records each.
  - `typecast` (boolean, optional): If true, Airtable attempts automatic data conversion from string values on a best-effort basis (e.g., '123' to 123).
  - `performUpsert` (object, optional): Configuration for upsert behavior when updating records.
  - `tableIdOrName` (string, required): The ID or name of the table within the base where records will be updated.
  - `returnFieldsByFieldId` (boolean, optional): If true, returns field values keyed by field ID instead of field name. Useful for integrations resilient to field name changes.


## Usage Examples

#### AIRTABLE_CREATE_BASE
```
Use MCP tool: mcp__composio__AIRTABLE_CREATE_BASE
Parameters: {
  "name": "your_name_here",
  "workspaceId": "your_workspaceId_here"
}
```

#### AIRTABLE_CREATE_COMMENT
```
Use MCP tool: mcp__composio__AIRTABLE_CREATE_COMMENT
Parameters: {
  "baseId": "your_baseId_here",
  "tableIdOrName": "your_tableIdOrName_here"
}
```

#### AIRTABLE_CREATE_FIELD
```
Use MCP tool: mcp__composio__AIRTABLE_CREATE_FIELD
Parameters: {
  "baseId": "your_baseId_here",
  "tableIdOrName": "your_tableIdOrName_here"
}
```

## Quick Reference

| MCP Tool | Description |
|----------|-------------|
| `mcp__composio__AIRTABLE_CREATE_BASE` | Creates a new Airtable base with specified tables ... |
| `mcp__composio__AIRTABLE_CREATE_COMMENT` | Tool to create a comment on a specific Airtable re... |
| `mcp__composio__AIRTABLE_CREATE_FIELD` | Creates a new field within a specified table in an... |
| `mcp__composio__AIRTABLE_CREATE_MULTIPLE_RECORDS` | Creates multiple new records in a specified Airtab... |
| `mcp__composio__AIRTABLE_CREATE_RECORD` | Creates a new record in a specified Airtable table... |
| `mcp__composio__AIRTABLE_CREATE_RECORD_FROM_NATURAL_LANGUAGE` | Creates a new record in an Airtable table from a n... |
| `mcp__composio__AIRTABLE_CREATE_RECORDS` | Tool to create multiple records (up to 10) in a sp... |
| `mcp__composio__AIRTABLE_CREATE_TABLE` | Creates a new table within a specified existing Ai... |
| `mcp__composio__AIRTABLE_DELETE_COMMENT` | Tool to delete a comment from a record in an Airta... |
| `mcp__composio__AIRTABLE_DELETE_MULTIPLE_RECORDS` | Tool to delete up to 10 specified records from a t... |
| `mcp__composio__AIRTABLE_DELETE_RECORD` | Permanently deletes a specific record from an exis... |
| `mcp__composio__AIRTABLE_GET_BASE_SCHEMA` | Retrieves the detailed schema for a specified Airt... |
| `mcp__composio__AIRTABLE_GET_RECORD` | Retrieves a specific record from an Airtable table... |
| `mcp__composio__AIRTABLE_GET_USER_INFO` | Retrieves information, such as ID and permission s... |
| `mcp__composio__AIRTABLE_LIST_BASES` | Retrieves all Airtable bases accessible to the aut... |
| `mcp__composio__AIRTABLE_LIST_COMMENTS` | Tool to list comments on a specific Airtable recor... |
| `mcp__composio__AIRTABLE_LIST_RECORDS` | Tool to list records from an Airtable table with f... |
| `mcp__composio__AIRTABLE_UPDATE_COMMENT` | Tool to update an existing comment on a specific A... |
| `mcp__composio__AIRTABLE_UPDATE_FIELD` | Updates a field's name or description in an Airtab... |
| `mcp__composio__AIRTABLE_UPDATE_MULTIPLE_RECORDS` | Tool to update up to 10 records in an Airtable tab... |

## When to Use This Integration

**Use Airtable when:**
- Interacting with Airtable services
- Automating Airtable workflows
- Reading and writing Airtable data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:
1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
