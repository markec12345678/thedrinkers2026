---
name: supabase
description: Supabase is an open-source backend-as-a-service providing a Postgres database, authentication, storage, and real-time subscription APIs for building modern applications integration via Composio MCP. Use when user needs to interact with Supabase.
---

# Supabase Integration

> Auto-generated by Composio Skills Extension on 2026-03-09T21:02:00.990Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Supabase is an open-source backend-as-a-service providing a Postgres database, authentication, storage, and real-time subscription APIs for building modern applications

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### SUPABASE_ALPHA_CREATES_A_NEW_API_KEY_FOR_THE_PROJECT

**MCP Tool**: `mcp__composio__SUPABASE_ALPHA_CREATES_A_NEW_API_KEY_FOR_THE_PROJECT`

Creates a 'publishable' or 'secret' API key for an existing Supabase project, optionally with a description; 'secret' keys can have customized JWT templates.

**Parameters:**
  - `ref` (string, required): The unique reference ID of the Supabase project.
  - `name` (string, required): Name for the API key. Must start with a lowercase letter or underscore, followed only by lowercase alphanumeric characters or underscores. Example: 'my_api_key_01'.
  - `type` (string, required): Specifies the type of API key: 'publishable' for client-side use or 'secret' for server-side operations.
  - `description` (string, optional): Optional human-readable description for the API key.

### SUPABASE_ALPHA_DELETES_AN_API_KEY_FOR_THE_PROJECT

**MCP Tool**: `mcp__composio__SUPABASE_ALPHA_DELETES_AN_API_KEY_FOR_THE_PROJECT`

Permanently deletes a specific API key (identified by `id`) from a Supabase project (identified by `ref`), revoking its access.

**Parameters:**
  - `id` (string, required): The unique identifier of the API key that needs to be deleted from the project.
  - `ref` (string, required): The unique reference ID of the Supabase project from which the API key is to be deleted.

### SUPABASE_ALPHA_GET_A_THIRD_PARTY_INTEGRATION

**MCP Tool**: `mcp__composio__SUPABASE_ALPHA_GET_A_THIRD_PARTY_INTEGRATION`

Retrieves the detailed configuration for a specific third-party authentication (TPA) provider, identified by `tpa_id`, within an existing Supabase project specified by `ref`.

**Parameters:**
  - `ref` (string, required): The unique identifier (reference) of the Supabase project.
  - `tpa_id` (string, required): The unique identifier of the specific third-party authentication provider configuration to retrieve.

### SUPABASE_ALPHA_LISTS_ALL_THIRD_PARTY_AUTH_INTEGRATIONS

**MCP Tool**: `mcp__composio__SUPABASE_ALPHA_LISTS_ALL_THIRD_PARTY_AUTH_INTEGRATIONS`

Lists all configured third-party authentication provider integrations for an existing Supabase project (using its `ref`), suitable for read-only auditing or verifying current authentication settings.

**Parameters:**
  - `ref` (string, required): The unique identifier of the Supabase project.

### SUPABASE_ALPHA_REMOVES_A_THIRD_PARTY_AUTH_INTEGRATION

**MCP Tool**: `mcp__composio__SUPABASE_ALPHA_REMOVES_A_THIRD_PARTY_AUTH_INTEGRATION`

Removes a third-party authentication provider (e.g., Google, GitHub) from a Supabase project's configuration; this immediately prevents users from logging in via that method.

**Parameters:**
  - `ref` (string, required): The unique reference ID of the Supabase project.
  - `tpa_id` (string, required): The unique identifier (ID) of the third-party authentication provider configuration to be removed.

### SUPABASE_ALPHA_UPDATES_AN_API_KEY_FOR_THE_PROJECT

**MCP Tool**: `mcp__composio__SUPABASE_ALPHA_UPDATES_AN_API_KEY_FOR_THE_PROJECT`

Updates an existing Supabase project API key's `description` and/or `secret_jwt_template` (which defines its `role`); does not regenerate the key string.

**Parameters:**
  - `id` (string, required): The unique identifier of the API key that needs to be updated.
  - `ref` (string, required): The unique reference ID of the Supabase project to which the API key belongs.
  - `description` (string, optional): Optional new description for the API key.

### SUPABASE_APPLY_A_MIGRATION

**MCP Tool**: `mcp__composio__SUPABASE_APPLY_A_MIGRATION`

Tool to apply database migrations to a Supabase project. Use when you need to execute SQL schema changes, create tables, alter columns, or run other DDL/DML operations as part of a tracked migration. This is a Beta feature in the Supabase Management API.

**Parameters:**
  - `ref` (string, required): The unique reference ID of the Supabase project (e.g., 'pamqaklsszmjzugtywow'). Found in Project Settings > General > Reference ID.
  - `name` (string, optional): A unique name for the migration to track it in the migration history. If not provided, a default name will be assigned.
  - `query` (string, required): The SQL migration query to execute against the project's database. Must be at least 1 character long.
  - `rollback` (string, optional): Optional SQL query to rollback/undo this migration if needed.
  - `idempotency_key` (string, optional): A unique key to ensure the same migration is tracked only once. If provided, prevents duplicate migrations with the same key from being applied.

### SUPABASE_BETA_ACTIVATES_A_CUSTOM_HOSTNAME_FOR_A_PROJECT

**MCP Tool**: `mcp__composio__SUPABASE_BETA_ACTIVATES_A_CUSTOM_HOSTNAME_FOR_A_PROJECT`

Activates a previously configured custom hostname for a Supabase project, assuming DNS settings are verified externally.

**Parameters:**
  - `ref` (string, required): The unique identifier (project ID) of the Supabase project.

### SUPABASE_BETA_ACTIVATES_A_VANITY_SUBDOMAIN_FOR_A_PROJECT

**MCP Tool**: `mcp__composio__SUPABASE_BETA_ACTIVATES_A_VANITY_SUBDOMAIN_FOR_A_PROJECT`

Activates a vanity subdomain for the specified Supabase project (e.g., 'my-brand.supabase.co'). Important notes: - Vanity subdomains require a paid plan (Pro/Team/Enterprise) - Usage of vanity subdomains and custom domains is mutually exclusive - After activation, your project's auth services will no longer work on the original {project-ref}.supabase.co hostname - Schedule a downtime window to update client code and OAuth providers before activating

**Parameters:**
  - `ref` (string, required): Unique reference ID of the Supabase project.
  - `vanity_subdomain` (string, required): Vanity subdomain to activate (e.g., 'my-example-brand'). Must be 1-63 alphanumeric characters or hyphens; cannot start or end with a hyphen.

### SUPABASE_BETA_AUTHORIZE_USER_THROUGH_OAUTH

**MCP Tool**: `mcp__composio__SUPABASE_BETA_AUTHORIZE_USER_THROUGH_OAUTH`

Generates a Supabase OAuth 2.0 authorization URL for user redirection. IMPORTANT: This action requires a pre-registered OAuth client_id and a redirect_uri that matches one of the pre-registered URIs for that OAuth application. Without a valid registered OAuth application, this endpoint will return a 400 error. To use this action: 1. Register an OAuth application in the Supabase dashboard 2. Use the client_id from the registered application 3. Ensure redirect_uri matches one of the registered callback URLs

**Parameters:**
  - `scope` (string, optional): Space-separated list of OAuth scopes for requested permissions on user resources (e.g., 'email profile', 'storage.objects.read').
  - `state` (string, optional): Opaque value to maintain state between request and callback; returned unmodified to aid CSRF prevention.
  - `client_id` (string, required): Unique identifier for the OAuth client application, registered with Supabase. Must be a valid client_id from a registered OAuth application.
  - `redirect_uri` (string, required): URI where Supabase redirects the user after authorization; must exactly match a registered URI for the client application.
  - `response_mode` (string, optional): Specifies how authorization response parameters are returned (e.g., 'query', 'fragment', 'form_post'). Default is determined by `response_type` if not specified.
  - `response_type` (string, required): Specifies the authorization flow type: 'code' for Authorization Code Grant; 'token' for Implicit Grant (access token); 'id_token token' for OIDC hybrid flows (ID and access tokens).
  - `code_challenge` (string, optional): PKCE code challenge to secure authorization code grants, typically a BASE64URL-encoded SHA256 hash of `code_verifier`.
  - `code_challenge_method` (string, optional): Method to derive `code_challenge` for PKCE: 'S256'/'sha256' for SHA256 hashing; 'plain' sends verifier as is (not for production if hashing possible). Required if `code_challenge` is provided.

### SUPABASE_BETA_CHECKS_VANITY_SUBDOMAIN_AVAILABILITY

**MCP Tool**: `mcp__composio__SUPABASE_BETA_CHECKS_VANITY_SUBDOMAIN_AVAILABILITY`

Checks if a specific vanity subdomain is available for a Supabase project; this action does not reserve or assign the subdomain.

**Parameters:**
  - `ref` (string, required): The unique reference ID of the Supabase project.
  - `vanity_subdomain` (string, required): The desired vanity subdomain to check for availability (e.g., 'my-app'). Must be 1-63 alphanumeric characters or hyphens; cannot start or end with a hyphen.

### SUPABASE_BETA_ENABLES_DATABASE_WEBHOOKS_ON_THE_PROJECT

**MCP Tool**: `mcp__composio__SUPABASE_BETA_ENABLES_DATABASE_WEBHOOKS_ON_THE_PROJECT`

Enables database webhooks for the Supabase project `ref`, triggering real-time notifications for INSERT, UPDATE, or DELETE events.

**Parameters:**
  - `ref` (string, required): The unique identifier for the Supabase project.

### SUPABASE_BETA_GET_PROJECT_S_SSL_ENFORCEMENT_CONFIGURATION

**MCP Tool**: `mcp__composio__SUPABASE_BETA_GET_PROJECT_S_SSL_ENFORCEMENT_CONFIGURATION`

Retrieves the SSL enforcement configuration for a specified Supabase project, indicating if SSL connections are mandated for its database.

**Parameters:**
  - `ref` (string, required): The unique identifier of the Supabase project. This can typically be found in your project's dashboard URL (e.g., `https://supabase.com/dashboard/project/<project-ref>`).

### SUPABASE_BETA_GETS_CURRENT_VANITY_SUBDOMAIN_CONFIG

**MCP Tool**: `mcp__composio__SUPABASE_BETA_GETS_CURRENT_VANITY_SUBDOMAIN_CONFIG`

Fetches the current vanity subdomain configuration, including its status and custom domain name, for a Supabase project identified by its reference ID.

**Parameters:**
  - `ref` (string, required): The unique reference ID of the Supabase project for which to fetch the vanity subdomain configuration.

### SUPABASE_BETA_GETS_PROJECT_S_CUSTOM_HOSTNAME_CONFIG

**MCP Tool**: `mcp__composio__SUPABASE_BETA_GETS_PROJECT_S_CUSTOM_HOSTNAME_CONFIG`

Retrieves a Supabase project's custom hostname configuration, including its status, SSL certificate, and ownership verification, noting that availability may depend on the project's plan.

**Parameters:**
  - `ref` (string, required): The unique reference ID of the Supabase project. This ID can typically be found in your project's dashboard URL.

### SUPABASE_BETA_GETS_PROJECT_S_NETWORK_BANS

**MCP Tool**: `mcp__composio__SUPABASE_BETA_GETS_PROJECT_S_NETWORK_BANS`

Retrieves the list of banned IPv4 addresses for a Supabase project using its unique project reference string; this is a read-only operation.

**Parameters:**
  - `ref` (string, required): The unique project reference identifier for the Supabase project. Must be a valid 20-character project ref. You can retrieve valid project refs using the 'List all projects' action.

### SUPABASE_BETA_GETS_PROJECT_S_NETWORK_RESTRICTIONS

**MCP Tool**: `mcp__composio__SUPABASE_BETA_GETS_PROJECT_S_NETWORK_RESTRICTIONS`

Retrieves the network restriction settings (IP allowlists) for a Supabase project. Use this action to: - Check which IPv4/IPv6 CIDR blocks are allowed to connect to the project's database - Verify if network restrictions are enabled (entitlement: "allowed") or disabled ("disallowed") - Audit current network security configuration - Check if network restrictions have been modified (old_config present) Note: Default values 0.0.0.0/0 (IPv4) and ::/0 (IPv6) mean all IPs are allowed. Network restrictions require a Pro, Team, or Enterprise plan.

**Parameters:**
  - `ref` (string, required): The unique project reference ID (also called project ID or ref). This is a 20-character alphanumeric string that identifies the Supabase project. Can be found in the project URL or retrieved via SUPABASE_LIST_ALL_PROJECTS.

### SUPABASE_BETA_GETS_PROJECT_S_PGSODIUM_CONFIG

**MCP Tool**: `mcp__composio__SUPABASE_BETA_GETS_PROJECT_S_PGSODIUM_CONFIG`

Retrieves the PGSodium configuration, including the root encryption key, for an existing Supabase project identified by its `ref`.

**Parameters:**
  - `ref` (string, required): The unique identifier of the Supabase project.

### SUPABASE_BETA_REMOVE_A_READ_REPLICA

**MCP Tool**: `mcp__composio__SUPABASE_BETA_REMOVE_A_READ_REPLICA`

Remove a read replica from a Supabase project (Pro plan or higher required). This beta endpoint initiates the removal of a specified read replica database. The operation is irreversible. Before removal, ensure all application traffic is redirected from the replica to the primary database. Requirements: - Project must be on Pro plan or higher - Bearer token with infra_read_replicas_write permission (FGA) - Valid read replica database identifier Note: Returns 201 on success with an empty response body.

**Parameters:**
  - `ref` (string, required): The unique project reference ID (20 lowercase characters, e.g. 'abcdefghijklmnopqrst'). Found in project settings or URL.
  - `database_identifier` (string, required): The unique identifier of the read replica database to remove. Obtain from the project's database settings or read replica listing.

### SUPABASE_BETA_REMOVE_NETWORK_BANS

**MCP Tool**: `mcp__composio__SUPABASE_BETA_REMOVE_NETWORK_BANS`

Removes specified IPv4 addresses from a Supabase project's network ban list, granting immediate access; IPs not currently banned are ignored.

**Parameters:**
  - `ref` (string, required): The unique reference ID of the Supabase project.
  - `ipv4_addresses` (array, required): A list of IPv4 addresses to be removed from the project's network ban list.


## Usage Examples

#### SUPABASE_ALPHA_CREATES_A_NEW_API_KEY_FOR_THE_PROJECT
```
Use MCP tool: mcp__composio__SUPABASE_ALPHA_CREATES_A_NEW_API_KEY_FOR_THE_PROJECT
Parameters: {
  "ref": "your_ref_here",
  "name": "your_name_here"
}
```

#### SUPABASE_ALPHA_DELETES_AN_API_KEY_FOR_THE_PROJECT
```
Use MCP tool: mcp__composio__SUPABASE_ALPHA_DELETES_AN_API_KEY_FOR_THE_PROJECT
Parameters: {
  "ref": "your_ref_here",
  "id": "your_id_here"
}
```

#### SUPABASE_ALPHA_GET_A_THIRD_PARTY_INTEGRATION
```
Use MCP tool: mcp__composio__SUPABASE_ALPHA_GET_A_THIRD_PARTY_INTEGRATION
Parameters: {
  "ref": "your_ref_here",
  "tpa_id": "your_tpa_id_here"
}
```

## Quick Reference

| MCP Tool | Description |
|----------|-------------|
| `mcp__composio__SUPABASE_ALPHA_CREATES_A_NEW_API_KEY_FOR_THE_PROJECT` | Creates a 'publishable' or 'secret' API key for an... |
| `mcp__composio__SUPABASE_ALPHA_DELETES_AN_API_KEY_FOR_THE_PROJECT` | Permanently deletes a specific API key (identified... |
| `mcp__composio__SUPABASE_ALPHA_GET_A_THIRD_PARTY_INTEGRATION` | Retrieves the detailed configuration for a specifi... |
| `mcp__composio__SUPABASE_ALPHA_LISTS_ALL_THIRD_PARTY_AUTH_INTEGRATIONS` | Lists all configured third-party authentication pr... |
| `mcp__composio__SUPABASE_ALPHA_REMOVES_A_THIRD_PARTY_AUTH_INTEGRATION` | Removes a third-party authentication provider (e.g... |
| `mcp__composio__SUPABASE_ALPHA_UPDATES_AN_API_KEY_FOR_THE_PROJECT` | Updates an existing Supabase project API key's `de... |
| `mcp__composio__SUPABASE_APPLY_A_MIGRATION` | Tool to apply database migrations to a Supabase pr... |
| `mcp__composio__SUPABASE_BETA_ACTIVATES_A_CUSTOM_HOSTNAME_FOR_A_PROJECT` | Activates a previously configured custom hostname ... |
| `mcp__composio__SUPABASE_BETA_ACTIVATES_A_VANITY_SUBDOMAIN_FOR_A_PROJECT` | Activates a vanity subdomain for the specified Sup... |
| `mcp__composio__SUPABASE_BETA_AUTHORIZE_USER_THROUGH_OAUTH` | Generates a Supabase OAuth 2.0 authorization URL f... |
| `mcp__composio__SUPABASE_BETA_CHECKS_VANITY_SUBDOMAIN_AVAILABILITY` | Checks if a specific vanity subdomain is available... |
| `mcp__composio__SUPABASE_BETA_ENABLES_DATABASE_WEBHOOKS_ON_THE_PROJECT` | Enables database webhooks for the Supabase project... |
| `mcp__composio__SUPABASE_BETA_GET_PROJECT_S_SSL_ENFORCEMENT_CONFIGURATION` | Retrieves the SSL enforcement configuration for a ... |
| `mcp__composio__SUPABASE_BETA_GETS_CURRENT_VANITY_SUBDOMAIN_CONFIG` | Fetches the current vanity subdomain configuration... |
| `mcp__composio__SUPABASE_BETA_GETS_PROJECT_S_CUSTOM_HOSTNAME_CONFIG` | Retrieves a Supabase project's custom hostname con... |
| `mcp__composio__SUPABASE_BETA_GETS_PROJECT_S_NETWORK_BANS` | Retrieves the list of banned IPv4 addresses for a ... |
| `mcp__composio__SUPABASE_BETA_GETS_PROJECT_S_NETWORK_RESTRICTIONS` | Retrieves the network restriction settings (IP all... |
| `mcp__composio__SUPABASE_BETA_GETS_PROJECT_S_PGSODIUM_CONFIG` | Retrieves the PGSodium configuration, including th... |
| `mcp__composio__SUPABASE_BETA_REMOVE_A_READ_REPLICA` | Remove a read replica from a Supabase project (Pro... |
| `mcp__composio__SUPABASE_BETA_REMOVE_NETWORK_BANS` | Removes specified IPv4 addresses from a Supabase p... |

## When to Use This Integration

**Use Supabase when:**
- Interacting with Supabase services
- Automating Supabase workflows
- Reading and writing Supabase data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:
1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
