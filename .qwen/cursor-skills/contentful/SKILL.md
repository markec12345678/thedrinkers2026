---
name: contentful
description: contentful integration via Composio MCP. Use when user needs to interact with contentful.
---

# contentful Integration

> Auto-generated by Composio Skills Extension on 2026-03-09T19:28:56.883Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Integration with contentful services.

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### CONTENTFUL_ACTIVATE_CONTENT_TYPE

**MCP Tool**: `mcp__composio__CONTENTFUL_ACTIVATE_CONTENT_TYPE`

Activates (publishes) a content type in Contentful. Use this to make a content type available for creating entries. Requires the current version number for optimistic locking.

**Parameters:**
  - `version` (integer, required): Current version number of the content type (required for optimistic locking). This is sent via the X-Contentful-Version header.
  - `space_id` (string, required): ID of the space containing the content type
  - `environment_id` (string, required): ID of the environment containing the content type (typically 'master')
  - `content_type_id` (string, required): ID of the content type to activate (publish)

### CONTENTFUL_ARCHIVE_ASSET

**MCP Tool**: `mcp__composio__CONTENTFUL_ARCHIVE_ASSET`

Tool to archive an asset in Contentful. Use when you need to archive an asset to remove it from active use without deleting it permanently.

**Parameters:**
  - `asset_id` (string, required): ID of the asset to archive
  - `space_id` (string, required): ID of the space containing the asset
  - `environment_id` (string, required): ID of the environment containing the asset (typically 'master')
  - `X_Contentful_Version` (integer, optional): Version number of the asset to archive. If not provided, Contentful will use the latest version.

### CONTENTFUL_ARCHIVE_ENTRY

**MCP Tool**: `mcp__composio__CONTENTFUL_ARCHIVE_ENTRY`

Tool to archive an entry in Contentful. Use when you need to move an entry to archived status. The entry must not be currently published, and you may need to provide the X-Contentful-Version header matching the entry's current version.

**Parameters:**
  - `entry_id` (string, required): ID of the entry to archive
  - `space_id` (string, required): ID of the space containing the entry
  - `environment_id` (string, required): ID of the environment containing the entry (typically 'master')
  - `X-Contentful-Version` (integer, optional): Version of the entry to archive. Required for version control. Use the current version number of the entry.

### CONTENTFUL_ARCHIVE_RELEASE

**MCP Tool**: `mcp__composio__CONTENTFUL_ARCHIVE_RELEASE`

Tool to archive a release in Contentful. Use when you need to lock a release to prevent modifications. An archived release cannot be edited until unarchived. Requires the current version number for optimistic locking.

**Parameters:**
  - `version` (integer, required): Current version of the release for optimistic locking (X-Contentful-Version header)
  - `space_id` (string, required): The space identifier
  - `release_id` (string, required): The release identifier to archive
  - `environment_id` (string, required): The environment identifier (typically 'master')

### CONTENTFUL_CREATE_APP_DEFINITIONS_KEYS

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_APP_DEFINITIONS_KEYS`

Tool to generate an app signing key for a Contentful app definition. Use when you need to create cryptographic keys for app authentication. Either provide generate=true to let Contentful create a key pair (returns private key), or provide a pre-generated public key in JWK format.

**Parameters:**
  - `jwk` (object, optional): JSON Web Key object containing a pre-generated public key to upload. Either this field or generate must be provided.
  - `generate` (boolean, optional): Set to true to let Contentful generate the key pair. If true, the response will include the private key. Either this field or jwk must be provided.
  - `organization_id` (string, required): ID of the organization containing the app definition
  - `app_definition_id` (string, required): ID of the app definition to create a key for

### CONTENTFUL_CREATE_CONTENT_TYPE

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_CONTENT_TYPE`

Tool to create a new content type in a Contentful environment. Use when you need to define a new content structure with custom fields. Content types define the schema for entries.

**Parameters:**
  - `name` (string, required): Name of the content type
  - `fields` (array, required): Array of field definitions for the content type. A content type can have up to 50 fields
  - `space_id` (string, required): ID of the space where the content type will be created
  - `description` (string, optional): Description of the content type explaining its purpose
  - `displayField` (string, optional): ID of the field to use as the display field (entry title) for this content type. Must match the 'id' of one of the fields in the fields array
  - `environment_id` (string, required): ID of the environment where the content type will be created (typically 'master')

### CONTENTFUL_CREATE_ENTRY_TASK

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_ENTRY_TASK`

Tool to create a task on a Contentful entry. Use when you need to create workflow tasks for collaboration on entries. Tasks can be assigned to users and are used for managing content review and approval workflows.

**Parameters:**
  - `body` (string, required): Description or body content of the task
  - `status` (string, required): Status of the task, typically 'active' for new tasks
  - `entry_id` (string, required): ID of the entry to create the task for
  - `space_id` (string, required): ID of the space containing the entry
  - `assignedTo` (object, required): User to assign the task to, with sys.id, sys.linkType='User', and sys.type='Link'
  - `environment_id` (string, required): ID of the environment containing the entry (typically 'master')

### CONTENTFUL_CREATE_ENVIRONMENT

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_ENVIRONMENT`

Tool to create a new environment within a Contentful space. Use when you need to set up a new environment for content management, such as staging or production. Optionally clone from an existing source environment.

**Parameters:**
  - `name` (string, required): Name of the new environment
  - `space_id` (string, required): ID of the space where the environment will be created
  - `source_environment_id` (string, optional): ID of the source environment to copy content from. If provided, the new environment will be cloned from this environment. If not provided, an empty environment will be created.

### CONTENTFUL_CREATE_ORGANIZATIONS_APP_DEF_ACCESS_GRANTS

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_APP_DEF_ACCESS_GRANTS`

Creates an access grant for an app definition to allow organizations to install the app. Use when you need to grant installation permissions for an app to specific organizations or all organizations (marketplace apps).

**Parameters:**
  - `grantee_id` (string, required): ID of the organization to grant access to, or 'all' to allow all organizations to install the app
  - `grantee_type` (string, required): Type of grantee. Must be 'Organization'
  - `organization_id` (string, required): The ID of the organization that owns the app definition
  - `app_definition_id` (string, required): The ID of the app definition to grant access to

### CONTENTFUL_CREATE_ORGANIZATIONS_APP_DEFINITIONS

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_APP_DEFINITIONS`

Tool to create a new app definition in a Contentful organization. Use when you need to register a new app that can be installed into space environments.

**Parameters:**
  - `src` (string, optional): URL where the root HTML document of the app can be found
  - `name` (string, required): Name of the app definition
  - `locations` (array, optional): Locations where the app can be installed and rendered within Contentful
  - `parameters` (object, optional): Model for app parameters configuration.
  - `organization_id` (string, required): The ID of the organization where the app definition will be created

### CONTENTFUL_CREATE_ORGANIZATIONS_APP_DEFINITIONS_ACTIONS

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_APP_DEFINITIONS_ACTIONS`

Tool to create an action for a Contentful app definition. Use when you need to define actions that your app can perform, such as endpoint invocations or function calls.

**Parameters:**
  - `url` (string, required): HTTPS URL for the endpoint (required when type is endpoint). Must be a valid HTTPS URL.
  - `name` (string, required): Name of the action to create
  - `type` (string, required): Type of action - must be one of: endpoint (for HTTP endpoints), function (for Contentful functions), or function-invocation (for function invocations)
  - `category` (string, required): Category ID from available categories (e.g., Entries.v1.0, Notification.v1.0). Use get_organizations_app_actions_categories action to retrieve valid category IDs.
  - `organization_id` (string, required): ID of the organization containing the app definition
  - `app_definition_id` (string, required): ID of the app definition to create an action for

### CONTENTFUL_CREATE_ORGANIZATIONS_INVITATIONS

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_INVITATIONS`

Create an invitation to an organization in Contentful (alpha feature). Use this to invite new users to join an organization with a specified role.

**Parameters:**
  - `role` (string, optional): Enum for invitation role values.
  - `email` (string, required): The email address of the person being invited
  - `lastName` (string, optional): The invitee's last name
  - `firstName` (string, optional): The invitee's first name
  - `organization_id` (string, required): ID of the organization to create the invitation for

### CONTENTFUL_CREATE_ORGANIZATIONS_SECURITY_CONTACTS

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_SECURITY_CONTACTS`

Creates a security contact for a specific organization. Use when you need to add an email address to receive security notifications for the organization.

**Parameters:**
  - `email` (string, required): Email address for the security contact. This email will receive security-related notifications for the organization.
  - `organization_id` (string, required): The ID of the organization to create a security contact for

### CONTENTFUL_CREATE_ORGANIZATIONS_TAXONOMY_CONCEPTS

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_TAXONOMY_CONCEPTS`

Tool to create a new taxonomy concept in a Contentful organization. Use when you need to add a concept for classifying and organizing content within an organization's taxonomy.

**Parameters:**
  - `uri` (string, optional): Optional globally unique identifier (URI) for the concept
  - `altLabels` (object, optional): Alternative labels for the concept with locale as key and array of strings as value (e.g., {'en-US': ['Tech', 'IT']})
  - `notations` (array, optional): Formal designations like classification codes
  - `prefLabel` (object, required): The preferred label for the concept with locale as key (e.g., {'en-US': 'Technology'}). At least one locale is required.
  - `hiddenLabels` (object, optional): Hidden labels for searching with locale as key and array of strings as value (e.g., {'en-US': ['Hidden1']})
  - `organization_id` (string, required): The organization ID to create the taxonomy concept in

### CONTENTFUL_CREATE_ORGANIZATIONS_TAXONOMY_CONCEPT_SCHEMES

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_TAXONOMY_CONCEPT_SCHEMES`

Tool to create a new taxonomy concept scheme in an organization. Use when you need to establish a new taxonomy structure or classification system for organizing content.

**Parameters:**
  - `uri` (string, optional): Optional URI identifier for the concept scheme
  - `prefLabel` (object, required): Localized preferred label for the concept scheme. Each key is a locale code (e.g., 'en-US') and value is the label text (e.g., {'en-US': 'Product Categories'})
  - `definition` (object, optional): Localized definition/description of the concept scheme. Each key is a locale code (e.g., 'en-US') and value is the definition text (e.g., {'en-US': 'A classification scheme for products'})
  - `organization_id` (string, required): The ID of the organization to create the concept scheme in

### CONTENTFUL_CREATE_RELEASE

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_RELEASE`

Tool to create a new release in Contentful. Use when you need to group entries and assets into a release for coordinated publishing or tracking. Requires at least one entity to be specified.

**Parameters:**
  - `title` (string, required): Title of the release
  - `entities` (object, required): Collection of entities (entries/assets) to include in the release
  - `space_id` (string, required): ID of the space where the release will be created
  - `environment_id` (string, required): ID of the environment where the release will be created (typically 'master')

### CONTENTFUL_CREATE_SPACES_API_KEYS

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_SPACES_API_KEYS`

Tool to create a new Delivery API key for a Contentful space. Use when you need to generate new API credentials for accessing content delivery. The created key provides access to specified environments within the space.

**Parameters:**
  - `name` (string, required): Name for the new API key. This helps identify the key's purpose
  - `space_id` (string, required): ID of the space to create the API key in
  - `description` (string, optional): Optional description for the API key to explain its purpose or usage
  - `environments` (array, required): List of environments this API key will have access to. Each environment is specified as a link object with sys.id, sys.type, and sys.linkType

### CONTENTFUL_CREATE_SPACES_ENVIRONMENTS_ASSETS

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_SPACES_ENVIRONMENTS_ASSETS`

Tool to create a new asset in a Contentful space environment. Use when you need to upload files like images, videos, or documents. After creation, the asset must be processed before it's available, and you may need to publish it to make it publicly accessible.

**Parameters:**
  - `fields` (object, required): Asset fields containing file information and optional title/description with locale-specific values
  - `metadata` (object, optional): Model for asset metadata.
  - `space_id` (string, required): ID of the space where the asset will be created
  - `environment_id` (string, required): ID of the environment where the asset will be created (typically 'master')

### CONTENTFUL_CREATE_SPACES_ENVIRONMENTS_ENTRIES

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_SPACES_ENVIRONMENTS_ENTRIES`

Tool to create a new entry in a Contentful space environment. Use when you need to add new content entries. Requires specifying the content type ID via X-Contentful-Content-Type header, and providing fields organized by locale.

**Parameters:**
  - `fields` (object, required): Entry fields organized by locale. Structure: {fieldName: {locale: value}}. Example: {'title': {'en-US': 'Hello World'}}
  - `metadata` (object, optional): Entry metadata including tags. Example: {'tags': [{'sys': {'id': 'nyCampaign', 'linkType': 'Tag', 'type': 'Link'}}]}
  - `space_id` (string, required): ID of the space where the entry will be created
  - `environment_id` (string, required): ID of the environment where the entry will be created (typically 'master')
  - `X-Contentful-Content-Type` (string, required): Content type ID that defines the structure of the entry being created. Must be a valid content type ID from your space.

### CONTENTFUL_CREATE_SPACES_ENVIRONMENTS_ENTRIES_COMMENTS

**MCP Tool**: `mcp__composio__CONTENTFUL_CREATE_SPACES_ENVIRONMENTS_ENTRIES_COMMENTS`

Tool to create a comment on an entry in Contentful. Use when you need to add a comment to a specific entry. Returns the created comment with status 'active' by default.

**Parameters:**
  - `body` (string, required): The comment text content to create
  - `entry_id` (string, required): ID of the entry to comment on
  - `space_id` (string, required): ID of the space containing the entry
  - `environment_id` (string, required): ID of the environment containing the entry (typically 'master')


## Usage Examples

#### CONTENTFUL_ACTIVATE_CONTENT_TYPE
```
Use MCP tool: mcp__composio__CONTENTFUL_ACTIVATE_CONTENT_TYPE
Parameters: {
  "space_id": "your_space_id_here",
  "environment_id": "your_environment_id_here"
}
```

#### CONTENTFUL_ARCHIVE_ASSET
```
Use MCP tool: mcp__composio__CONTENTFUL_ARCHIVE_ASSET
Parameters: {
  "space_id": "your_space_id_here",
  "environment_id": "your_environment_id_here"
}
```

#### CONTENTFUL_ARCHIVE_ENTRY
```
Use MCP tool: mcp__composio__CONTENTFUL_ARCHIVE_ENTRY
Parameters: {
  "space_id": "your_space_id_here",
  "environment_id": "your_environment_id_here"
}
```

## Quick Reference

| MCP Tool | Description |
|----------|-------------|
| `mcp__composio__CONTENTFUL_ACTIVATE_CONTENT_TYPE` | Activates (publishes) a content type in Contentful... |
| `mcp__composio__CONTENTFUL_ARCHIVE_ASSET` | Tool to archive an asset in Contentful. Use when y... |
| `mcp__composio__CONTENTFUL_ARCHIVE_ENTRY` | Tool to archive an entry in Contentful. Use when y... |
| `mcp__composio__CONTENTFUL_ARCHIVE_RELEASE` | Tool to archive a release in Contentful. Use when ... |
| `mcp__composio__CONTENTFUL_CREATE_APP_DEFINITIONS_KEYS` | Tool to generate an app signing key for a Contentf... |
| `mcp__composio__CONTENTFUL_CREATE_CONTENT_TYPE` | Tool to create a new content type in a Contentful ... |
| `mcp__composio__CONTENTFUL_CREATE_ENTRY_TASK` | Tool to create a task on a Contentful entry. Use w... |
| `mcp__composio__CONTENTFUL_CREATE_ENVIRONMENT` | Tool to create a new environment within a Contentf... |
| `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_APP_DEF_ACCESS_GRANTS` | Creates an access grant for an app definition to a... |
| `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_APP_DEFINITIONS` | Tool to create a new app definition in a Contentfu... |
| `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_APP_DEFINITIONS_ACTIONS` | Tool to create an action for a Contentful app defi... |
| `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_INVITATIONS` | Create an invitation to an organization in Content... |
| `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_SECURITY_CONTACTS` | Creates a security contact for a specific organiza... |
| `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_TAXONOMY_CONCEPTS` | Tool to create a new taxonomy concept in a Content... |
| `mcp__composio__CONTENTFUL_CREATE_ORGANIZATIONS_TAXONOMY_CONCEPT_SCHEMES` | Tool to create a new taxonomy concept scheme in an... |
| `mcp__composio__CONTENTFUL_CREATE_RELEASE` | Tool to create a new release in Contentful. Use wh... |
| `mcp__composio__CONTENTFUL_CREATE_SPACES_API_KEYS` | Tool to create a new Delivery API key for a Conten... |
| `mcp__composio__CONTENTFUL_CREATE_SPACES_ENVIRONMENTS_ASSETS` | Tool to create a new asset in a Contentful space e... |
| `mcp__composio__CONTENTFUL_CREATE_SPACES_ENVIRONMENTS_ENTRIES` | Tool to create a new entry in a Contentful space e... |
| `mcp__composio__CONTENTFUL_CREATE_SPACES_ENVIRONMENTS_ENTRIES_COMMENTS` | Tool to create a comment on an entry in Contentful... |

## When to Use This Integration

**Use contentful when:**
- Interacting with contentful services
- Automating contentful workflows
- Reading and writing contentful data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:
1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
