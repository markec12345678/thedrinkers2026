---
name: facebook
description: facebook integration via Composio MCP. Use when user needs to interact with facebook.
---

# facebook Integration

> Auto-generated by Composio Skills Extension on 2026-03-09T19:28:55.450Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Integration with facebook services.

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### FACEBOOK_ADD_PHOTOS_TO_ALBUM

**MCP Tool**: `mcp__composio__FACEBOOK_ADD_PHOTOS_TO_ALBUM`

Adds photos to an existing Facebook album. IMPORTANT: This action requires the 'pages_manage_posts' permission. The legacy 'publish_actions' permission has been deprecated by Facebook. If you encounter permission errors, consider using the FACEBOOK_UPLOAD_PHOTO action instead, which uploads photos directly to the page's timeline (photos are automatically placed in the 'Photos' or 'Timeline Photos' album). Note: System albums (Profile Pictures, Cover Photos) cannot be modified directly. Only user-created albums support adding photos via this endpoint. Currently adds one photo at a time. For true batch upload, use Facebook's batch API endpoint directly.

**Parameters:**
  - `photos` (array, optional): List of local photo files to add (alternative to 'photo_urls')
  - `page_id` (string, optional): The ID of the Facebook Page that owns the album. Required for proper authentication with page token.
  - `album_id` (string, required): The ID of the album to add photos to
  - `published` (boolean, optional): Whether to publish the photos immediately
  - `photo_urls` (array, optional): List of photo URLs to add to the album (alternative to 'photos')

### FACEBOOK_ADD_REACTION

**MCP Tool**: `mcp__composio__FACEBOOK_ADD_REACTION`

Adds a reaction to a Facebook post or comment. Note: Due to API limitations, only LIKE reactions can be added programmatically.

**Parameters:**
  - `type` (string, optional): Reaction type: Currently only LIKE is supported via API. Other reactions (LOVE, WOW, HAHA, SAD, ANGRY, THANKFUL) cannot be added programmatically
  - `object_id` (string, required): The ID of the post or comment to react to. Facebook IDs are numeric strings (typically 15-20 digits). IMPORTANT: Always pass IDs as strings to preserve precision. Integer values will be converted to strings, but float values (including scientific notation like 5.3e+32) are rejected because they lose precision.

### FACEBOOK_ASSIGN_PAGE_TASK

**MCP Tool**: `mcp__composio__FACEBOOK_ASSIGN_PAGE_TASK`

Assigns tasks/roles to a business-scoped user or system user for a specific Facebook Page. Important: This action requires a business-scoped user ID or system user ID from Facebook Business Manager. Regular Facebook user IDs cannot be used. The page must also be managed through Facebook Business Manager for this action to work. Required permissions: business_management, pages_manage_metadata

**Parameters:**
  - `user` (string, required): The business-scoped user ID or system user ID to assign tasks to. Note: Regular Facebook user IDs are not accepted - only business-scoped IDs (from Business Manager) or system user IDs can be used with this endpoint.
  - `tasks` (array, required): List of tasks to assign. Valid values include: 'MANAGE', 'CREATE_CONTENT', 'MODERATE', 'ADVERTISE', 'ANALYZE', 'MESSAGING'. Example: ['MANAGE', 'CREATE_CONTENT']
  - `page_id` (string, required): The ID of the Facebook Page

### FACEBOOK_CREATE_COMMENT

**MCP Tool**: `mcp__composio__FACEBOOK_CREATE_COMMENT`

Creates a comment on a Facebook post or replies to an existing comment.

**Parameters:**
  - `message` (string, required): The text content of the comment
  - `object_id` (string, required): The ID of the post or comment to comment on. Must be a numeric ID (e.g., '3071372469667482') or compound format 'pageId_postId' (e.g., '678465505624869_3071372469667482'). Do not include prefixes like 'post_', 'id_', or 'p'.
  - `attachment_id` (string, optional): ID of an unpublished photo to attach to the comment
  - `attachment_url` (string, optional): URL of a photo to attach to the comment
  - `attachment_share_url` (string, optional): URL of a GIF to attach to the comment

### FACEBOOK_CREATE_PHOTO_ALBUM

**MCP Tool**: `mcp__composio__FACEBOOK_CREATE_PHOTO_ALBUM`

Creates a new photo album on a Facebook Page. Note: This endpoint requires the 'pages_manage_posts' permission or equivalent permissions to be granted to your Facebook application.

**Parameters:**
  - `name` (string, required): Name of the photo album
  - `message` (string, optional): Description of the album
  - `page_id` (string, required): The ID of the Facebook Page
  - `privacy` (object, optional): Privacy settings for the album (e.g., {'value': 'EVERYONE'})
  - `location` (string, optional): Location associated with the album

### FACEBOOK_CREATE_PHOTO_POST

**MCP Tool**: `mcp__composio__FACEBOOK_CREATE_PHOTO_POST`

Creates a photo post on a Facebook Page. Requires an image to be provided via either 'url' (publicly accessible image URL) or 'photo' (local image file upload). This action is specifically for posting images with optional captions, not text-only posts.

**Parameters:**
  - `url` (string, optional): URL of a publicly accessible image to upload. Supports direct image links with or without file extensions (e.g., https://example.com/image.jpg or hash-based URLs from services like Imgur, Gyazo, Postimages). The image host must not block requests from Facebook. Cannot be a Facebook URL. At least one of 'url', 'photo', or 'media' is required.
  - `media` (string, optional): Alias for 'photo'. for uploading a local image file (e.g..jpg.png.gif). At least one of 'media', 'photo', or 'url' is required.
  - `photo` (string, optional): for uploading a local image file (e.g..jpg.png.gif). At least one of 'photo', 'url', or 'media' is required.
  - `message` (string, optional): Caption text for the photo. Can also be provided as 'caption'.
  - `page_id` (string, required): The numeric ID of the Facebook Page to post to. Can be provided as a string or number.
  - `published` (boolean, optional): Set to true to publish immediately, false to save as unpublished
  - `backdated_time` (integer, optional): Unix timestamp to backdate the post
  - `scheduled_publish_time` (integer, optional): Unix timestamp for scheduled posts (required if published=false)
  - `backdated_time_granularity` (string, optional): Granularity of backdated time: year, month, day, hour, or min

### FACEBOOK_CREATE_POST

**MCP Tool**: `mcp__composio__FACEBOOK_CREATE_POST`

Creates a new post on a Facebook Page.

**Parameters:**
  - `link` (string, optional): URL to include in the post
  - `message` (string, required): The text content of the post
  - `page_id` (string, required): The numeric ID of the Facebook Page to post to. This is a numeric string (e.g., '123456789012345'). To obtain a valid page_id, use the 'Get User Pages' or 'List Managed Pages' action which returns page IDs for pages you have access to manage.
  - `published` (boolean, optional): Set to true to publish immediately, false to save as draft or schedule
  - `targeting` (object, optional): Audience targeting specifications
  - `scheduled_publish_time` (integer, optional): Unix timestamp for when the post should be published. Must be at least 10 minutes in the future. When provided, published must be false (will be auto-set to false if true).

### FACEBOOK_CREATE_VIDEO_POST

**MCP Tool**: `mcp__composio__FACEBOOK_CREATE_VIDEO_POST`

Creates a video post on a Facebook Page.

**Parameters:**
  - `title` (string, optional): Title of the video
  - `video` (string, optional): Local video file to upload. At least one of 'video' or 'file_url' must be provided.
  - `page_id` (string, required): The ID of the Facebook Page
  - `file_url` (string, optional): URL of the video file to upload. At least one of 'file_url' or 'video' must be provided.
  - `published` (boolean, optional): Whether to publish immediately
  - `targeting` (object, optional): Audience targeting specifications
  - `description` (string, optional): Description of the video
  - `scheduled_publish_time` (integer, optional): Unix timestamp to schedule the video post

### FACEBOOK_DELETE_COMMENT

**MCP Tool**: `mcp__composio__FACEBOOK_DELETE_COMMENT`

Deletes a Facebook comment. Requires a Page Access Token with appropriate permissions for comments on Page-owned content. The page_id parameter helps ensure the correct page token is used for authentication.

**Parameters:**
  - `page_id` (string, optional): Optional: The ID of the Facebook Page that owns the post containing this comment. If not provided, the action will use the first available managed page. Providing the correct page_id ensures proper authentication.
  - `comment_id` (string, required): The ID of the comment to delete. Can be in format 'parentId_commentId' (e.g., '122157027176937815_1371138271476143') or just the comment ID.

### FACEBOOK_DELETE_POST

**MCP Tool**: `mcp__composio__FACEBOOK_DELETE_POST`

Deletes a Facebook Page post.

**Parameters:**
  - `post_id` (string, required): The ID of the post to delete

### FACEBOOK_GET_COMMENT

**MCP Tool**: `mcp__composio__FACEBOOK_GET_COMMENT`

Retrieves details of a specific Facebook comment.

**Parameters:**
  - `fields` (string, optional): Comma-separated list of fields to return
  - `comment_id` (string, required): The ID of the comment to retrieve

### FACEBOOK_GET_COMMENTS

**MCP Tool**: `mcp__composio__FACEBOOK_GET_COMMENTS`

Retrieves comments from a Facebook post or comment (for replies). This endpoint requires appropriate permissions: - For page-owned posts: A Page Access Token with 'pages_read_engagement' permission - The API automatically swaps user tokens for page tokens when available API Version: Uses v23.0 which was released May 2025.

**Parameters:**
  - `limit` (integer, optional): Number of comments to return (max 100)
  - `order` (string, optional): Order of comments: 'chronological' (oldest first) or 'reverse_chronological' (newest first, default).
  - `fields` (string, optional): Comma-separated list of fields to return for each comment. Available fields: id, message, created_time, from, attachment, comment_count, like_count, is_hidden, user_likes, can_comment, can_remove, can_hide, permalink_url, parent, comments (for nested replies). Note: 'from' field requires a Page Token to access user information (since Graph API v2.11).
  - `filter` (string, optional): Filter comments by type: 'stream' returns all comments including replies in flat list (default), 'toplevel' returns only top-level comments without replies.
  - `object_id` (string, required): The ID of the post or comment to get comments from. Must be in full format 'pageId_postId' for posts (e.g., '123456789_987654321'). For comments, use the comment ID directly.

### FACEBOOK_GET_CONVERSATION_MESSAGES

**MCP Tool**: `mcp__composio__FACEBOOK_GET_CONVERSATION_MESSAGES`

Retrieves messages from a specific conversation.

**Parameters:**
  - `limit` (integer, optional): Number of messages to return (max 25)
  - `fields` (string, optional): Comma-separated list of fields to return for each message. Available fields include: id, created_time, from, to, message, attachments, sticker, shares, tags.
  - `page_id` (string, required): The ID of the Facebook Page that owns the conversation. Required to obtain the correct page access token. Get this from the List Managed Pages action.
  - `conversation_id` (string, required): The ID of the conversation in the format 't_' followed by a numeric ID (e.g., 't_3638640842939952'). Obtain valid conversation IDs from the Get Page Conversations action. If a numeric-only ID is provided, the 't_' prefix will be added automatically.

### FACEBOOK_GET_CURRENT_USER

**MCP Tool**: `mcp__composio__FACEBOOK_GET_CURRENT_USER`

Validates the access token and retrieves the current user's profile via /me.

**Parameters:**
  - `fields` (string, optional): Comma-separated list of fields to return for the current user

### FACEBOOK_GET_MESSAGE_DETAILS

**MCP Tool**: `mcp__composio__FACEBOOK_GET_MESSAGE_DETAILS`

Retrieves details of a specific message sent or received by the Page.

**Parameters:**
  - `fields` (string, optional): Comma-separated list of fields to return
  - `message_id` (string, required): The ID of the message to retrieve details for

### FACEBOOK_GET_PAGE_CONVERSATIONS

**MCP Tool**: `mcp__composio__FACEBOOK_GET_PAGE_CONVERSATIONS`

Retrieves a list of conversations between users and the Page.

**Parameters:**
  - `limit` (integer, optional): Number of conversations to return (max 25)
  - `fields` (string, optional): Comma-separated list of fields to return for each conversation
  - `page_id` (string, required): The ID of the Facebook Page. Numeric IDs are accepted and will be converted to strings.

### FACEBOOK_GET_PAGE_DETAILS

**MCP Tool**: `mcp__composio__FACEBOOK_GET_PAGE_DETAILS`

Fetches details about a specific Facebook Page.

**Parameters:**
  - `fields` (string, optional): Comma-separated list of fields to return for the Page. Common valid fields include: id, name, about, category, description, fan_count, followers_count, website, link, username, is_published, access_token, emails, phone, location, hours, cover, picture, engagement, verification_status, and many more. IMPORTANT: The following fields are NOT valid for direct Page queries and will be automatically filtered out: 'tasks' (only available via /me/accounts endpoint - use FACEBOOK_LIST_MANAGED_PAGES to get page tasks), 'created_time' (not supported on all page node types such as ProfileDelegatePage). For a complete list of valid Page fields, refer to the Facebook Graph API Page reference.
  - `page_id` (string, required): The unique numeric ID of the Facebook Page to get details for. This must be a valid Facebook Page ID that the authenticated user has access to view. Facebook Page IDs are numeric strings typically 15-16 digits long (e.g., '678594635343968'). To find valid page IDs you have access to, first use the FACEBOOK_LIST_MANAGED_PAGES or FACEBOOK_GET_USER_PAGES actions to retrieve a list of pages you manage, which will include their IDs. You can also find a page's ID in its Facebook URL (e.g., https://www.facebook.com/123456789012345) or in the Page's 'About' section. Do not use arbitrary numbers, timestamps, bank account numbers, or other non-Facebook identifiers.

### FACEBOOK_GET_PAGE_INSIGHTS

**MCP Tool**: `mcp__composio__FACEBOOK_GET_PAGE_INSIGHTS`

Retrieves analytics and insights for a Facebook Page. Returns metrics like impressions, page views, fan counts, and engagement data.

**Parameters:**
  - `since` (string, optional): Start of date range as Unix timestamp (e.g., '1704067200'), ISO 8601 datetime (e.g., '2024-10-01T00:00:00+0000', '2024-10-01'), or strtotime-compatible string (e.g., 'yesterday', '-7 days'). Maximum range is 90 days when combined with 'until'.
  - `until` (string, optional): End of date range as Unix timestamp (e.g., '1704672000'), ISO 8601 datetime (e.g., '2025-01-29T05:12:31+0000', '2025-01-29'), or strtotime-compatible string (e.g., 'now', '-1 day'). Maximum range is 90 days when combined with 'since'.
  - `period` (string, optional): Period for the metrics: day, week, days_28, month, lifetime
  - `metrics` (string, optional): Comma-separated list of metrics to retrieve. VALID METRICS: page_follows (total followers), page_daily_follows_unique (new follows), page_daily_unfollows_unique (unfollows), page_media_view (content views), page_post_engagements (engagement count), page_video_views (video views), page_total_actions (CTA clicks), page_actions_post_reactions_total (reactions breakdown). DEPRECATED (will be auto-replaced): page_impressions -> page_media_view, page_fans -> page_follows, page_engaged_users -> page_post_engagements, page_fan_adds -> page_daily_follows_unique. Individual reaction metrics (page_actions_post_reactions_like_total, etc.) are deprecated; use page_actions_post_reactions_total instead.
  - `page_id` (string, required): The ID of the Facebook Page

### FACEBOOK_GET_PAGE_PHOTOS

**MCP Tool**: `mcp__composio__FACEBOOK_GET_PAGE_PHOTOS`

Retrieves photos from a Facebook Page.

**Parameters:**
  - `type` (string, optional): Filter by photo type: uploaded, tagged
  - `limit` (integer, optional): Number of photos to return (max 100)
  - `fields` (string, optional): Comma-separated list of valid Photo fields to return. Valid fields include: id, created_time, updated_time, name, images, height, width, picture, link, icon, from, album, backdated_time, place, page_story_id, target, event, can_delete, can_tag, webp_images. NOTE: 'reactions' and 'comments' are NOT valid fields - they are edges that must be accessed via separate API calls (e.g., /{photo-id}/reactions, /{photo-id}/comments).
  - `page_id` (string, required): The numeric ID of the Facebook Page (e.g., '678594635343968'). You can obtain page IDs using the FACEBOOK_LIST_MANAGED_PAGES action. Do NOT pass datetime strings, timestamps, or date values - only valid Facebook page IDs.

### FACEBOOK_GET_PAGE_POSTS

**MCP Tool**: `mcp__composio__FACEBOOK_GET_PAGE_POSTS`

Retrieves posts from a Facebook Page. Endpoint choice: Uses /{page_id}/feed instead of /posts or /published_posts because: - /feed returns all content on page timeline (page's posts + visitor posts + tagged posts) - /posts returns only posts created by the page itself - /published_posts returns only published posts by the page (excludes scheduled/unpublished) The /feed endpoint provides the most comprehensive view of page activity. API Version: Uses v23.0 which was released May 2025. Version v20.0 and earlier will be deprecated by Meta. See: https://developers.facebook.com/docs/graph-api/changelog

**Parameters:**
  - `limit` (integer, optional): Number of posts to return (max 100)
  - `since` (string, optional): Filter posts updated after this time. Accepts: Unix timestamp (e.g., '1705320000'), strtotime values (e.g., 'yesterday', '7 days ago', 'last week'), or datetime strings (e.g., '2024-01-15', '2024-01-15T12:00:00'). Datetime strings are automatically converted to Unix timestamps.
  - `until` (string, optional): Filter posts updated before this time. Accepts: Unix timestamp (e.g., '1705320000'), strtotime values (e.g., 'yesterday', '7 days ago', 'last week'), or datetime strings (e.g., '2024-01-15', '2024-01-15T12:00:00'). Datetime strings are automatically converted to Unix timestamps.
  - `fields` (string, optional): Comma-separated list of fields to return for each post. Supported fields include: id, message, created_time, updated_time, permalink_url, attachments, story, from, status_type, full_picture, shares, reactions, comments, is_hidden, is_published. For summary counts, use '.summary(true)' syntax (e.g., 'reactions.summary(true)', 'comments.summary(true)', 'likes.summary(true)'). Note: 'type', 'link', 'source', 'picture', 'name', 'caption', 'description', and 'icon' are deprecated since Graph API v3.3 and will be automatically removed if requested.
  - `page_id` (string, required): The ID of the Facebook Page. Can be provided as a string or number.
  - `removed_deprecated_fields` (array, optional): Internal field to track deprecated fields that were automatically removed.


## Usage Examples

#### FACEBOOK_ADD_PHOTOS_TO_ALBUM
```
Use MCP tool: mcp__composio__FACEBOOK_ADD_PHOTOS_TO_ALBUM
Parameters: {
  "album_id": "your_album_id_here"
}
```

#### FACEBOOK_ADD_REACTION
```
Use MCP tool: mcp__composio__FACEBOOK_ADD_REACTION
Parameters: {
  "object_id": "your_object_id_here"
}
```

#### FACEBOOK_ASSIGN_PAGE_TASK
```
Use MCP tool: mcp__composio__FACEBOOK_ASSIGN_PAGE_TASK
Parameters: {
  "page_id": "your_page_id_here",
  "user": "your_user_here"
}
```

## Quick Reference

| MCP Tool | Description |
|----------|-------------|
| `mcp__composio__FACEBOOK_ADD_PHOTOS_TO_ALBUM` | Adds photos to an existing Facebook album. IMPORTA... |
| `mcp__composio__FACEBOOK_ADD_REACTION` | Adds a reaction to a Facebook post or comment. Not... |
| `mcp__composio__FACEBOOK_ASSIGN_PAGE_TASK` | Assigns tasks/roles to a business-scoped user or s... |
| `mcp__composio__FACEBOOK_CREATE_COMMENT` | Creates a comment on a Facebook post or replies to... |
| `mcp__composio__FACEBOOK_CREATE_PHOTO_ALBUM` | Creates a new photo album on a Facebook Page. Note... |
| `mcp__composio__FACEBOOK_CREATE_PHOTO_POST` | Creates a photo post on a Facebook Page. Requires ... |
| `mcp__composio__FACEBOOK_CREATE_POST` | Creates a new post on a Facebook Page. |
| `mcp__composio__FACEBOOK_CREATE_VIDEO_POST` | Creates a video post on a Facebook Page. |
| `mcp__composio__FACEBOOK_DELETE_COMMENT` | Deletes a Facebook comment. Requires a Page Access... |
| `mcp__composio__FACEBOOK_DELETE_POST` | Deletes a Facebook Page post. |
| `mcp__composio__FACEBOOK_GET_COMMENT` | Retrieves details of a specific Facebook comment. |
| `mcp__composio__FACEBOOK_GET_COMMENTS` | Retrieves comments from a Facebook post or comment... |
| `mcp__composio__FACEBOOK_GET_CONVERSATION_MESSAGES` | Retrieves messages from a specific conversation. |
| `mcp__composio__FACEBOOK_GET_CURRENT_USER` | Validates the access token and retrieves the curre... |
| `mcp__composio__FACEBOOK_GET_MESSAGE_DETAILS` | Retrieves details of a specific message sent or re... |
| `mcp__composio__FACEBOOK_GET_PAGE_CONVERSATIONS` | Retrieves a list of conversations between users an... |
| `mcp__composio__FACEBOOK_GET_PAGE_DETAILS` | Fetches details about a specific Facebook Page. |
| `mcp__composio__FACEBOOK_GET_PAGE_INSIGHTS` | Retrieves analytics and insights for a Facebook Pa... |
| `mcp__composio__FACEBOOK_GET_PAGE_PHOTOS` | Retrieves photos from a Facebook Page. |
| `mcp__composio__FACEBOOK_GET_PAGE_POSTS` | Retrieves posts from a Facebook Page. Endpoint cho... |

## When to Use This Integration

**Use facebook when:**
- Interacting with facebook services
- Automating facebook workflows
- Reading and writing facebook data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:
1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
