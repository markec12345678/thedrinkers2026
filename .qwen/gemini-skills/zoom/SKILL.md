---
name: zoom
description: zoom integration via Composio MCP. Use when user needs to interact with zoom.
---

# zoom Integration

> Auto-generated by Composio Skills Extension on 2026-03-09T19:28:58.161Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Integration with zoom services.

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### ZOOM_ADD_A_MEETING_REGISTRANT

**MCP Tool**: `mcp__composio__ZOOM_ADD_A_MEETING_REGISTRANT`

Registers a participant for a Zoom meeting that has registration enabled. **Prerequisites:** - The meeting host must have a **licensed (paid) Zoom account** - this will NOT work with free/basic accounts - The meeting must have registration enabled (approval_type = 0 for automatic or 1 for manual approval) - Maximum of 4,999 registrants per meeting **Required fields:** meetingId, first_name, last_name, email **Optional fields:** address, city, state, zip, country, phone, comments, industry, job_title, org, language, auto_approve **Common errors:** - "Only available for paid users" - The meeting host needs a licensed Zoom account - "Registration has not been enabled" - Enable registration via update_a_meeting with approval_type = 0 or 1 - "Meeting not found" - Invalid meeting ID or meeting has ended

**Parameters:**
  - `org` (string, optional): The registrant"s organization.
  - `zip` (string, optional): The registrant"s ZIP or postal code.
  - `city` (string, optional): The registrant"s city.
  - `email` (string, required): The registrant"s email address.
  - `phone` (string, optional): The registrant"s phone number.
  - `state` (string, optional): The registrant"s state or province.
  - `address` (string, optional): The registrant"s address.
  - `country` (string, optional): The registrant"s two-letter [country code](https://marketplace.zoom.us/docs/api-reference/other-references/abbreviation-lists#countries). 
  - `comments` (string, optional): The registrant"s questions and comments.
  - `industry` (string, optional): The registrant"s industry.
  - `language` (string, optional): The registrant"s language preference for confirmation emails:  * `en-US` &mdash; English (US)  * `de-DE` &mdash; German (Germany)  * `es-ES` &mdash; Spanish (Spain)  * `fr-FR` &mdash; French (France)  * `jp-JP` &mdash; Japanese  * `pt-PT` &mdash; Portuguese (Portugal)  * `ru-RU` &mdash; Russian  * `zh-CN` &mdash; Chinese (PRC)  * `zh-TW` &mdash; Chinese (Taiwan)  * `ko-KO` &mdash; Korean  * `it-IT` &mdash; Italian (Italy)  * `vi-VN` &mdash; Vietnamese  * `pl-PL` &mdash; Polish  * `Tr-TR` &mdash; Turkish 
  - `job_title` (string, optional): The registrant"s job title.
  - `last_name` (string, required): The registrant"s last name.
  - `meetingId` (integer, required): The meeting"s ID.   When storing this value in your database, you must store it as a long format integer and **not** an integer. Meeting IDs can exceed 10 digits. 
  - `first_name` (string, required): The registrant"s first name.
  - `auto_approve` (boolean, optional): If a meeting was scheduled with the `approval_type` field value of `1` (manual approval) but you want to automatically approve meeting registrants, set the value of this field to `true`.  **Note:** You cannot use this field to change approval setting for a meeting originally scheduled with the `approval_type` field value of `0` (automatic approval). 
  - `occurrence_ids` (string, optional): A comma-separated list of meeting occurrence IDs. You can get this value with the [Get a meeting](https://developers.zoom.us) API. 
  - `no_of_employees` (string, optional): The registrant"s number of employees:  * `1-20`  * `21-50`  * `51-100`  * `101-500`  * `500-1,000`  * `1,001-5,000`  * `5,001-10,000`  * `More than 10,000` 
  - `custom_questions` (array, optional): Information about custom questions.
  - `purchasing_time_frame` (string, optional): The registrant"s purchasing time frame:  * `Within a month`  * `1-3 months`  * `4-6 months`  * `More than 6 months`  * `No timeframe` 
  - `role_in_purchase_process` (string, optional): The registrant"s role in the purchase process:  * `Decision Maker`  * `Evaluator/Recommender`  * `Influencer`  * `Not involved` 

### ZOOM_ADD_A_WEBINAR_REGISTRANT

**MCP Tool**: `mcp__composio__ZOOM_ADD_A_WEBINAR_REGISTRANT`

Registers a participant for a Zoom webinar that has registration enabled. **Prerequisites:** - The webinar host must have a **Pro or higher plan with Webinar add-on** - this will NOT work with basic/free accounts - The webinar must have registration enabled (approval_type = 0 for automatic or 1 for manual approval) - Registration type must be properly configured (1=once for all, 2=each occurrence, 3=choose occurrences) **Required fields:** webinarId, first_name, email **Optional fields:** last_name, address, city, state, zip, country, phone, comments, industry, job_title, org, language, occurrence_ids, source_id **Common errors:** - "Webinar plan is missing" - The host needs a Zoom webinar license/add-on - "Registration has not been enabled" - Enable registration via webinar settings with approval_type = 0 or 1 - "Webinar not found" (404) - Invalid webinar ID or webinar has ended - "Host cannot register" - The webinar host cannot register themselves as an attendee

**Parameters:**
  - `org` (string, optional): The registrant"s organization.
  - `zip` (string, optional): The registrant"s ZIP or postal code.
  - `city` (string, optional): The registrant"s city.
  - `email` (string, required): The registrant"s email address.
  - `phone` (string, optional): The registrant"s phone number.
  - `state` (string, optional): The registrant"s state or province.
  - `address` (string, optional): The registrant"s address.
  - `country` (string, optional): The registrant"s two-letter [country code](https://developers.zoom.us/docs/api/rest/other-references/abbreviation-lists/#countries). 
  - `comments` (string, optional): The registrant"s questions and comments.
  - `industry` (string, optional): The registrant"s industry.
  - `language` (string, optional): The registrant"s language preference for confirmation emails:  * `en-US` - English (US)  * `de-DE` - German (Germany)  * `es-ES` - Spanish (Spain)  * `fr-FR` - French (France)  * `jp-JP` - Japanese  * `pt-PT` - Portuguese (Portugal)  * `ru-RU` - Russian  * `zh-CN` - Chinese (PRC)  * `zh-TW` - Chinese (Taiwan)  * `ko-KO` - Korean  * `it-IT` - Italian (Italy)  * `vi-VN` - Vietnamese  * `pl-PL` - Polish  * `Tr-TR` - Turkish 
  - `job_title` (string, optional): The registrant"s job title.
  - `last_name` (string, optional): The registrant"s last name.
  - `source_id` (string, optional): The tracking source"s unique identifier.
  - `webinarId` (integer, required): The webinar"s ID.
  - `first_name` (string, required): The registrant"s first name.
  - `occurrence_ids` (string, optional): A comma-separated list of webinar occurrence IDs. Get this value with the [Get a webinar](https://developers.zoom.us) API. Make sure the `registration_type` is 3 if updating multiple occurrences with this API. 
  - `no_of_employees` (string, optional): The registrant"s number of employees:  * `1-20`  * `21-50`  * `51-100`  * `101-500`  * `500-1,000`  * `1,001-5,000`  * `5,001-10,000`  * `More than 10,000` 
  - `custom_questions` (array, optional): Information about custom questions.
  - `purchasing_time_frame` (string, optional): The registrant"s purchasing time frame:  * `Within a month`  * `1-3 months`  * `4-6 months`  * `More than 6 months`  * `No timeframe` 
  - `role_in_purchase_process` (string, optional): The registrant"s role in the purchase process:  * `Decision Maker`  * `Evaluator/Recommender`  * `Influencer`  * `Not involved` 

### ZOOM_CREATE_A_MEETING

**MCP Tool**: `mcp__composio__ZOOM_CREATE_A_MEETING`

Enable Zoom meeting creation via user-level apps with "me". "Start_url" for hosts expires in 2 hours, or 90 days for "custCreate" users. Renew via API, capped at 100 requests/day. Requires "meeting:write" permission, subject to medium rate limit.

**Parameters:**
  - `type` (integer, optional): The type of meeting. * `1` - An instant meeting.  * `2` - A scheduled meeting.  * `3` - A recurring meeting with no fixed time.  * `8` - A recurring meeting with fixed time. 
  - `topic` (string, optional): The meeting"s topic.
  - `agenda` (string, optional): The meeting"s agenda. This value has a maximum length of 2,000 characters. 
  - `userId` (string, optional): The user"s user ID or email address. For user-level apps, pass the `me` value. Defaults to `me` for user-level apps. 
  - `duration` (integer, optional): The meeting"s scheduled duration, in minutes. This field is only used for scheduled meetings (`2`). 
  - `password` (string, optional): The passcode required to join the meeting. By default, a passcode can **only** have a maximum length of 10 characters and only contain alphanumeric characters and the `@`, `-`, `_`, and `*` characters.  * If the account owner or administrator has configured [minimum passcode requirement settings](https://support.zoom.us/hc/en-us/articles/360033559832-Meeting-and-webinar-passwords#h_a427384b-e383-4f80-864d-794bf0a37604), the passcode **must** meet those requirements.  * If passcode requirements are enabled, use the [**Get user settings**](https://developers.zoom.us/docs/api-reference/zoom-api/methods#operation/userSettings) API or the [**Get account settings**](https://developers.zoom.us/docs/api-reference/zoom-api/ma#operation/accountSettings) API to get the requirements. 
  - `timezone` (string, optional): The timezone to assign to the `start_time` value. This field is only used for scheduled or recurring meetings with a fixed time. For a list of supported timezones and their formats, see our [timezone list](https://developers.zoom.us/docs/api/rest/other-references/abbreviation-lists/#timezones). 
  - `start_time` (string, optional): The meeting"s start time. This field is only used for scheduled or recurring meetings with a fixed time. This supports local time and GMT formats.  * To set a meeting"s start time in GMT, use the `yyyy-MM-ddTHH:mm:ssZ` date-time format. For example, `2020-03-31T12:02:00Z`.  * To set a meeting"s start time using a specific timezone, use the `yyyy-MM-ddTHH:mm:ss` date-time format and specify the [timezone ID](https://developers.zoom.us/docs/api/rest/other-references/abbreviation-lists/#timezones) in the `timezone` field. If you do not specify a timezone, the `timezone` value defaults to your Zoom account"s timezone. You can also use `UTC` for the `timezone` value. **Note:** If no `start_time` is set for a scheduled meeting, the `start_time` is set at the current time and the meeting type changes to an instant meeting, which expires after 30 days. 
  - `template_id` (string, optional): The account admin meeting template ID used to schedule a meeting using a [meeting template](https://support.zoom.us/hc/en-us/articles/360036559151-Meeting-templates). For a list of account admin-provided meeting templates, use the [**List meeting templates**](https://developers.zoom.us/docs/api-reference/zoom-api/methods#operation/listMeetingTemplates) API.  * At this time, this field **only** accepts account admin meeting template IDs.  * To enable the account admin meeting templates feature, [contact Zoom support](https://support.zoom.us/hc/en-us). 
  - `pre_schedule` (boolean, optional): Whether to create a prescheduled meeting via the [GSuite app](https://support.zoom.us/hc/en-us/articles/360020187492-Zoom-for-GSuite-add-on). This **only** supports the meeting `type` value of `2` (scheduled meetings) and `3` (recurring meetings with no fixed time).  * `true` - Create a prescheduled meeting.  * `false` - Create a regular meeting. 
  - `schedule_for` (string, optional): The email address or user ID of the user to schedule a meeting for.
  - `settings__audio` (string, optional): How participants join the audio portion of the meeting. * `both` - Both telephony and VoIP.  * `telephony` - Telephony only.  * `voip` - VoIP only.  * `thirdParty` - Third party audio conference. 
  - `tracking_fields` (array, optional): Information about the meeting"s tracking fields.
  - `default_password` (boolean, optional): Whether to generate a default passcode using the user"s settings. This value defaults to `false`.  If this value is `true` and the user has the PMI setting enabled with a passcode, then the user"s meetings will use the PMI passcode. It will **not** use a default passcode. 
  - `recurrence__type` (integer, optional): Recurrence meeting types.  `1` - Daily.    `2` - Weekly.    `3` - Monthly. 
  - `settings__use__pmi` (boolean, optional): Whether to use a [Personal Meeting ID (PMI)](https://developers.zoom.us/docs/api/rest/using-zoom-apis/#understanding-personal-meeting-id-pmi) instead of a generated meeting ID. This field is only used for scheduled meetings (`2`), instant meetings (`1`), or recurring meetings with no fixed time (`3`). This value defaults to `false`. 
  - `settings__jbh__time` (integer, optional): If the value of the `join_before_host` field is `true`, this field indicates the time limits when a participant can join a meeting before the meeting"s host. * `0` - Allow the participant to join the meeting at anytime. * `5` - Allow the participant to join 5 minutes before the meeting"s start time. * `10` - Allow the participant to join 10 minutes before the meeting"s start time. 
  - `settings__resources` (array, optional): The meeting"s resources. Each resource must include resource_id and resource_type fields.
  - `settings__watermark` (boolean, optional): Whether to add a watermark when viewing a shared screen.
  - `settings__cn__meeting` (boolean, optional): Whether to host the meeting in China (CN). This value defaults to `false`. 
  - `settings__focus__mode` (boolean, optional): Whether to enable the [**Focus Mode** feature](https://support.zoom.us/hc/en-us/articles/360061113751-Using-focus-mode) when the meeting starts. 
  - `settings__host__video` (boolean, optional): Whether to start meetings with the host video on.
  - `settings__in__meeting` (boolean, optional): Whether to host the meeting in India (IN). This value defaults to `false`. 
  - `recurrence__end__times` (integer, optional): Select how many times the meeting should recur before it is canceled. If `end_times` is set to 0, it means there is no end time. The maximum number of recurring is 60. Cannot be used with `end_date_time`. 
  - `settings__contact__name` (string, optional): The contact name for meeting registration.
  - `settings__waiting__room` (boolean, optional): Whether to enable the [**Waiting Room** feature](https://support.zoom.us/hc/en-us/articles/115000332726-Waiting-Room). If this value is `true`, this **disables** the `join_before_host` setting. 
  - `recurrence__monthly__day` (integer, optional): Use this field **only if you're scheduling a recurring meeting of type** `3` to state the day in a month when the meeting should recur. The value range is from 1 to 31. For the meeting to recur on 23rd of each month, provide `23` as this field's value and `1` as the `repeat_interval` field's value. Instead, if you would like the meeting to recur every three months, on 23rd of the month, change the value of the `repeat_interval` field to `3`. 
  - `recurrence__weekly__days` (string, optional): This field is required if you're scheduling a recurring meeting of type `2` to state the days of the week when the meeting should repeat. The value must be a single number between `1` to `7` in string format. For instance, if the meeting should recur on Sunday, provide `1` as this field's value. To set the meeting to occur on multiple days, you must call the API multiple times or use a different recurrence pattern. `1` - Sunday. `2` - Monday. `3` - Tuesday. `4` - Wednesday. `5` - Thursday. `6` - Friday. `7` - Saturday.
  - `settings__approval__type` (integer, optional): Enable meeting registration approval. * `0` - Automatically approve registration. * `1` - Manually approve registration. * `2` - No registration required. This value defaults to `2`. 
  - `settings__contact__email` (string, optional): The contact email address for meeting registration.
  - `recurrence__monthly__week` (integer, optional): Use this field **only if you're scheduling a recurring meeting of type** `3` to state the week of the month when the meeting should recur. If you use this field, you must also use the `monthly_week_day` field to state the day of the week when the meeting should recur.     `-1` - Last week of the month.    `1` - First week of the month.    `2` - Second week of the month.    `3` - Third week of the month.    `4` - Fourth week of the month. 
  - `settings__auto__recording` (string, optional): The automatic recording settings.  * `local` - Record the meeting locally.  * `cloud` - Record the meeting to the cloud.  * `none` - Auto-recording disabled. This value defaults to `none`. 
  - `settings__encryption__type` (string, optional): The type of [end-to-end (E2EE) encryption](https://support.zoom.us/hc/en-us/articles/360048660871) to use for the meeting.  * `enhanced_encryption` - Enhanced encryption. Encryption is stored in the cloud when you enable this option.  * `e2ee` - End-to-end encryption. The encryption key is stored on your local device and **cannot** be obtained by anyone else. When you use E2EE encryption, [certain features](https://support.zoom.us/hc/en-us/articles/360048660871), such as cloud recording or phone and SIP/H.323 dial-in, are **disabled**. 
  - `settings__private__meeting` (boolean, optional): Whether to set the meeting as private.
  - `recurrence__end__date__time` (string, optional): Select the final date when the meeting will recur before it is canceled. Should be in UTC time, such as 2017-11-25T12:00:00Z. Cannot be used with `end_times`. 
  - `settings__internal__meeting` (boolean, optional): Whether to set the meeting as an internal meeting.
  - `settings__meeting__invitees` (array, optional): A list of the meeting"s invitees.
  - `settings__mute__upon__entry` (boolean, optional): Whether to mute participants upon entry.
  - `recurrence__repeat__interval` (integer, optional): Define the interval when the meeting should recur. For instance, to schedule a meeting that recurs every two months, set this field's value as `2` and the value of the `type` parameter as `3`.  For a daily meeting, the maximum interval you can set is `90` days. For a weekly meeting the maximum interval that you can set is  of `12` weeks. For a monthly meeting, there is a maximum of `3` months.  
  - `settings__alternative__hosts` (string, optional): A semicolon-separated list of the meeting"s alternative hosts" email addresses or IDs. 
  - `settings__join__before__host` (boolean, optional): Whether participants can join the meeting before its host. This field is only used for scheduled meetings (`2`) or recurring meetings (`3` and `8`). This value defaults to `false`. If the [**Waiting Room** feature](https://support.zoom.us/hc/en-us/articles/115000332726-Waiting-Room) is enabled, this setting is **disabled**. 
  - `settings__participant__video` (boolean, optional): Whether to start meetings with the participant video on.
  - `settings__registration__type` (integer, optional): The meeting"s registration type.  * `1` - Attendees register once and can attend any meeting occurrence.  * `2` - Attendees must register for each meeting occurrence.  * `3` - Attendees register once and can select one or more meeting occurrences to attend. This field is only for recurring meetings with fixed times (`8`). This value defaults to `1`. 
  - `settings__close__registration` (boolean, optional): Whether to close registration after the event date. This value defaults to `false`. 
  - `settings__email__notification` (boolean, optional): Whether to send email notifications to [alternative hosts](https://support.zoom.us/hc/en-us/articles/208220166) and [users with scheduling privileges](https://support.zoom.us/hc/en-us/articles/201362803-Scheduling-privilege). This value defaults to `true`. 
  - `settings__show__share__button` (boolean, optional): Whether to include social media sharing buttons on the meeting"s registration page. This setting is only applied to meetings with registration enabled. 
  - `recurrence__monthly__week__day` (integer, optional): Use this field **only if you're scheduling a recurring meeting of type** `3` to state a specific day in a week when the monthly meeting should recur. To use this field, you must also use the `monthly_week` field.      `1` - Sunday.    `2` - Monday.    `3` - Tuesday.    `4` -  Wednesday.    `5` - Thursday.    `6` - Friday.    `7` - Saturday. 
  - `settings__breakout__room__rooms` (array, optional): Information about the breakout rooms.
  - `settings__authentication__option` (string, optional): If the `meeting_authentication` value is `true`, the type of authentication required for users to join a meeting. To get this value, use the `authentication_options` array"s `id` value in the [**Get user settings**](https://developers.zoom.us/docs/api-reference/zoom-api/methods#operation/userSettings) API response. 
  - `settings__breakout__room__enable` (boolean, optional): Whether to enable the [**Breakout Room pre-assign**](https://support.zoom.us/hc/en-us/articles/360032752671-Pre-assigning-participants-to-breakout-rooms) option. 
  - `settings__audio__conference__info` (string, optional): Third party audio conference info.
  - `settings__authentication__domains` (string, optional): The meeting"s authenticated domains. Only Zoom users whose email address contains an authenticated domain can join the meeting. Comma-separate multiple domains or use a wildcard for listing domains. 
  - `settings__meeting__authentication` (boolean, optional): If true, only [authenticated](https://support.zoom.us/hc/en-us/articles/360037117472-Authentication-Profiles-for-Meetings-and-Webinars) users can join the meeting. 
  - `settings__allow__multiple__devices` (boolean, optional): Whether to allow attendees to join a meeting from multiple devices. This setting is only applied to meetings with registration enabled. 
  - `settings__host__save__video__order` (boolean, optional): Whether the **Allow host to save video order** feature is enabled.
  - `settings__authentication__exception` (array, optional): A list of participants that can bypass meeting authentication. These participants will receive a unique meeting invite. Each exception must include name and email fields.
  - `settings__push__change__to__calendar` (boolean, optional): Whether to push meeting changes to the calendar.   To enable this feature, configure the **Configure Calendar and Contacts Service** in the user"s profile page of the Zoom web portal and enable the **Automatically sync Zoom calendar events information bi-directionally between Zoom and integrated calendars.** setting in the **Settings** page of the Zoom web portal. * `true` - Push meeting changes to the calendar. * `false` - Do not push meeting changes to the calendar. 
  - `settings__global__dial__in__countries` (array, optional): A list of available global dial-in countries.
  - `settings__auto__start__meeting__summary` (boolean, optional): Whether to automatically start a meeting summary.
  - `settings__participant__focused__meeting` (boolean, optional): Whether to set the meeting as a participant focused meeting.
  - `settings__alternative__host__update__polls` (boolean, optional): Whether the **Allow alternative hosts to add or edit polls** feature is enabled. This requires Zoom version 5.8.0 or higher. 
  - `settings__language__interpretation__enable` (boolean, optional): Whether to enable [language interpretation](https://support.zoom.us/hc/en-us/articles/360034919791-Language-interpretation-in-meetings-and-webinars) for the meeting. 
  - `settings__registrants__confirmation__email` (boolean, optional): Whether to send registrants an email confirmation.  * `true` - Send a confirmation email.  * `false` - Do not send a confirmation email. 
  - `settings__registrants__email__notification` (boolean, optional): Whether to send registrants email notifications about their registration approval, cancellation, or rejection. * `true` - Send an email notification. * `false` - Do not send an email notification.  Set this value to `true` to also use the `registrants_confirmation_email` parameter. 
  - `settings__additional__data__center__regions` (array, optional): Add additional meeting [data center regions](https://support.zoom.us/hc/en-us/articles/360042411451-Selecting-data-center-regions-for-hosted-meetings-and-webinars). Provide this value as an array of [country codes](https://developers.zoom.us/docs/api/rest/other-references/abbreviation-lists/#countries) for the countries available as data center regions in the [**Account Profile**](https://zoom.us/account/setting) interface but have been opted out of in the [user settings](https://zoom.us/profile). For example, the data center regions selected in your [**Account Profile**](https://zoom.us/account) are `Europe`, `Hong Kong SAR`, `Australia`, `India`, `Japan`, `China`, `United States`, and `Canada`. However, in the [**My Profile**](https://zoom.us/profile) settings, you did **not** select `India` and `Japan` for meeting and webinar traffic routing. To include `India` and `Japan` as additional data centers, use the `[IN, TY]` value for this field. 
  - `settings__continuous__meeting__chat__enable` (boolean, optional): Whether to enable the **Enable continuous meeting chat** setting.
  - `settings__approved_denied_regions__denied__list` (array, optional): The list of blocked countries or regions.
  - `settings__auto__start__ai__companion__questions` (boolean, optional): Whether to automatically start AI Companion questions.
  - `settings__language__interpretation__interpreters` (array, optional): Information about the meeting"s language interpreters. Each interpreter must include email and languages fields.
  - `settings__sign__language__interpretation__enable` (boolean, optional): Whether to enable [sign language interpretation](https://support.zoom.us/hc/en-us/articles/9644962487309-Using-sign-language-interpretation-in-a-meeting-or-webinar) for the meeting. 
  - `settings__alternative__hosts__email__notification` (boolean, optional): Whether to send email notifications to alternative hosts. This value defaults to `true`. 
  - `settings__approved_denied_regions__approved__list` (array, optional): The list of approved countries or regions.
  - `settings__meeting__chat__auto__add__external__users` (boolean, optional): Whether to enable the **Automatically add invited external users** setting. 
  - `settings__sign__language__interpretation__interpreters` (array, optional): Information about the meeting"s sign language interpreters. Each interpreter must include email and sign_language fields.
  - `settings__approved__or__denied__countries__or__regions__enable` (boolean, optional): Whether to enable the [**Approve or block entry for users from specific countries/regions**](https://support.zoom.us/hc/en-us/articles/360060086231-Approve-or-block-entry-for-users-from-specific-countries-regions) setting. 
  - `settings__approved__or__denied__countries__or__regions__method` (string, optional): Whether to allow or block users from specific countries or regions. * `approve` - Allow users from specific countries or regions to join the meeting. If you select this setting, include the approved countries or regions in the `approved_list` field.  * `deny` - Block users from specific countries or regions from joining the meeting. If you select this setting, include the blocked countries or regions in the `denied_list` field. 

### ZOOM_CREATE_IQ_CONVERSATION

**MCP Tool**: `mcp__composio__ZOOM_CREATE_IQ_CONVERSATION`

Tool to create a Zoom Revenue Accelerator (formerly Zoom IQ) conversation. Use when you need to create a new conversation entry, typically after uploading recording files, transcripts, and metadata.

**Parameters:**
  - `topic` (string, optional): Topic or title of the conversation.
  - `host_id` (string, optional): User ID of the conversation host. If not provided, defaults to the authenticated user.
  - `duration` (integer, optional): Duration of the conversation in seconds.
  - `meeting_id` (string, optional): Meeting ID to associate with this conversation.
  - `start_time` (string, optional): Conversation start time in ISO 8601 date-time format (e.g., 2024-01-15T10:30:00Z).
  - `primary_language` (string, optional): Primary language code for the conversation (e.g., 'en', 'es', 'fr').
  - `conversation_type` (string, optional): Type of conversation (e.g., 'meeting', 'phone', 'external').

### ZOOM_CREATE_IQ_CONVERSATION_COMMENT

**MCP Tool**: `mcp__composio__ZOOM_CREATE_IQ_CONVERSATION_COMMENT`

Tool to create a comment on a Zoom IQ conversation. Use when you need to add comments, replies, or annotations to IQ conversation recordings or transcripts.

**Parameters:**
  - `comment` (string, required): The text content of the comment to add to the conversation.
  - `is_private` (boolean, optional): Whether the comment is private (visible only to specific users) or public (visible to all conversation participants).
  - `conversationId` (string, required): The conversation's unique identifier. If the conversation ID begins with '/' or contains '//', it will be automatically double-encoded as required by the API.
  - `mention_team_ids` (array, optional): Array of team IDs to mention in the comment. Team members will be notified.
  - `mention_user_ids` (array, optional): Array of user IDs to mention in the comment. These users will be notified.
  - `parent_comment_id` (string, optional): Parent comment ID for threaded replies. Leave empty for top-level comments.
  - `time_in_recording` (string, optional): Timestamp in the recording where the comment should be anchored.

### ZOOM_CREATE_IQ_USER_CONVERSATION

**MCP Tool**: `mcp__composio__ZOOM_CREATE_IQ_USER_CONVERSATION`

Tool to create a Zoom IQ conversation for a user. Use when you need to create a new conversation entry in Zoom Revenue Accelerator for a specific user.

**Parameters:**
  - `id` (string, optional): The user ID or email address of the user. Use 'me' to create a conversation for the authenticated user. Defaults to 'me'.
  - `type` (string, optional): The type of conversation (e.g., 'meeting', 'phone_call').
  - `topic` (string, optional): The topic or title of the conversation.

### ZOOM_CREATE_ZRA_CONVERSATION

**MCP Tool**: `mcp__composio__ZOOM_CREATE_ZRA_CONVERSATION`

Tool to create a new conversation in Zoom Revenue Accelerator (ZRA). Use when uploading meeting recordings or phone calls for AI-powered analytics, including transcription, sentiment analysis, and engagement scoring.

**Parameters:**
  - `deal_id` (string, optional): Zoom deal ID to associate this conversation with a specific deal in your CRM.
  - `file_id` (string, optional): IQ file's unique identifier for uploading a recording file that was already uploaded to Zoom IQ.
  - `host_id` (string, optional): Host user ID or email address. If not provided, the OAuth token owner will be set as the default host.
  - `timeline` (array, optional): Speech timeline showing when each participant spoke during the recording. Helps with speaker diarization and analytics.
  - `timezone` (string, optional): Time zone for formatting timestamps in the conversation analytics (e.g., 'America/New_York', 'Europe/London').
  - `download_url` (string, optional): Third-party download URL for the recording file. Use this to directly upload a file from an external file service.
  - `participants` (array, optional): List of participants in the conversation. Maximum 200 participants. Each participant must have a display_name and optionally an email.
  - `meeting_end_time` (string, optional): End time of the meeting or call in ISO-8601 format. Optional but recommended for accurate duration tracking.
  - `primary_language` (string, optional): Supported primary languages for conversation analysis
  - `conversation_type` (string, required): Type of conversation to create. Use 'meeting' for Zoom meetings or video calls, 'phone' for phone calls.
  - `conversation_topic` (string, optional): Topic or title of the meeting or phone call. Helps identify the conversation in analytics.
  - `meeting_start_time` (string, required): Start time of the meeting or call in ISO-8601 format (e.g., '2026-02-17T10:00:00Z'). Required for all conversations.

### ZOOM_CREATE_ZRA_CONVERSATION_COMMENT

**MCP Tool**: `mcp__composio__ZOOM_CREATE_ZRA_CONVERSATION_COMMENT`

Tool to create a comment on a Zoom Revenue Accelerator conversation. Use when you need to add comments, replies, or annotations to ZRA conversation recordings or transcripts.

**Parameters:**
  - `id` (string, required): The conversation identifier for the Zoom Revenue Accelerator conversation. If the conversation ID begins with '/' or contains '//', it will be automatically double-encoded as required by the API.
  - `comment` (string, required): The text content of the comment to add to the conversation.
  - `is_private` (boolean, optional): Whether the comment is private (visible only to specific users) or public (visible to all conversation participants).
  - `mention_team_ids` (array, optional): Array of team IDs to mention in the comment. Team members will be notified.
  - `mention_user_ids` (array, optional): Array of user IDs to mention in the comment. These users will be notified.
  - `parent_comment_id` (string, optional): Parent comment ID for nested replies. Leave empty for top-level comments.
  - `time_in_recording` (string, optional): Timestamp in the recording where the comment should be anchored (format: HH:MM:SS).

### ZOOM_CREATE_ZRA_CRM_ACCOUNTS

**MCP Tool**: `mcp__composio__ZOOM_CREATE_ZRA_CRM_ACCOUNTS`

Tool to create or update CRM accounts in Zoom Revenue Accelerator (ZRA). Use when you need to sync CRM account data to Zoom IQ. Supports up to 2000 accounts per request and returns an async task ID for polling status.

**Parameters:**
  - `data` (array, required): Array of CRM account objects to create or update. Maximum 2000 accounts per request.

### ZOOM_CREATE_ZRA_CRM_CONTACTS

**MCP Tool**: `mcp__composio__ZOOM_CREATE_ZRA_CRM_CONTACTS`

Tool to bulk import or delete CRM contacts in Zoom Revenue Accelerator. Use when syncing contacts from external CRM systems. Accepts up to 2,000 contacts per request and returns a task ID for async status tracking.

**Parameters:**
  - `data` (array, required): Array of CRM contact objects. Up to 2,000 contacts can be submitted per request

### ZOOM_CREATE_ZRA_CRM_DEALS

**MCP Tool**: `mcp__composio__ZOOM_CREATE_ZRA_CRM_DEALS`

Tool to bulk import or delete CRM deals in Zoom Revenue Accelerator (ZRA). Use when you need to synchronize deals from an external CRM to Zoom's ZRA system. Supports up to 2000 deals per request and processes asynchronously.

**Parameters:**
  - `data` (array, required): List of CRM deal objects for bulk import or deletion. Maximum of 2000 deals per request.

### ZOOM_CREATE_ZRA_CRM_LEADS

**MCP Tool**: `mcp__composio__ZOOM_CREATE_ZRA_CRM_LEADS`

Tool to bulk import CRM leads into Zoom Revenue Accelerator (ZRA). Use when you need to import or update multiple leads asynchronously. Supports up to 2000 leads per request and returns a task_id to track import progress.

**Parameters:**
  - `data` (array, required): Array of CRM lead objects to bulk import. Maximum of 2000 leads per request. Each lead must include crm_lead_id, lead_name, and modify_time.

### ZOOM_CREATE_ZRA_CRM_SETTINGS

**MCP Tool**: `mcp__composio__ZOOM_CREATE_ZRA_CRM_SETTINGS`

Tool to register a new CRM API integration for Zoom Revenue Accelerator (ZRA). Use when you need to configure CRM settings including type, name, currency, deal stages, and URL patterns for accessing CRM records.

**Parameters:**
  - `crm_name` (string, optional): Display name for the CRM type. If not provided, defaults to the value of crm_type.
  - `crm_type` (string, required): CRM type identifier, 1-16 alphanumeric characters, must start with letter (e.g., 'oracle', 'salesforce').
  - `deal_stages` (array, required): Array of deal stage definitions. Must include at least one stage for each type: OPEN, CLOSED_WON, and CLOSED_LOST.
  - `currency_type` (string, optional): Currency unit to use (e.g., 'USD', 'EUR', 'JPY'). Defaults to 'USD' if not specified.
  - `url_pattern_deal` (string, optional): URL pattern template for opening deal objects in the CRM. Use {id} as placeholder for the deal ID (e.g., 'https://example.com/deals/{id}').
  - `url_pattern_lead` (string, optional): URL pattern template for opening lead objects in the CRM. Use {id} as placeholder (e.g., 'https://example.com/leads/{id}').
  - `url_pattern_account` (string, optional): URL pattern template for opening account objects in the CRM. Use {id} as placeholder (e.g., 'https://example.com/accounts/{id}').
  - `url_pattern_contact` (string, optional): URL pattern template for opening contact objects in the CRM. Use {id} as placeholder (e.g., 'https://example.com/contacts/{id}').

### ZOOM_CREATE_ZRA_USER_CONVERSATION

**MCP Tool**: `mcp__composio__ZOOM_CREATE_ZRA_USER_CONVERSATION`

Tool to create a new conversation in Zoom Revenue Accelerator for a specific user. Use when you need to initiate a ZRA conversation entry with topic and start time.

**Parameters:**
  - `id` (string, required): The user ID or 'me' to create a conversation for. Use 'me' for the authenticated user.
  - `topic` (string, required): The conversation title/topic. Specifies the name or subject of the conversation.
  - `start_time` (string, required): Meeting start timestamp in ISO 8601 format (e.g., '2026-02-17T10:00:00Z').

### ZOOM_DELETE_A_MEETING

**MCP Tool**: `mcp__composio__ZOOM_DELETE_A_MEETING`

Delete or cancel a scheduled Zoom meeting. Use occurrence_id to delete a specific occurrence of a recurring meeting. Supports notification options for hosts and registrants. Returns HTTP 204 on success. Rate limit: Light. Required scopes: meeting:write or meeting:write:admin.

**Parameters:**
  - `meetingId` (string, required): The meeting ID or meeting UUID to delete/cancel. When storing this value in your database, store it as a long format integer and not an integer. Meeting IDs can be more than 10 digits. UUIDs starting with '/' or containing '//' are automatically double-encoded as required by the API.
  - `occurrence_id` (string, optional): The meeting occurrence ID to delete a specific occurrence of a recurring meeting. If provided, only that occurrence will be deleted. If not provided and the meeting is recurring, all occurrences will be deleted.
  - `schedule_for_reminder` (boolean, optional): Whether to send a notification to the host and alternative hosts about the meeting cancellation. Set to true to notify the meeting host and alternative hosts.
  - `cancel_meeting_reminder` (boolean, optional): Whether to send a cancellation notification to meeting registrants. Set to true to notify all registrants that the meeting has been cancelled.

### ZOOM_DELETE_IQ_CONVERSATION

**MCP Tool**: `mcp__composio__ZOOM_DELETE_IQ_CONVERSATION`

Tool to delete a Zoom IQ conversation by its ID. Use when you need to permanently remove a specific Zoom IQ conversation from the system. Returns HTTP 200 with empty body on success.

**Parameters:**
  - `id` (string, required): The conversation ID to delete. This is the unique identifier for the Zoom IQ conversation that you want to remove.

### ZOOM_DELETE_IQ_CONVERSATION_COMMENT

**MCP Tool**: `mcp__composio__ZOOM_DELETE_IQ_CONVERSATION_COMMENT`

Tool to delete a comment from a Zoom IQ (Revenue Accelerator) conversation. Use when you need to remove a specific comment from a conversation. The endpoint uses idempotent DELETE behavior, returning success regardless of whether the comment exists.

**Parameters:**
  - `commentId` (string, required): The comment's unique identifier within that conversation.
  - `conversationId` (string, required): The conversation's unique identifier. If the conversation ID begins with '/' or contains '//', it will be automatically double-encoded as required by the API.

### ZOOM_DELETE_IQ_DEAL_ACTIVITIES

**MCP Tool**: `mcp__composio__ZOOM_DELETE_IQ_DEAL_ACTIVITIES`

Tool to delete activities associated with a Zoom IQ deal. Use when you need to remove activity records from a specific deal. Can delete all activities or specific ones by providing activity IDs.

**Parameters:**
  - `id` (string, required): The unique identifier of the deal whose activities should be deleted.
  - `activity_ids` (string, optional): Comma-separated list of activity IDs to delete. If not provided, all activities for the deal will be deleted.

### ZOOM_DELETE_MEETING_RECORDINGS

**MCP Tool**: `mcp__composio__ZOOM_DELETE_MEETING_RECORDINGS`

Deletes all cloud recordings for a meeting or webinar. Requires the account to have Cloud Recording enabled. Rate limit: Light. Required scopes: cloud_recording:delete:meeting_recording or cloud_recording:delete:meeting_recording:admin.

**Parameters:**
  - `action` (string, optional): The delete action to perform: 'trash' (default) moves recordings to trash where they can be recovered; 'delete' permanently removes recordings and cannot be undone.
  - `meetingId` (string, required): The meeting ID or meeting UUID whose cloud recordings you want to delete. If a numeric meeting ID is provided, recordings from the latest meeting instance are deleted. Also accepts webinar ID or UUID. Note: UUIDs starting with '/' or containing '//' are automatically double-encoded as required by the API.

### ZOOM_DELETE_ZRA_CONVERSATION

**MCP Tool**: `mcp__composio__ZOOM_DELETE_ZRA_CONVERSATION`

Deletes a ZRA (Zoom Revenue Accelerator) conversation by ID. Use when you need to remove a conversation from the Revenue Accelerator system. This is an undocumented endpoint that returns HTTP 200 on success.

**Parameters:**
  - `id` (string, required): The ZRA (Zoom Revenue Accelerator) conversation ID to delete. This is a unique identifier for the conversation in Zoom's Revenue Accelerator system.


## Usage Examples

#### ZOOM_ADD_A_MEETING_REGISTRANT
```
Use MCP tool: mcp__composio__ZOOM_ADD_A_MEETING_REGISTRANT
Parameters: {
  "meetingId": "123",
  "first_name": "your_first_name_here"
}
```

#### ZOOM_ADD_A_WEBINAR_REGISTRANT
```
Use MCP tool: mcp__composio__ZOOM_ADD_A_WEBINAR_REGISTRANT
Parameters: {
  "webinarId": "123",
  "first_name": "your_first_name_here"
}
```

#### ZOOM_CREATE_A_MEETING
```
Use MCP tool: mcp__composio__ZOOM_CREATE_A_MEETING
Parameters: {}
```

## Quick Reference

| MCP Tool | Description |
|----------|-------------|
| `mcp__composio__ZOOM_ADD_A_MEETING_REGISTRANT` | Registers a participant for a Zoom meeting that ha... |
| `mcp__composio__ZOOM_ADD_A_WEBINAR_REGISTRANT` | Registers a participant for a Zoom webinar that ha... |
| `mcp__composio__ZOOM_CREATE_A_MEETING` | Enable Zoom meeting creation via user-level apps w... |
| `mcp__composio__ZOOM_CREATE_IQ_CONVERSATION` | Tool to create a Zoom Revenue Accelerator (formerl... |
| `mcp__composio__ZOOM_CREATE_IQ_CONVERSATION_COMMENT` | Tool to create a comment on a Zoom IQ conversation... |
| `mcp__composio__ZOOM_CREATE_IQ_USER_CONVERSATION` | Tool to create a Zoom IQ conversation for a user. ... |
| `mcp__composio__ZOOM_CREATE_ZRA_CONVERSATION` | Tool to create a new conversation in Zoom Revenue ... |
| `mcp__composio__ZOOM_CREATE_ZRA_CONVERSATION_COMMENT` | Tool to create a comment on a Zoom Revenue Acceler... |
| `mcp__composio__ZOOM_CREATE_ZRA_CRM_ACCOUNTS` | Tool to create or update CRM accounts in Zoom Reve... |
| `mcp__composio__ZOOM_CREATE_ZRA_CRM_CONTACTS` | Tool to bulk import or delete CRM contacts in Zoom... |
| `mcp__composio__ZOOM_CREATE_ZRA_CRM_DEALS` | Tool to bulk import or delete CRM deals in Zoom Re... |
| `mcp__composio__ZOOM_CREATE_ZRA_CRM_LEADS` | Tool to bulk import CRM leads into Zoom Revenue Ac... |
| `mcp__composio__ZOOM_CREATE_ZRA_CRM_SETTINGS` | Tool to register a new CRM API integration for Zoo... |
| `mcp__composio__ZOOM_CREATE_ZRA_USER_CONVERSATION` | Tool to create a new conversation in Zoom Revenue ... |
| `mcp__composio__ZOOM_DELETE_A_MEETING` | Delete or cancel a scheduled Zoom meeting. Use occ... |
| `mcp__composio__ZOOM_DELETE_IQ_CONVERSATION` | Tool to delete a Zoom IQ conversation by its ID. U... |
| `mcp__composio__ZOOM_DELETE_IQ_CONVERSATION_COMMENT` | Tool to delete a comment from a Zoom IQ (Revenue A... |
| `mcp__composio__ZOOM_DELETE_IQ_DEAL_ACTIVITIES` | Tool to delete activities associated with a Zoom I... |
| `mcp__composio__ZOOM_DELETE_MEETING_RECORDINGS` | Deletes all cloud recordings for a meeting or webi... |
| `mcp__composio__ZOOM_DELETE_ZRA_CONVERSATION` | Deletes a ZRA (Zoom Revenue Accelerator) conversat... |

## When to Use This Integration

**Use zoom when:**
- Interacting with zoom services
- Automating zoom workflows
- Reading and writing zoom data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:
1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
